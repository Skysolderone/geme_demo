# （本项目未使用 backend 分层）

本项目的分层是 **core**（规则内核 `Siege.Core` / `Siege.Sim`）与 **presentation**（Godot 表现层）。

- 规则内核规范：[`.trellis/spec/core/index.md`](../core/index.md)
- 表现层规范：到子任务 `09-12-tactical-ui` 时创建

此目录下的模板文件由 `trellis init` 生成，未填写，请勿据此开发。
