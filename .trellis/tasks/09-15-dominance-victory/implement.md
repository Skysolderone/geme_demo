# 09-15-dominance-victory — 执行计划

> **执行进度以本文件为准。** `openspec/changes/dominance-victory/tasks.md` 是同一份清单的规格侧快照。
> 实现前先读 `prd.md` → `openspec/changes/dominance-victory/design.md`（D1–D5 + 裁决 1–6）→ 两份 specs。

## 1. 对局配置

- [x] 1.1 `MatchOptions` 新增"碾压起始大回合"（初值 4，0 = 关闭）；开局固定、进入公开视图、入存档、旧存档回填 4 并可查知。算例：插旗阶段公开视图读到 4；进行中修改抛规则异常。
- [x] 1.2 既有测试中测其它规则的建局点显式传 0 关闭碾压；测试方法本身不改。逐个列在提交信息。

## 2. 候选制判定

- [x] 2.1 终局原因 `PowerDominance`（已在枚举末尾追加，保持）。
- [x] 2.2 候选建立：当前大回合 ≥ 起始大回合，结算 / Pass 后若恰有一名参赛玩家满足 `势力 ≥ 其余参赛玩家之和` 且无现存候选 → 成为候选，记录待回应名单。算例：A=210、B=80、C=70、D=55 → 候选 A，名单 B、C、D；A=204 → 无候选；开局首手 5 vs 0 → 第 1 大回合不判；A=B=10、C=D=0 → 多人满足不产生候选。
- [x] 2.3 维持与取消：每个检查点复查，跌破或候选出局 / 弃赛即取消并清空名单；名单玩家完成小回合即移除，出局 / 弃赛亦移除。算例：B 提子后 A=150 < 220 → 取消；取消后再满足 → 重新建立、进度不保留。
- [x] 2.4 成立：名单为空且仍满足 → 候选获胜、立即终局。算例：B、C、D 依次行动后 A 仍满足 → A 获胜。
- [x] 2.5 只统计参赛玩家。算例：A=120、B=60、C 弃赛 90、D 出局 0 → 候选 A、名单只有 B。
- [x] 2.6 优先级：只剩一人 > 碾压 > 棋盘填满 > 整轮 Pass > 达上限，逐对断言。
- [x] 2.7 名次复用 `FinalStandings`，碾压获胜者第 1 名。
- [x] 2.8 候选与待回应名单公开、随存档往返（活对象逐字段 + 逐字节两条腿；往返用非回填值）。

## 3. 遥测、报告与界面

- [x] 3.1 Sim 日志首部记碾压起始大回合；终局原因分布含碾压；报告新增碾压段落（占比、平均触发大回合、获胜者与第 2 名势力比），占比分母为纳入局。
- [x] 3.2 终端 `play` 与 Godot `Names.End` 显示中文终局原因；Godot HUD 与终端版在候选存在时显示一行"玩家 X 碾压中，待回应：…"。

## 4. 设计文档

- [x] 4.1 §12.3 增补势力碾压（候选制、第 4 大回合起、配置为 0 关闭）与优先级说明。
- [x] 4.2 文末变更记录加一行（依据：200 局零出局；首版判定式在稀疏局面退化，改为候选制）。

## 5. 变异验证与回归

- [x] 5.1 变异验证并记录（≥ 7 条）：判定改 `>`、弃赛者计入分母、起始大回合门槛去掉、跌破不取消、名单不清空就判胜、多人满足也建候选、存档不写候选、优先级反转、报告分母用全部局。
- [ ] 5.2 200 局回归（主会话执行）：碾压胜占比、达上限占比、平均结束大回合、第 3 大回合领先者胜率，与基线 25% / 12.5 / 38% 对照。

## 验证命令

```bash
dotnet build
dotnet test
openspec validate dominance-victory
```

## 阻断记录（2026-09-15，implement 阶段）

- **判定式在稀疏局面下退化，全部清单暂停，待项目负责人裁决。** 按 D1/D2 字面接入 `CheckEndConditions` 后：Sim `run --count 4 --max-rounds 6` 4 局全部 `PowerDominance`，合计 4 个小回合（首个落子者势力 X > 0、其余 0 → X ≥ 0；首手 Pass 时全员 0 ≥ 0 同样触发）；既有测试红 55 条。
- 探测（已还原，cmp 校验）：加"第 4 大回合起才判定"门槛 → Sim 20 局 19 局碾压、平均 6.25 大回合；既有测试仍红 32 条（第 5 大回合的稀疏摆盘夹具）。
- 已保留、未接线：`EndReason.PowerDominance`（枚举末尾追加）、`DominanceCheck.FindWinner` 纯函数、`PlayCommand` 文案一行。`MatchFlow.cs` 与 HEAD 无差异，`dotnet test` 583/583。

## 实现备注（2026-09-15，候选制复工）

- 1.2 显式关闭碾压的既有测试共 8 处，均只改建局那一行（`options: MatchFixtures.DominanceOff`），方法体其余不动；夹具默认值保持 4：`基础排序Tests.排除非参赛玩家`、`排名的作用范围Tests.排名不累计`、`先手值公式Tests.参赛人数变化影响公式`、`小回合的五个阶段Tests.快照在回合内不变`、`流程回归Tests.弃赛改变参赛人数后立即重比Pass计数`、`对局持久化Tests.小回合边界存档恢复后状态完全一致`、`终局名次与并列判定Tests.达上限时按同一规则排名`、`三类终局条件Tests.整轮Pass`。其余 575 条在默认开启（4）下原样通过。
- 5.1 变异 M-DV1..M-DV16，15 条红、1 条等价（M-DV14，快照状态替代权威名册，0 红，理由记在 `势力碾压Tests.碾压只统计参赛玩家` 注释）。记录在各测试注释中。
- 5.2 未执行（主会话）。参考：`run --count 20 --max-rounds 15`（起始 4）碾压 14 / 整轮 Pass 6，平均结束 8.35 大回合，碾压局平均触发 6.79，其中 5 局第 4 大回合即结束；第 3 大回合领先者胜率 60%（12/20，样本小）。
