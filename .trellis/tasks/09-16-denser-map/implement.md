# 09-16-denser-map — 执行计划

> **执行进度以本文件为准。** `openspec/changes/denser-map/tasks.md` 是规格侧快照。
> 实现前先读 `prd.md` → `openspec/changes/denser-map/design.md`（D1–D5 + 裁决 1–5）→ 两份 specs。

## 1. 校验区间

- [x] 1.1 `map-definition` 校验放宽：障碍占比 25%–35%、可落子格 80–95、出生区每区 12–14 格；新增"可落子格总数"校验项。算例：109 格的旧图被拒并报告越界；12 格障碍（9.9%）被拒并报告低于 25%。验证：单元测试。
- [x] 1.2 出生区容量校验保持不变（前三大回合 9 枚部署不卡死）。验证：既有测试仍绿。

## 2. 新版地图

- [x] 2.1 设计新障碍布局：11×11 外接，障碍约 36 格，可落子格约 85，四区各 12–14 格，信物格仍 14（出生区 8、公共 6），保持 D2 对称。验证：`MapValidator` 全项通过。
- [x] 2.2 距离容差 ≤ 1：四区到最近公共信物、中央入口、咽喉的最短绕障距离两两差 ≤ 1。验证：校验器通过；测试断言四组距离。
- [x] 2.3 导出 `maps/siege-4p-base-v2.json`，`map` 子命令打印新图。验证：人工看图 + 导出文件存在。
- [x] 2.4 地图 Id 与引用更新。验证：全量测试通过。

## 3. 既有测试

- [x] 3.1 改写写死尺寸 / 可落子格 / 障碍数 / 出生区格数的测试。逐条列入提交信息。
- [x] 3.2 棋盘几何类测试若用到被新障碍占用的坐标，**换坐标重构用例**，不得改断言期望值。逐条列入提交信息并说明新坐标为何等价。

## 4. 遥测与文档

- [x] 4.1 报告首次冲突段落补"盘面占用率"（首次提子时盘面棋子数 ÷ 可落子格），批次均值与分布。验证：单元测试，手算样本核对。
- [x] 4.2 设计文档 §3.3 更新地图描述与障碍占比，§16 说明冲突时点由密度决定；变更记录加一行。验证：人工检查。

## 5. 变异验证与回归

- [x] 5.1 变异验证并记录：校验区间写反、可落子格校验缺失、障碍集合少一格仍通过校验、距离容差判定用直线距离而非绕障。
- [x] 5.2 200 局回归：首次冲突大回合与占用率、平均结束大回合、终局原因分布、第 3 大回合领先者胜率、各棋子势力占比，与 catch-up-recruit 之后对照。验证：如实记录；若平均结束大回合 < 7 立即报告。

## 验证命令

```bash
dotnet build
dotnet test
dotnet run --project src/Siege.Sim -c Release -- map
openspec validate denser-map
```

## 实现记录（2026-09-16）

- 新图 `siege-4p-base-v2`：障碍 36（公共区与通道 28 + 出生区内 8），可落子 85，四区各 13 格，信物 14（出生区 8 / 公共 6），D2 对称，四区到公共信物 / 中央入口 / 咽喉的绕障最短距离精确相等（2 / 6 / 3，与 v1 相同）。
- 障碍种子：公共 `C5 E3 A6 F1 D3 D5 E2 E5`，出生区内 `E1 B4`（都落在出生区斜边 x+y=4 上）。
- 校验器：障碍占比 25%–35%；4 人可落子格 80–95（独立成规则第 6 条 `ValidatePlayableCount`）；新增 `BIRTH_ZONE_SIZE_OUT_OF_RANGE`（4 人图每区 12–14；2 / 3 人图规格未给区间，不校验）。
- 变异验证：M-DM1…M-DM9，全部红，明细见实现报告。

## check 阶段的变异验证记录（2026-09-16，基线 658 绿 / 退出码 0）

testing.md「没有记录的变异验证等于没做」：下表是 check 阶段实测的红数，不照抄实现阶段的数字。
还原一律 `cp` 备份 + `cmp` 校验，不用 `git checkout`。

| 编号 | 改了哪一行 | 红数 | 红在哪 |
|---|---|---:|---|
| M-CK1 | `MapValidator.ValidateDistanceBalance`：`max - min > map.DistanceTolerance` → `> map.DistanceTolerance + 1` | 1 | `地图静态校验规则Tests.距离极差恰好等于容差时通过` |
| M-CK2 | `Budgets[4]` 上界 `95` → `115` | 2 | `人数适配预算Tests.格数超出预算`、`地图静态校验规则Tests.可落子格越界` |
| M-CK2' | 上界判据 `playable > budget.MaxPlayable` → `> budget.MaxPlayable + 50`（只改行为不改消息） | 3 | 上面两条 + `人数适配预算Tests.三人预算区间` |
| M-CK3 | `Budgets[4]` 下界 `80` → `60` | 2 | 同 M-CK2（但两条都只是**消息文本**断言在红） |
| M-CK3' | 下界判据 `playable < budget.MinPlayable` → `< budget.MinPlayable - 20`（只改行为不改消息） | 补测前 1 / 补测后 2 | 补测前只有 `人数适配预算Tests.两人预算区间`（2 人档），4 人档下界零行为覆盖；check 在 `可落子格越界` 补了 75 格算例后该条也红 |
| M-CK4 | `Budgets[4]` 出生区下界 `12` → `9` | 1 | `地图静态校验规则Tests.出生区可落子格超出区间` |
| M-CK5 | `MatchSession` 写入端 `PlayableCells = Match.Map.PlayableCount` → `Match.Map.Width * Match.Map.Height`（占用率分母改成外接面积） | 1 | `冲突占用率口径Tests.真实跑局把可落子格写进日志首部` |
| M-CK6 | `BalanceAnalyzer`：首次提子快照改成**最后一次**提子快照（`conflictRound is null &&` 移出条件） | 1 | `冲突占用率口径Tests.冲突时的盘面占用率` |
| M-CK7 | `MapFile.ToJson`：`Obstacles = Sorted(map.Obstacles)` → `Obstacles = []` | 4 | `地图文件往返Tests` ×3、`地图文件健壮性Tests.全小写键名的地图能正确加载`；`磁盘上的基准地图文件与代码一致` **不红**（它是 `ToJson(a) == ToJson(b)` 的恒等形状，对 `ToJson` 漏字段天然盲，靠往返测试兜住） |

独立复核（check 侧自写 BFS，不调用 `MapValidator`）：v1 与 v2 的四区距离都是公共信物 2 / 中央入口 6 / 咽喉 3，
`基准地图对称性Tests` 里新钉的 `[2, 6, 3]` 不是抄实现输出；v2 障碍 36、可落子 85、四区区形 15 / 可落子 13；
`必死口袋` 用的新墙只圈出唯一一块 7 格连通空区 `{B1 C1 D1 A2 B2 D2 A3}`，全盘连通块恰为 2。
- 5.2 的 200 局回归留主会话执行；20 局冒烟：首次冲突平均第 5.36 大回合（v1 为 6.52），占用率均值 65.6%，平均结束第 9.82 大回合。
- 5.2 主会话 200 局回归结果（种子 1–200，与 v1 基线 `sim-out/cur-regress` 同口径，输出 `sim-out/denser-map-200`）：
  - 首次冲突 **5.46** 大回合（v1 6.52），分布 5×92 / 6×37 / 7×7 / 8×3 / 9×1，整局无冲突 60 局；冲突时占用率均值 **65.3%**（proposal 按 v1 曲线推的 62% 基本成立）。
  - 终局局平均结束 **9.43** 大回合（v1 11.15），落入 §16 的 7–10 目标；平均结束大回合 < 7 的报告条件未触发。
  - 终局原因 AllPassed×135 / MajorRoundLimit×55 / PowerDominance×10；**不收敛率 27.5%（v1 11.5%）**，这是本次唯一的负向指标。
  - 第 3 大回合领先者胜率 **64.5%**（v1 46.5%，目标 ≤50%）；四家胜率 46/49/56/49。
  - 归因（不属于本 change，转下一轮）：不收敛的根因是 `EvaluationWeights.Default.Safety=20` 未按新图校准——该值的 doc 注释本就标为"阶段 B 的首个校准项"。v2 上扫档 200 局/档：Safety 5→9.5%、10→31.5%、20→27.5%、30→34.0%、40→42.0%、60→57.5%，非单调且调高更糟。Safety=5 同时把首次冲突压到 4.68、整局无冲突降到 3 局、领先者胜率降到 51.0%。
  - 40 回合上限对照（`sim-out/denser-map-r40`）：55 局里 26 局在第 16–40 回合自然收敛，29 局（14.5%）仍不收敛；这 29 局第 31–40 回合落子 40.6 次、被提 40.6 次，净增长为零。
