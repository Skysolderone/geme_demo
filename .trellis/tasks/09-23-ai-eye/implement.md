# 09-23-ai-eye 实施记录

## 段 A（tasks 0.1、1.1–1.5）——九维结构、眼位、威胁、活形中性

### 0.1 前置核对（按 R4）

- `openspec/changes/archive/` 下有 `2026-09-22-restore-go-core-rules`、`2026-09-22-life-shape`、`2026-09-21-frontier-map`。开工时 `openspec list` 只有 `ai-eye`；段 A 进行中出现了 `terrain-surfaces`（0/36，非本任务创建，未触碰）。
- 基线数据：`sim-out/restore-smoke20/`（20 局 + config / report / summary）、`sim-out/life-shape/baseline200/`（200 局 + config / report / summary），均为未校准口径。
- 改动前的二进制在 `baseline200/config.json`（种子 1–20）上重跑，小回合快照与 `baseline200` 前 20 局逐条相同（1252 个小回合），即 HEAD 可复现该基线。
- `openspec validate ai-eye --strict` 通过。

### 改了什么

| 文件 | 改动 |
|---|---|
| `src/Siege.Core/Ai/EvaluationWeights.cs` | `EvaluationDimension` 末尾追加 `Eye`、`Threat`（前七维下标不变）；`EvaluationWeights` 增加两个位置参数 `Eye`、`Threat`，`Default` 取 0，依据段写明"未校准、段 D 扫档"；`Of` 分派两维。旧配置 / 旧首部缺这两个键时按 0 读入 |
| `src/Siege.Core/Ai/EvaluationBreakdown.cs` | `DimensionCount = 9`；新增 `Version = 2`（AI 评价版本：1 = 七维，2 = ai-eye 九维）；`ToString` 输出九项（`… 供给N 眼位N 威胁N = 总分`） |
| `src/Siege.Core/Ai/BatchEvaluator.cs` | 非简单难度时，前后盘面各做一次 `LifeShapeReport.Analyze`，由安全、眼位、威胁三维共用（每个盘面查一次，不跨盘面缓存）；新增 `EyeOf`（己方眼空间眼值之和，每块眼空间只计一次 + 己方活形棋串数 × `AliveGroupEyeBonus = 3`）、`ThreatOf`（参赛敌方、非活形、气数 ≤ 3 的棋串棋子总数）；两者都取前后差；`SafetyOf` 改为遍历活形查询里的己方棋串 |
| `src/Siege.Core/Ai/GroupSafety.cs` | 删除自带的眼点循环与 `EyePoints`；参数改为 `(Liberties, EyeValueSum, DispersedLiberties, Size, IsAlive)`，`Analyze(GameBoard, GroupLife)`；`TwoEyePotential = min(2, EyeValueSum)`；已确定活形时 `Score` 恒为 `AliveScore = 4 + 2 × 6 + 2 = 18`（公式上界）；分散气循环保留（只看气与气是否相邻，不看占用者） |
| `src/Siege.Sim/Logging/MatchLog.cs` | `LogHeader.AiEvaluationVersion`（`int?`，旧日志为 null，不回填） |
| `src/Siege.Sim/Running/MatchSession.cs` | 首部写入 `AiEvaluationVersion = EvaluationBreakdown.Version`；落子事件的分解值随 `DimensionCount` 自动带上 `Eye` / `Threat` 两项 |
| `.trellis/spec/core/boundaries.md` | "活形 / 禁入"一行里 `GroupSafety.EyePoints 暂与之并存，去留归 ai-eye` 已过时，改写为已删除并注明守门 |
| `openspec/changes/ai-eye/tasks.md` | 勾选 0.1、1.1–1.5 |

1.5 的结论：`EnemyLoss` 现在只有"参赛敌方势力的实际下降 + 提子数 × 2"两项，**没有**潜在提子价值项，所以不需要改实现；用「不攻打活棋」里的一条断言钉住"攻活棋时敌损只等于已实现的下降"，并用变异 M-A5 证明它能红。

### 测试

新增（测试类 = Requirement，方法 = Scenario）：

- `启发式评价维度Tests`：`做出第二个眼获得眼位正贡献`（= 4，同一批次简单难度为 0）、`自填眼位不得分`（未定 −1、活形 −4）、`点直三中间眼位增量为眼值1加活形3`（= 4）、`已活时点直三中间眼位增量只有眼值1`（= 1）、`共享眼空间只计一次`（一块单格眼同时是两条己方棋串的眼空间，原始值 = 1，不是 2）、`叫吃获得威胁正贡献`（= +5）、`AI不自带眼判定只经活形查询`（源码扫描守门）、`九维权重经配置往返且旧配置缺两维按0读入`（往返用非回填值 5 / 7）、`日志首部写入AI评价版本且分解值为九项`。
- 新类 `已确定活形的评价中性Tests`：`活棋补气不加分`、`做活在安全维度上是净收益`、`不攻打活棋`（含 1.5 的敌损断言）。

先红：实现前跑 `AiDecision`，7 条红（眼位 4 条、威胁 1 条、`活棋补气不加分`（实际 25）、源码扫描守门）；`做活在安全维度上是净收益` 与 `不攻打活棋` 在旧实现下本来就成立（旧公式做活也加分；旧实现不算威胁，恒 0），它们靠变异 M-A2 / M-A5 证明有效。`共享眼空间只计一次` 是实现之后补的：自查时发现，全部眼位算例都只有一条己方棋串，"按棋串求和"与"按眼空间求和"在这些算例上结果相同，属于等价变异。补上这条测试后，由 M-A7 证明它能红。

既有测试的改写 / 删除（逐条）：

1. `启发式评价维度Tests.评价覆盖七个维度` → 改名 `评价覆盖九个维度`（规格 Scenario 改名）；加断言：维度数 9、枚举值 9 个、分解文本含"眼位""威胁"。其余断言不变。
2. `启发式评价维度Tests.两眼潜力近似区分活形与死形` → 改写为 `两眼潜力取自活形查询`：原测试钉的是被删掉的眼点循环（`EyePoints`，以及旧变异 M-A10"放宽单点气判定"），已无对象。新测试用同样两个盘面：活形取常数 18，`EyeValueSum` 2；直三的眼值按活形查询是 1（旧循环给 0，这正是两套判定的分歧），得分 `4 + 1 × 6 + 0 = 10`。旧 check 变异 M-C2（`× 6` 改 `× 0`）在新测试上重跑仍红。
3. `启发式评价维度Tests.权重可配置`：两处 `new EvaluationWeights(…)` 补 `Eye: 0, Threat: 0`（新增的位置参数），期望不变。
4. `默认评价权重的校准Tests.默认权重被改动`：加 `Eye 0`、`Threat 0` 两行 InlineData，按属性读的 switch 补两维；`默认权重的校准依据随值一起更新` 的逐维清单补 `Eye`、`Threat`（源码里须有 `Eye = 0`、`Threat = 0`）。
5. `难度分级Tests.简单难度只看即时收益`：简单难度必须为 0 的维度清单补 `Eye`、`Threat`（注释"五维"改"七维"）。
6. `SimFixtures.RankedMatch`、`地形改造日志与分析Tests.地形可离线重建` 的写死权重补 `Eye: 0, Threat: 0`。
7. `地形改造日志与分析Tests.地形可离线重建`：**重挑种子 17–19 → 3–5**（断言与期望未改，只换样本）。1.4 之后走法变了，17–19 的致提子改造变为 0 / 0 / 0，"样本里确实有致提子的改造"这一下界响亮失败。用同一份写死权重（Safety 27、Eye / Threat 0）的 CLI 探针重扫种子 1–24；探针配置先用改动前的二进制复现了 17–19 的"改造 2 / 2 / 3、致提子 0 / 1 / 1"。新结果里只有种子 3、4 各有 1 次致提子；取 3–5，改造 5 / 6 / 1、致提子 1 / 1 / 0。
8. `候选格上限Tests.缺省不限制时标准图整局与改动前逐步相同`：黄金哈希 `CDEB4C13…70084563` → `14E1B0D2…104CBA72`，**走法确实变了**。1.1 完成时该测试仍以原值通过（1.1 之后那次全量测试包含它）。1.4 之后改动前、后的二进制各跑种子 31、24 个小回合，第 1 个小回合（P3）就分叉：旧落点是三枚要塞 B10 / B11 / C12，安全 12；新落点把 B11 换成普通子，安全 36（B10-B11 与 C12 两条棋串共享平台角上一块 8 格、眼值 2 的眼空间，都已确定活形，各取常数 18），眼位原始值 8 = 2 + 2 × 3（权重 0，不进总分）。新值连跑两次一致。另外，该断言改为 `Assert.True(golden == hash, "…现为 {hash}")`，失败时打印完整哈希（原 `Assert.Equal` 会截断）。

测试数：改动前 1266 通过、2 跳过；段 A 后 **1280 通过**、2 跳过（+14 = 新增 12 个测试方法 + `默认权重被改动` 的 2 行 InlineData）。

### 变异

脚本：scratchpad `mutate.py`。做法：二进制读写；锚点按文件实际行尾归一，并断言命中 1 次；还原放在 finally；备份名带时间戳；还原后与原文逐字节比对，再 `os.utime`；解析 `Passed!/Failed!` 统计行；不用 `if (false)`。下表的红数是在 `--filter FullyQualifiedName~AiDecision` 下统计的（M-A6t 用 `~默认评价权重的校准`）。9 条全部 EXIT 1，还原后全部逐字节一致；最后一条变异之后重新构建并跑全量测试，0 红。红数同时写进了对应测试的注释。

| 编号 | 改了哪里 | 红 | 红的测试 |
|---|---|---|---|
| M-A1 | `Evaluate`：眼位改取结算后的绝对值（`EyeOf(life) - _eyeBefore` → `EyeOf(life) - 0L`） | 4 | 做出第二个眼获得眼位正贡献、**自填眼位不得分**、点直三中间眼位增量为眼值1加活形3、已活时点直三中间眼位增量只有眼值1 |
| M-A2 | `ThreatOf`：删掉 `\|\| group.Life == LifeState.Alive`（不排除活形敌串） | 1 | **不攻打活棋** |
| M-A3 | `GroupSafety.Score`：`if (IsAlive)` → `if (IsAlive && Size < 0)`（运行时恒假，活棋仍走公式） | 2 | **活棋补气不加分**、候选格上限（黄金哈希） |
| M-A4 | `GroupSafety` 分散气循环里插入 `_ = board[n].Occupant is { } o && o.Owner == life.Group.Owner;` | 1 | **AI不自带眼判定只经活形查询** |
| M-A4b | `BatchEvaluator.SafetyOf` 里插入一行 LINQ 眼判定（`LibertyNeighbors(c).All(n => board[n].Occupant … Owner == _me)`） | 1 | **AI不自带眼判定只经活形查询** |
| M-A5（1.5） | `Evaluate`：敌损加一项不排除活形的"潜在提子价值"（全部非己方棋串中气数 ≤ 3 者的棋子数，不排除活形） | 2 | **不攻打活棋**、评价覆盖九个维度 |
| M-C2（重跑） | `Score`：`TwoEyePotential * EyeWeight` → `* 0` | 2 | **两眼潜力取自活形查询**、候选格上限（黄金哈希） |
| M-A7 | `EyeOf`：改为对己方棋串的 `EyeValueSum` 求和（共享眼空间被重复计） | 1 | **共享眼空间只计一次** |
| M-A6t | 只改测试、不改实现：`默认权重被改动` 的 Supply / Eye 两行期望对调（2 ↔ 0） | 2 | 默认权重被改动（Supply、Eye 两行） |

源码扫描守门的反面自证，写在测试里：扫描器必须认出两个样本——段 A 之前 `GroupSafety` 的眼点循环原文，以及一行 LINQ 写法；口径下界是扫描文件数 ≥ 10、命中邻格枚举 ≥ 1 处。

### 决策序列比对（种子 1–20，`baseline200/config.json`：v5、4 名 Standard AI、`--retention Full`）

投影规则：去掉首部与耗时字段；落子事件的分解值里去掉 `Eye` / `Threat` 两个键；候选文本里去掉"眼位N 威胁N"两项。比对脚本是 scratchpad 里的 `seqcmp.py`。

1. **1.1 完成后（九维结构、首部版本，新两维权重为 0、尚未计算）**：与改动前**逐步相同**，20 / 20 局一致。投影 33617 行，含 1252 个小回合快照，以及候选、预演、落子分解、征募等全部事件。旧配置只有七个权重键，读入后新两维为 0。黄金哈希在这一步没变（全量 1270 条全绿）。
2. **归因探针（段 A 完成后）**：两份二进制都把 Safety 权重设为 0、其余权重不变，跑种子 1–20，小回合快照 **20 / 20 局逐步相同**（9567 个小回合）。结论：眼位、威胁已真实计算但权重为 0，且把新 Safety 公式的影响排除之后，段 A 不改变任何决策；走法的变化全部来自 1.4 的 Safety 公式。
3. **段 A 完成后（默认权重）**：20 / 20 局分叉——17 局在第 1 个小回合、3 局在第 2 个小回合，与 D2 活形中性的预期一致。开局平台角上的棋串按活形查询已是已确定活形，拿安全常数 18。20 局全部以 AllPassed 结束；平均结束大回合 18.6 → 10.85，平均小回合 62.6 → 40.65；墙钟 77.9 s → 24.7 s（对局变短）。**这些数字属于未校准口径，只作参照，不作基线。**

### 段末自验

- `dotnet build siege.sln`：0 警告 0 错误
- `dotnet build src/godot/Siege.Godot.csproj`：0 警告 0 错误
- `dotnet test -c Release`：1280 通过、2 跳过、0 失败（RC 0，在最后一条变异之后跑）
- `openspec validate ai-eye --strict`：valid

### 待决

1. **威胁的"敌方"口径**：目前只计参赛（Active）敌方，与敌损"参赛敌方势力下降"同一口径；弃赛 / 出局者遗留的非活形棋串不计入威胁。design D1 原文只写"敌方"，请负责人确认。
2. **"直三点中间"两条算例的解读**：tasks 1.2 写的是"未成活形时增量 1；成活形时 +3"。同一棋串在点中间后必然眼值 ≥ 2、成为活形，所以"未成活形"的情形按"本手没有新增活形"来实现，即棋串本就已活（另有单格眼），点直三中间只得眼值增量 1。若原意是别的盘面（例如两个单格眼分属两条棋串），需另补算例。
3. **与 life-shape R8（活形过易）的耦合**：开局第 1 个小回合，平台角上的 3 子就已是已确定活形，于是活形中性把安全维直接推到常数上限（种子 31：安全 12 → 36），20 局平均结束大回合 18.6 → 10.85。这正是 R3 要求段 D 开工前先裁决 R8 的原因；段 A–C 不受影响。
4. **性能**：非简单难度下，每次评价都会多做一次 `LifeShapeReport.Analyze`（每个盘面一次，三维共用）。段 C 做缓存与耗时对比，可以先拿这组粗略参照：种子 1–20（Standard、v5、28 路并行，非受控测量），每个小回合的平均耗时由改动前的 567639 ms / 1252 ≈ 453 ms 变为段 A 后的 324769 ms / 813 ≈ 399 ms。
5. `openspec list` 里新出现 `terrain-surfaces`（0/36），不是本任务所建，也未触碰；0.1 的"只剩本 change"只对开工时刻成立。

## 段 B（tasks 2.1–2.5）——活形硬约束、停手阈值、日志 / CLI、难度

### 改了什么

| 文件 | 改动 |
|---|---|
| `src/Siege.Core/Ai/BatchEvaluator.cs` | 新增 `TryEvaluate(placements, result, context, out evaluation)`：先判活形硬约束，淘汰即返回 `false`（`evaluation` 为 null，不打分），否则走 `Evaluate`。判据 `ViolatesLifeConstraint`：本批次无提子，且（批次开始前己方已确定活形棋串的任一棋子在预演后所在棋串不再是己方已确定活形，或任一落点是批次开始前己方的单格眼）。构造时的活形查询改为**无条件**做一次（硬约束对全部难度生效），缓存 `_ownAliveStones`、`_ownSingleEyes`；三维仍只在非简单难度下计算。己方无活形棋串时不对预演盘面做活形分析。眼 / 活形只来自 `LifeShapeReport` |
| `src/Siege.Core/Ai/HeuristicTurnController.cs` | `RankPoints`、`Greedy` 两处在"预演合法之后"改经 `TryEvaluate`：淘汰即跳过（Greedy 先 `Unstage`），被淘汰的候选不进 `LastPointRanking`；`Greedy` 改为实例方法，保留条件 `next.Total > current.Total` → `next.Total - current.Total > Config.PassThreshold`。`PrefilterCells` 未动（段 C） |
| `src/Siege.Core/Ai/AiDifficulty.cs` | `AiSearchConfig` 增加位置参数 `PassThreshold = 0`（构造缺省 0，使该项出现之前记录的 `Search` 按 0 读入）；三档预设显式取 `DefaultPassThreshold = 20`（= 2 × PowerGain 10，未校准，常量 `PassThresholdCalibrationStatus`）；`Validated()` 拒负值；枚举注释"七维"改"九维"并写明硬约束 / 阈值三档共用（D7） |
| `src/Siege.Sim/Config/RunConfig.cs` | `int? PassThreshold`（null 不写出）；`Validated` 拒负值；`ResolvedFor` 在未配置时落成 `DefaultPassThreshold`，因此批次 `config.json` 与日志首部都记实际生效值 |
| `src/Siege.Sim/Running/MatchSession.cs` | `PassThreshold = Config.PassThreshold ?? (recorded ? 0 : Default)`：新建的局取缺省值，按首部重建时缺字段按 0；`AttachConfigured` 给未显式配置 `Search` 的玩家套上该阈值（显式 `Search` 自带阈值，原样生效，与 K 同口径） |
| `src/Siege.Sim/Program.cs` | `run` 新增 `--pass-threshold`（严格 CLI 认领）与用法说明 |
| `openspec/changes/ai-eye/tasks.md` | 勾选 2.1–2.5 |

Godot 与终端 `play` 两个入口经 `AiSearchConfig.ForMap` 取预设，自动得到阈值 20，没有改动（R6：Godot 侧不碰）；`play` 没有 `--pass-threshold`（见待决 4）。

### 测试

新增（测试类 = Requirement，方法 = Scenario）：

- 新类 `活形硬约束Tests`（6 个）：`不拆自己的活形`、`不填自己的单格眼`、`点三格眼空间的中间不被淘汰`、`为提子而填眼放行`、`用改造拆自己的活形也被淘汰`、`全部候选被淘汰则Pass`。每条先用断言钉住前提：候选在规则层合法（Core 的禁入 / 破坏活形只管非己方）、不提子、确实失活或确实是单格眼。排行把 N 开到 1000，覆盖全部合法候选，所以"扣分留在候选集"一定会出现在排行里。两个条件分开测：单格眼 → `不填自己的单格眼`（原本就未定），失活 → `用改造拆自己的活形也被淘汰`（地图预置栅栏 A1–A2、J1–J2，一排 B1…H1 两端各一个单格眼；匠人落 D2、立栅 D1–E1，切成两条未定棋串）。"填眼 + 提子"只能出现在批次层（单格眼的气边邻居全是己方），所以 `为提子而填眼放行` 直接调 `TryEvaluate`：只填眼 → 淘汰（反面），A1 + J8 提 J9 → 放行并打分。
- `难度分级Tests.简单难度也不拆自己的活形`（1 个）。
- 新类 `停手阈值Tests`（11 个）。七个 Scenario：`零收益不落子`、`低于阈值不落子`、`恰等于阈值不落子`、`高于阈值落子`、`无落点被保留则Pass`、`阈值为0时零变化`、`阈值进入记录`。另有 2.4 的四条：`阈值须为非负整数`、`跑局配置的停手阈值文本往返`、`首部缺停手阈值的旧日志按0回放`、`命令行停手阈值写入配置记录`。边界 15 / 20 / 21 的做法：E5 被 P0 三面、P1 一面夹住，部署上限 1、合法范围只有 E5；用断言钉住原始值（PowerGain 1、EnemyLoss 0）后，把 PowerGain 权重设成 15 / 20 / 21，于是总分恰为该值（简单难度下 Pass 总分为 0，边际提升 = 总分）。`阈值为0时零变化` 的对照值 `8EEC49A7…04679FC8` 取自严格提高实现（2.2 完成、2.3 之前）的实际运行；往返测试的样本值用 7 / 3，既非缺省 20，也非缺字段回填 0。
- `默认评价权重的校准Tests.默认停手阈值被改动`（1 个）：钉住取值 20、三档共用，以及源码里的未校准标注与依据段（`PassThreshold = 20`）。

先红：2.1 的 7 条在桩实现（`TryEvaluate` 恒放行）下 6 红；`点三格眼空间的中间不被淘汰` 按其性质本来就绿，靠 M-B3 证明有效。2.3 / 2.4 的 11 条在桩实现（字段存在但未接线、预设阈值 0）下 7 红。本来就绿的 4 条：`零收益不落子`、`高于阈值落子`、`阈值为0时零变化`（旧实现就是严格提高）与 `跑局配置的停手阈值文本往返`（桩属性已可序列化），分别由 M-B6、M-B15、M-B14、M-B9 证明有效。`默认停手阈值被改动` 写好后直接实现，没有单独跑出先红，由 M-B12t、M-B16 证明有效。

既有测试的改写（逐条，无删除）：

1. `候选格上限Tests.剪枝参数的文本往返与旧文本兼容`：旧文本（无阈值字段）的期望由 `Standard` 改为 `Standard with { PassThreshold = 0 }`。这正是"缺字段按 0"的体现：预设阈值是 20，旧文本不再等于预设本身。另加一条非缺省值 7 的往返。
2. `候选格上限Tests.缺省不限制时标准图整局与改动前逐步相同`：黄金哈希 `14E1B0D2…104CBA72` → `D1481DD5…772968BD`，**走法确实变了**，归因分两步见下节。另外 `TurnHash` 与 `V4GoldenTurnHash` 由 private 改 internal，供 `停手阈值Tests` 共用，行为不变。
3. `难度分级Tests.简单难度只看即时收益`：加 3 条断言——简单难度的阈值 = 缺省值 = 标准 = 困难（R1 三档共用，D7 简单难度同样走阈值）。其余不变。
4. `批量跑局Tests.批量执行并汇总`：`config.json` 与各局首部配置的期望由 `config.Effective()` / `config` 改为 `config with { PassThreshold = Default }`，两处。原因是规格要求实际生效的阈值写入配置记录与首部，未配置时落成 20；另加 `Assert.Null(config.PassThreshold)`，证明写出的 20 是落成的，不是输入里带的。
5. `可复现回放Tests.失败局可复现`：首部配置期望同上加 `PassThreshold = Default`。
6. `对局日志的记录内容Tests.揭示时间可查`：**只换样本** `Sample[1]` → `Sample[0]`，断言与期望未改。停手阈值之后，种子 12 在 16 个小回合内揭示了全部 13 枚信物，"整局未揭示标 null"这一支没有样本，下界响亮失败。种子 11 在改动前、硬约束后、阈值后都恰有 1 枚未揭示（CLI 探针三份二进制实测）。
7. `地形改造日志与分析Tests.地形可离线重建`：**只换样本**，种子 3–5 → 53–55，并把停手阈值写死为 20（与写死权重同理）。硬约束之后，1–24 里只剩种子 4 有 1 次致提子改造；阈值之后 1–24 全为 0，下界响亮失败。同一份写死配置用 CLI 探针扫种子 25–200，有 20 局出现致提子改造；取连续、且每局都有改造的 53–55，改造 1 / 5 / 3 次，致提子 1 / 1 / 1 次。

测试数：段 A 后 1280 通过、2 跳过 → 段 B 后 **1299 通过**、2 跳过（+19 = 新增 19 个测试方法）。

### 变异

脚本：scratchpad `mutateB.py`（沿用段 A 的 `mutate.py` 骨架：二进制读写，锚点按行尾归一且命中 1 次，还原放在 finally，还原后逐字节比对并 `os.utime`）。过滤：`FullyQualifiedName~AiDecision|FullyQualifiedName~SimulationHarness`；M-B12t 只跑 `~默认评价权重的校准`，M-B15 / M-B16 只跑 `~AiDecision`。16 条全部 EXIT 1，还原后全部逐字节一致。最后一条变异之后重新构建并跑全量测试，0 红（1299 通过）。红数也写进了对应测试的注释。

| 编号 | 改了哪里 | 红 | 红的测试 |
|---|---|---|---|
| M-B1 | `TryEvaluate`：淘汰改为扣分（违例候选照常打分，PowerGain 原始值减 10^9，返回 true） | 6 | 不拆自己的活形、不填自己的单格眼、为提子而填眼放行、用改造拆自己的活形也被淘汰、全部候选被淘汰则Pass、简单难度也不拆自己的活形 |
| M-B2 | 判据去掉"无提子"条件 | 1 | **为提子而填眼放行** |
| M-B3 | 单格眼判据去掉"格数为 1"（己方任意眼空间的格都算） | 4 | **点三格眼空间的中间不被淘汰**、零收益不落子、阈值为0时零变化、候选格上限黄金哈希 |
| M-B4 | 去掉"原活形失活"条件（棋子仍在盘上即视为未失活） | 1 | **用改造拆自己的活形也被淘汰** |
| M-B5 | 简单难度跳过硬约束（判据开头加 `_immediateOnly` 短路） | 1 | **简单难度也不拆自己的活形** |
| M-B6 | 保留条件 `>` 改 `>=` | 3 | 零收益不落子、恰等于阈值不落子、候选格上限黄金哈希 |
| M-B7 | 贪心仍按严格提高（`> 0`），不看阈值 | 4 | 低于阈值不落子、恰等于阈值不落子、无落点被保留则Pass、候选格上限黄金哈希 |
| M-B8 | `ResolvedFor` 不落成缺省阈值 | 6 | 阈值进入记录、首部缺停手阈值的旧日志按0回放、失败局可复现、纯AI局可凭种子复现、旧日志缺摘要字段…、批量执行并汇总 |
| M-B9 | `RunConfig.PassThreshold` 标 `[JsonIgnore]` | 7 | 跑局配置的停手阈值文本往返、阈值进入记录、首部缺…按0回放、命令行…、失败局可复现、纯AI局可凭种子复现、旧日志缺摘要字段… |
| M-B10 | 按首部重建时缺字段取缺省阈值而不是 0 | 1 | **首部缺停手阈值的旧日志按0回放** |
| M-B11 | `Program.Run` 不读 `--pass-threshold` | 1 | **命令行停手阈值写入配置记录** |
| M-B12t | 只改测试：默认停手阈值期望 20 → 21 | 1 | 默认停手阈值被改动 |
| M-B13 | `AiSearchConfig.Validated` 不拒负阈值 | 1 | **阈值须为非负整数** |
| M-B14 | 会话建 AI 时不传跑局配置的阈值 | 2 | 阈值进入记录、**阈值为0时零变化** |
| M-B15 | 保留条件 `> 阈值` 改 `> 阈值 + 1` | 2 | **高于阈值落子**、无落点被保留则Pass（int.MaxValue + 1 溢出为负） |
| M-B16 | 简单难度预设漏写阈值（回落到构造缺省 0） | 2 | 默认停手阈值被改动、简单难度只看即时收益 |

另记：M-B6（`>` 改 `>=`）**不会**让 `阈值为0时零变化` 变红。种子 31 的前 24 个小回合里没有边际提升恰为 0 的候选，阈值 0 下 `>` 与 `>=` 走法相同；这条变异由 `零收益不落子`、`恰等于阈值不落子` 挡住，测试注释已按实测改写。

### 决策序列比对（种子 1–20，`seq-A/config.json`：v5、4 名 Standard AI、`--retention Full`；脚本 scratchpad `seqcmp.py`）

1. **基线确认**：改动前（HEAD = 段 A 提交）的二进制重跑，与段 A 留存的 `seq-A` 逐步相同（20 / 20，投影 6903 行、813 个小回合）。
2. **硬约束归因探针**：把 2.2 完成后的判据临时短路成"恒不淘汰"，其余改动（`TryEvaluate` 接线、无条件的批次前活形查询）全部保留；种子 1–20 与改动前**逐步相同**（20 / 20，含候选与预演事件）。所以 2.2 引起的走法变化全部来自淘汰本身。探针源码还原逐字节一致并已 `os.utime`。
3. **2.2 完成后（严格提高 + 硬约束）vs 改动前**：19 / 20 局分叉（快照口径）。
4. **阈值为 0 vs 严格提高（2.2 完成、2.3 之前的二进制）**：`--pass-threshold 0` 跑种子 1–20，**逐步相同**（20 / 20，投影 4346 行、592 个小回合，含候选、预演与落子分解事件）。
5. **缺省阈值 20 vs 严格提高**：20 / 20 局分叉。

黄金哈希（种子 31、24 个小回合）按两步归因，写进了测试注释：① 硬约束：`14E1B0D2…` → `8EEC49A7…04679FC8`，第 23 个小回合（第 6 大回合，P2）起分叉，旧落点 E12（协同子）是被 v5 地形围成的 P2 单格眼，四邻只有 F12 有气边，新实现 Pass；② 阈值 20：`8EEC49A7…` → `D1481DD5…`，第 16 个小回合（第 4 大回合，P0）起分叉，旧选中批次 [G3 普通, G4 连珠, J9 要塞] = 1422 中，G3 是最后加入的一枚、边际提升 1422 − 1402 = 20，恰等于阈值而被撤回，同分 1422 的另一候选 [G3 要塞, G4 普通, J9 连珠] 当选。阈值取 0 时整局重现 `8EEC49A7…`（`停手阈值Tests.阈值为0时零变化`）。

### 20 局粗略参照（v5、4 名 Standard AI、种子 1–20；**未校准口径**：九维中 Eye / Threat 权重 0，其余七维沿用旧值，PassThreshold = 20 未校准）

| 二进制 | 平均结束大回合 | 平均小回合 | turn_limit 截断 | 终局原因 | 整局无提子 |
|---|---|---|---|---|---|
| 改动前（段 A） | 10.85 | 40.65 | 0 | AllPassed 20 | 13 / 20 |
| 2.2 后（硬约束） | 7.65 | 29.60 | 0 | AllPassed 20 | 16 / 20 |
| 段 B 完成（硬约束 + 阈值 20） | **7.70** | 29.45 | **0** | AllPassed 20 | **19 / 20** |

这组数字只作参照，不作基线；引用时 MUST 注明上面的权重口径。

### 段末自验

- `dotnet build siege.sln`：0 警告 0 错误
- `dotnet build src/godot/Siege.Godot.csproj`：0 警告 0 错误
- `dotnet test -c Release`：1299 通过、2 跳过、0 失败（RC 0，在最后一条变异之后跑）
- `openspec validate ai-eye --strict`：valid

### 待决

1. **整局无提子占比偏高**：20 局参照里 19 / 20 局整局无提子（段 A 为 13 / 20）。D6 选档准则第 2 条要求不高于 5%。这与 life-shape R8（活形过易：v5 平台角、地形小空区一开局就是活形 / 单格眼）和 design Risks"各围各的"直接相关，R3 规定须在段 D 开工前裁决 R8。
2. **AI 评价版本是否要递增**：`EvaluationBreakdown.Version` 的约定是"维度数或原始值算法变化才递增"，本段两项都不属于这类，所以仍为 2。但段 A 产出的 v2 日志用新二进制回放时，会因硬约束而分叉（阈值缺字段按 0 只能恢复阈值那一项）。是否提到 3、或在首部另记"硬约束"标记，请主会话裁决。
3. **硬约束是否也进 `PrefilterCells`**：候选格上限 K 的预筛阶段目前不过硬约束，K > 0 时一个会被淘汰的格仍可能占掉 K 个名额中的一个。口径归段 C 一并定。
4. **终端 `play` 与 Godot 入口没有阈值选项**：两者经预设取缺省值 20，只有批量 `run` 有 `--pass-threshold`。tasks 2.4 只要求严格 CLI（`run`）；是否给 `play` / Godot 补选项，请主会话定。
5. **性能**：简单难度原本不做活形分析，现在每次决策做 1 次批次前分析；己方有活形棋串时，每个候选再多 1 次预演后分析（标准难度的预演后分析与评价各做一次，未共用）。未测耗时，归段 C（缓存 / 耗时对比）。
6. 工作树里出现了 `.claude/worktrees/`（非本任务，未触碰）；开工时 `git status` 里的 `openspec/changes/terrain-surfaces/`，在段末已不在未跟踪列表中（非本任务，未读未碰）。
7. **R1"有提子即放行"在真实管线里走不到**：单格眼的气边邻居全是己方，单枚落进去不可能提子，所以 `RankPoints` 阶段它就被淘汰、不进排行；`Greedy` 只从排行里取点，永远不会把它和提子手组成同一批。`为提子而填眼放行` 是直接调 `TryEvaluate` 验证的。这是规格原文（两处都淘汰）的直接后果，不是 bug，但意味着 design Open Question 1 担心的"放行过宽"实际上零触发。规格是否要改成"单点阶段只判、组批阶段放行"，请负责人判断。
8. **`SimFixtures.RankedMatch` 写死了权重、没写死阈值**：段 D 改缺省阈值时 `RankedSample` 的走法会变（同 `地形可离线重建` 把阈值写死为 20 的理由），留给段 D 顺手处理。
