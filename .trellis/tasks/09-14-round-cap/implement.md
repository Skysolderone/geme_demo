# 09-14-round-cap — 执行计划

> **执行进度以本文件为准。** `openspec/changes/round-cap/tasks.md` 是同一份清单的规格侧快照，勾选状态不在那里维护。
> 实现前先读 `prd.md` → `openspec/changes/round-cap/design.md` → 两份 specs。

## 1. 对局配置与持久化

- [x] 1.1 对局配置新增"大回合上限"（非负整数，初值 15，0 = 不限），开局固定、进行中不可改。验证：开局后修改抛规则异常；默认读到 15。
- [x] 1.2 公开视图新增该字段，插旗阶段即可读；反射守门仍通过。
- [x] 1.3 存档写入 / 恢复；旧存档无字段回填 15。验证：第 7 大回合存档恢复逐字段 + 逐字节两条腿；旧格式存档恢复得 15。

## 2. 终局判定

- [x] 2.1 终局原因新增「达大回合上限」，序列化名称稳定。
- [x] 2.2 大回合结束、生成下一顺序之前检查：上限 > 0 且刚结束的序号 = 上限 且仍进行 → 终局。验证：调试接缝摆到第 15 大回合最后一位落子 → 终局；第 2 位落子 → 继续。
- [x] 2.3 优先级：第 15 大回合整轮 Pass → 原因为「整轮 Pass」。
- [x] 2.4 上限 0 不触发：跑到第 20 大回合仍进行。
- [x] 2.5 名次复用条件 2/3 唯一实现：达上限时势力 42 平、信物 3 vs 1 → 信物 3 者高。
- [x] 2.6 时序陷阱：比较用刚结束的轮次。变异：改成推进后的序号应变红。

## 3. 跑局层与报告

- [x] 3.1 `--max-rounds` 写入对局配置；删除跑局层"达上限"原因；上限 0 时保留防死锁硬停（1000 小回合，异常记录）。验证：`--max-rounds 12` 以规则原因结束于第 12 大回合；`--max-rounds 0` + 永不 Pass 的控制者 → 抛异常并落 `failed`。
- [x] 3.2 报告"收敛情况"按规则原因统计不收敛率；日志首部记上限。验证：4 局中 3 局达上限 → 75%。
- [x] 3.3 既有 heuristic-ai 测试中依赖跑局层"达上限"的用例改为规则级原因，改动列在提交信息。

## 4. 设计文档

- [x] 4.1 §12.3 增补条件 4 与配置说明；§18.2 加"固定轮数仍不实现；大回合上限是兜底"。
- [x] 4.2 变更记录与 cap-multiplier 共用一条 v1.1 记录。

## 5. 变异验证与回归

- [x] 5.1 1.1–1.3、2.2–2.6、3.1–3.2 的新测试做变异验证并记录（去掉上限检查 / 优先级反转 / 序号用推进后的值 / 存档不写字段 / 旧存档回填 0）。
- [ ] 5.2 与 cap-multiplier 都归档后跑 200 局 Standard 回归（上限 15）：终局原因分布、平均大回合数、不收敛率、峰值军势，写入两个 change 的裁决记录。

## 验证命令

```bash
dotnet build
dotnet test
openspec validate round-cap
```
