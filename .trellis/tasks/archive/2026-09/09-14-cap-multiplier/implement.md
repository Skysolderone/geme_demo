# 09-14-cap-multiplier — 执行计划

> **执行进度以本文件为准。** `openspec/changes/cap-multiplier/tasks.md` 是同一份清单的规格侧快照，勾选状态不在那里维护。
> 实现前先读 `prd.md` → `openspec/changes/cap-multiplier/design.md` → `specs/power-score/spec.md`。

## 1. 计分实现

- [x] 1.1 在全项目唯一的倍率实现中把指数改为 `min(倍增子数量, 5)`，封顶常量只定义一处；调用方继续传原始数量。验证：普通子×3 + 堡垒子×1 + 倍增子×2 → 20 不变；普通子×4 + 倍增子×5 → 68；普通子×4 + 倍增子×6 → 75。
- [x] 1.2 倍率显示字符串按生效指数生成，最大 `7.59375`。验证：8 枚倍增子显示 `7.59375`。
- [x] 1.3 势力明细每条棋串新增"生效倍率指数"，保留"倍增子数量"。验证：9 枚倍增子 → 数量 9、生效 5、倍率 7.59375。
- [x] 1.4 封顶不影响其他效果。验证：倍增子×7 + 协同子×1 → 协同加值 2，气数与 8 子串一致，只有倍率指数取 5。
- [x] 1.5 溢出守门改写：封顶后不可能溢出，checked 保留；改注释与相关测试（若无溢出测试则补一条）。

## 2. 既有守门与遥测

- [x] 2.1 全量测试定位并改写依赖"倍率无上限"的断言为"n ≥ 5 时倍率恒为 243/32"，改动列在提交信息。
  - 备注（改写的既有断言）：`PieceEffects/倍增子的棋串倍率Tests.倍率不设上限` → 改名 `倍率指数封顶为5`（12 枚 → 243/32、军势 1556 → 91）；同类 `超出整数范围时响亮失败`（n=60/81 不再溢出，改为 `long.MaxValue × 243/32` 溢出守门，checked 保留）；`PowerScore/棋串军势公式Tests` 两个取整边界 Theory 的 n=6/7/8 行期望值改为封顶版（729/2187/6561 → 486/972/1944；740/2204/6586 → 493/979/1951）。其余既有方法未动。
- [x] 2.2 遥测倍率峰值保留原始数量、可得生效指数。验证：8 枚 → 数量 8、生效 5。
- [x] 2.3 `Siege.Sim` 快照棋串明细加"生效倍率指数"，报告"高倍率棋串"段落双列；旧日志缺字段按 `min(数量, 5)` 回填。验证：用旧格式日志行解析成功并回填正确。

## 3. 设计文档

- [x] 3.1 §10.1 公式改封顶版并保留示例；§9.2 倍增子效果加"至多 5 枚生效"。
- [x] 3.2 标题版本 v1.1，文末变更记录（日期、change、36 局依据、推翻 territory-power 裁决 3）。

## 4. 变异验证与回归

- [x] 4.1 1.1–1.4、2.2 每个新测试做变异验证并记录（封顶常量改 6 / 去掉 min / 显示用原始指数 / 明细两字段互换）。
  - 记录：M-M1 MaxExponent=6 → 红 15；M-M2 去掉 min → 红 13；M-M3 ToString 用原始指数 → 红 3；M-M4 明细/峰值生效指数退化为原始数量 → 红 6；M-M5 Sim 回填不封顶 → 红 2；M-M6 Sim init 丢弃写入值 → 红 1；M-M7 分析器生效分布按原始数量计 → 红 2；C1 unchecked long 乘法 → 红 1。全部还原后 506/506。
  - check 阶段复做（trellis-check，2026-09-14）：Sim 回填硬编码 4 → 红 2；GroupPower.EffectiveMultiplierCount => MultiplierCount（不动 Peak）→ 红 5；报告双列改单列 → 红 1；Apply 两处 checked 改 unchecked → 红 1；Numerator 用原始 Count → 红 13。全部还原后 506/506。
- [ ] 4.2 200 局 Standard 回归（round-cap 归档后一起跑）：峰值军势分布、倍增子选择率、不收敛率、平均大回合数，写入 design.md 裁决记录。

## 验证命令

```bash
dotnet build
dotnet test
openspec validate cap-multiplier
```
