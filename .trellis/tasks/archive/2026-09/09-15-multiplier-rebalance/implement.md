# 09-15-multiplier-rebalance — 执行计划

> **执行进度以本文件为准。** `openspec/changes/multiplier-rebalance/tasks.md` 是规格侧快照。
> 实现前先读 `prd.md` → `openspec/changes/multiplier-rebalance/design.md`（D1–D4 + 裁决 1–5）→ 两份 specs。

## 1. 公式与封顶

- [x] 1.1 势力组装改为 `⌊基础军势总和 × 倍率⌋ + 位置加值`，仍经唯一的 `Multiplier.Apply`，只是传入值改为基础军势。算例（设计文档 §10.1）：普通子×3 + 堡垒子×1 + 倍增子×2 → 20 不变；基础 6、加值 5、1 枚倍增 → 14（不是 16）。验证：单元测试。
- [x] 1.2 位置加值不被放大。算例：4 枚连珠子成横线 + 2 枚倍增子 → `⌊6×2.25⌋ + 12 = 25`（不是 40）。验证：单元测试。
- [x] 1.3 `Multiplier.MaxExponent` 4 → 3。算例：普通×4 + 倍增×3 → 23；普通×4 + 倍增×4 → 27（不是 40）；12 枚 → 倍率 3.375；倍增×7 + 协同×1 → 29。验证：单元测试。
- [x] 1.4 倍率显示上限 `3.375`，明细中 9 枚倍增子 → 数量 9、生效 3、倍率 3.375。验证：单元测试。
- [x] 1.5 明细可复算棋串军势：任一棋串 `⌊基础 × 倍率⌋ + 加值 = 军势`。验证：单元测试（覆盖含加值与不含加值两类棋串）。

## 2. 既有算例与遥测

- [x] 2.1 按新公式改写受影响的既有算例（含倍增子且含位置加值者；封顶边界算例；取整边界 Theory 的 n ≥ 4 行），改动逐条列入提交信息。验证：全量测试通过。
- [x] 2.2 Sim 快照棋串明细补记各棋子类型计数；平衡报告新增"各棋子势力占比"段落（倍增子计其放大出的部分，其余计基础军势与所分得的位置加值）。验证：单元测试——构造已知棋串，占比与手算一致。

## 3. 设计文档

- [x] 3.1 §9.2 倍增子效果改为"至多 3 枚生效，只放大基础军势"；§10.1 公式改写，保留标准算例并注明结果不变。验证：人工检查。
- [x] 3.2 文末变更记录加一行（依据：200 局终局盘面势力归因，倍增子占 76.3%；离线重算比较的几种公式）。验证：人工检查。

## 4. 变异验证与回归

- [x] 4.1 变异验证并记录：加值重新放入倍率、封顶改回 4、取整放到加值之后、显示用原始指数、明细复算用旧公式。
- [x] 4.2 200 局回归（种子 1–200，Standard）：各棋子势力占比与选择率、峰值军势、终局原因分布、平均结束大回合、第 3 大回合领先者胜率，与 growth-pass-1 之后对照。验证：跑局统计如实记录；达上限 > 15% 立即报告。

## 变异验证记录（4.1，实现阶段；全量 628 条，"红 N"为失败条数，含 Theory 行）

| 编号 | 变异 | 结果 |
|---|---|---|
| M-MR1 | `GroupPowerOf` 改回 `Apply(baseTotal + positionBonus)`（加值重新放进倍率） | 红 5：含位置加值的计算、位置加值不被倍率放大、封顶不影响其他效果、倍率不作用于位置加值、明细可复算棋串军势 |
| M-MR2 | `MaxExponent` 3 → 4 | 红 23（14 个方法，含两个取整边界 Theory 的 n=4..8、Sim 回填 / 报告 / 占比） |
| M-MR3 | 取整放到加值之后 `⌊(基础·3^e + 加值·2^e) / 2^e⌋` | 红 0：加值为整数，与 `⌊基础·倍率⌋ + 加值` 恒等（design.md D2），等价变异，不造测试 |
| M-MR4 | `Multiplier.ToString` 用原始 `Count` | 红 7（6 个方法，含明细可复算棋串军势——复算取显示串） |
| M-MR5 | `Apply` 改向上取整；再把复算守门改用 `g.Multiplier.Apply(...)` | 独立整数式：复算循环断言处红；改用 `Apply` 后复算循环全过、只剩手算字面值断言红——用被测方法复算是恒真守门 |
| M-MR6a/b | 写入端 Basic↔Fortress / Line↔Synergy 写反 | 各红 1：真实跑局快照的类型计数与明细自洽 |
| M-MR7 | 占比分母加上被跳过局的棋串军势 | 红 1：各棋子势力占比按口径手算 |
| M-MR8 | `PieceCounts` get 恒 null / init 丢值 | 红 3 |
| M-MR9 | 终局快照改取 `Turns[0]` | 红 1：各棋子势力占比按口径手算 |
| M-MR10 | 预演 `FormulaText` 恢复"（基础 + 加值）× 倍率" | 红 2 |

全部变异均 `cp` 备份、还原后 `cmp` 一致。

## check 阶段（全量 629 条 = 628 + check 补 1）

- 替换假测试：`势力明细Tests.明细可复算总势力` 原用 `GroupPowerOf` 复算（委托目标恒真），改为测试内独立整数式 `⌊基础·3^e/2^e⌋ + 加值`（e = min(n, 3)），并补一条含倍增子且含加值的字面值抽查（P1 串 13）。
- 补测试：`各棋子势力占比Tests.含连珠线协同倍增的真实棋串写快照读回并按口径归因`（连珠线 3 + 协同 + 倍增×2 → 23，归因 连珠 9 / 协同 5 / 倍增 9）；为此 `MatchSession.PieceCountsOf` 由 private 改 internal。

| 编号 | 变异 | 结果 |
|---|---|---|
| M-MR1 复做 | 同上 | 红 7：原 5 + 明细可复算总势力（替换后）+ 含连珠线协同倍增的真实棋串… |
| M-MR2 复做 | 同上 | 红 23，与实现方一致 |
| M-MR6a 复做 | 同上 | 红 1，与实现方一致 |
| C-MR2 | 占比口径倍增子放大部分不减基础（`Apply(Base)`） | 红 2：各棋子势力占比按口径手算、含连珠线协同倍增的真实棋串… |
| C-MR3 | 连珠加值分给倍增子（Line 分支 0、Multiplier 分支 + LineBonus） | 红 2：同上两条 |
| C-MR4 | 旧日志缺计数不跳过、按 0 回填 | 红 1：各棋子势力占比按口径手算 |

## 验证命令

```bash
dotnet build
dotnet test
openspec validate multiplier-rebalance
```
