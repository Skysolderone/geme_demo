# 09-14-cap-multiplier — 执行计划

> **执行进度以本文件为准。** `openspec/changes/cap-multiplier/tasks.md` 是同一份清单的规格侧快照，勾选状态不在那里维护。
> 实现前先读 `prd.md` → `openspec/changes/cap-multiplier/design.md` → `specs/power-score/spec.md`。

## 1. 计分实现

- [ ] 1.1 在全项目唯一的倍率实现中把指数改为 `min(倍增子数量, 5)`，封顶常量只定义一处；调用方继续传原始数量。验证：普通子×3 + 堡垒子×1 + 倍增子×2 → 20 不变；普通子×4 + 倍增子×5 → 68；普通子×4 + 倍增子×6 → 75。
- [ ] 1.2 倍率显示字符串按生效指数生成，最大 `7.59375`。验证：8 枚倍增子显示 `7.59375`。
- [ ] 1.3 势力明细每条棋串新增"生效倍率指数"，保留"倍增子数量"。验证：9 枚倍增子 → 数量 9、生效 5、倍率 7.59375。
- [ ] 1.4 封顶不影响其他效果。验证：倍增子×7 + 协同子×1 → 协同加值 2，气数与 8 子串一致，只有倍率指数取 5。
- [ ] 1.5 溢出守门改写：封顶后不可能溢出，checked 保留；改注释与相关测试（若无溢出测试则补一条）。

## 2. 既有守门与遥测

- [ ] 2.1 全量测试定位并改写依赖"倍率无上限"的断言为"n ≥ 5 时倍率恒为 243/32"，改动列在提交信息。
- [ ] 2.2 遥测倍率峰值保留原始数量、可得生效指数。验证：8 枚 → 数量 8、生效 5。
- [ ] 2.3 `Siege.Sim` 快照棋串明细加"生效倍率指数"，报告"高倍率棋串"段落双列；旧日志缺字段按 `min(数量, 5)` 回填。验证：用旧格式日志行解析成功并回填正确。

## 3. 设计文档

- [ ] 3.1 §10.1 公式改封顶版并保留示例；§9.2 倍增子效果加"至多 5 枚生效"。
- [ ] 3.2 标题版本 v1.1，文末变更记录（日期、change、36 局依据、推翻 territory-power 裁决 3）。

## 4. 变异验证与回归

- [ ] 4.1 1.1–1.4、2.2 每个新测试做变异验证并记录（封顶常量改 6 / 去掉 min / 显示用原始指数 / 明细两字段互换）。
- [ ] 4.2 200 局 Standard 回归（round-cap 归档后一起跑）：峰值军势分布、倍增子选择率、不收敛率、平均大回合数，写入 design.md 裁决记录。

## 验证命令

```bash
dotnet build
dotnet test
openspec validate cap-multiplier
```
