# 《围杀 Siege》核心玩法方案

> 版本：v1.4（核心原型基准；v1.0 冻结后经 cap-multiplier 等修订，最近一次为 artisan-terrain-edit，见文末变更记录）  
> 日期：2026-09-10  
> 状态：玩法设计已确认，进入原型验证  
> 适用范围：4 人核心原型；正式产品支持 2–4 人对战

## 1. 游戏定位

《围杀 Siege》是一款 2–4 人共享棋盘的回合制策略构筑游戏，融合围棋式围杀、地块争夺与《小丑牌》式“加值 × 倍率”成长。

- 核心体验占比：棋盘空间博弈约 60%，随机构筑约 40%。
- 核心张力：扩张可以获得信物和先手，但战线扩大后更容易暴露薄弱棋串。
- 核心循环：探索信物 → 定向征募 → 批次落子 → 围杀与抢夺据点 → 提升势力和先手 → 争夺更强信物 → 形成高风险数值爆发。
- 对本局体验：玩家应能解释胜负来自落点、阵型、资源取舍和构筑方向，而不是单纯归因于刷新运气。
- 标准局目标：平均 20–30 分钟，不设置固定轮数，允许形成更长的拉锯战。
- 产品形态：最终支持本地 AI、人机混合与联网对战；首轮原型以单机调试控制和 AI 验证为主。

## 2. 核心术语

- **大回合**：所有仍在比赛中的玩家各完成一次小回合。
- **小回合**：一名玩家依次完成信物快照、手牌整理、征募和批次部署。
- **批次**：玩家在一个小回合中暂放并一次性确认的全部棋子。
- **棋串**：同一玩家经气边直接或间接相连的全部棋子；不同棋子类型可以组成同一棋串并共享气。
- **气**：与棋串任一棋子之间存在气边、且当前为空的可落子格。
- **覆盖**：棋子按覆盖关系对地块施加的控制影响，决定空格归属、信物与据点的控制（§7）。
- **格属性 / 边属性**：格属性是每格的静态地形——高度（0 / 1 / 2）、地表（草地 / 土路 / 林地 / 深水）、障碍、预置桥；边属性是两个几何相邻格之间的栅栏。开局值全部由地图数据给定（§3.1）。**地形不是对局内的不变量**：地表与设施（桥、栅栏）可以被匠人的地形改造单向改写（§3.4），高度、障碍、信物格与据点位置则对局中恒定。
- **匠人**：第六种棋子，基础军势 1，规则上与普通子完全相同（有气、被围杀、无免死、无持续效果），唯一的特殊之处是**落子的那一瞬间**可以携带一个地形改造（§3.4）。
- **地形改造**：匠人落子时可选携带的一次性地形改写，只有三种动作——**搭桥**（未架桥深水格 → 带桥）、**立栅**（一条边 → 带栅栏）、**烧林**（林地格 → 草地）。改造不可逆、不属于任何玩家、不新增回合阶段、不额外占部署额度（§3.4）。
- **气边**：在几何四邻之上导出的对称关系——两个几何相邻格都可落子、高度差 ≤ 1 且之间无栅栏。连接、棋串、气与围杀全部沿气边（§3.1）。
- **覆盖关系**：在几何四邻之上导出的、可不对称的关系——棋子向哪些格提供覆盖：林地不接收、跨崖壁只能居高临下、可隔一格宽的深水、栅栏不挡。覆盖、空格归属、唯一覆盖与信物发现全部按覆盖关系（§3.1）。
- **信物**：预先生成在指定地块、被发现后永久公开、仅在控制期间生效的构筑资源。
- **据点**：地图上固定的计分格，分营帐 / 篝火 / 石碑三档，与信物格完全分离；被某玩家占据或唯一覆盖时向其计入该档分值（§7.4）。据点格是普通可落子格。
- **据点分**：玩家当前控制的全部据点的分值之和；三档分值是对局配置，标准局 5 / 15 / 45。
- **高地加值**：棋子的覆盖目标上存在高度严格更低的敌方棋子时，该棋子提供 1 点位置加值（每枚至多 1 点，不被倍率放大，§10.1）。
- **势力值**：当前盘面上的据点分与全部棋串军势之和，不可消耗，也不累计历史积分；空格归属不计分。
- **参赛玩家**：尚未出局且未弃赛的玩家。

## 3. 棋盘与地图

### 3.1 基础格子规则

- 地图由格子、格间栅栏、信物格、据点格和出生区组成。每个格子带四项**格属性**：高度 0（低地）/ 1（平台）/ 2（高台）；地表 草地 / 土路 / 林地 / 深水；障碍（岩石）；预置桥（只能架在深水格上）。**边属性**只有一项：栅栏，立在两个几何相邻格之间，属于边不属于格，两侧仍可落子，不属于任何玩家。地形、栅栏、出生区、信物格与据点位置的**开局值**由设计师固定；只随机生成信物的具体内容。出生区、信物格、据点位置、高度与障碍对局中恒定；**地表与设施（桥、栅栏）可被匠人的地形改造单向改写**（§3.4），改写后本节两套关系按新地形实时重算，预置设施与对局中造出来的设施在规则上与外观上完全等价。
- **可落子格** = 非障碍且非未架桥深水的格。障碍与未架桥的深水都不可落子、不可控制、不计分，视为封堵气与覆盖的边界（棋盘外沿同此）；架桥的深水格按普通格处理，高度取高度表；林地是普通可落子格；草地与土路规则上等价，只作视觉区分。信物格本身仍是普通可落子格，不阻挡棋子。
- 几何邻接只有上下左右四邻，不含斜向。规则不直接用几何邻接，而是在它之上导出两套关系，各自只有一处实现、每次按当前格属性实时导出：
  - **气边**（对称）：两个几何相邻格之间存在气边，当且仅当 ① 两格都可落子；② 高度差的绝对值 ≤ 1；③ 之间无栅栏。高度差 ≥ 2 即崖壁。连接、棋串、气与围杀判定全部沿气边。
  - **覆盖关系**（可不对称）：格 s 上的棋子向格 t 提供覆盖，按顺序判：① s 与 t 几何相邻，t 可落子、不是林地，且 h_t − h_s ≤ 1 → 覆盖 t；② s 与 t 几何相邻但 t 是未架桥的深水 → 看沿同一方向再走一格的 u：u 在盘内、可落子、不是林地且 h_u − h_s ≤ 1 → 覆盖 u，不覆盖 t（只穿一格宽的水，宽河不行；对岸的桥格按普通格可被隔岸覆盖）；③ 其他情况不覆盖。栅栏不影响覆盖。覆盖、空格归属、唯一覆盖与信物发现全部按覆盖关系。
- 一句话记法：**栅栏挡气不挡覆盖，林地挡覆盖不挡气，崖壁挡气、覆盖只能居高临下，深水挡气、覆盖可隔一格水。** 因此"被某方覆盖的空格"与"某条棋串的气"可以不同，任何规则不得以一个集合代替另一个（§14.2）。

标准算例（与 `terrain` / `board-topology` 规格及测试一致；坐标只表示相对位置，未提到的格默认 h=0 空草地）：

| 算例 | 布置 | 结果 |
|---|---|---|
| 崖壁切断气 | 孤子在 h=0 的 `F6`，上方 `F7` 是 h=2 的空格，其余三面为 h=0 或 h=1 的空格 | 气数 3，`F7` 不计入 |
| 缓坡不切断气 | 同一玩家两子在 h=1 的 `F6` 与 h=2 的 `F7` | 同一棋串 |
| 栅栏切断气与连接 | 同一玩家两子在 `F6`、`G6`，两格之间有栅栏 | 分属两个棋串，`G6` 腾空后也不是 `F6` 那串的气 |
| 深水切断气 / 桥恢复气 | 孤子在 `F6`，右侧 `G6` 为未架桥深水，其余三面为空 | 气数 3；`G6` 架桥且为空则气数 4 |
| 靠崖壁的棋串 | 一条 h=0 的棋串三面被敌子围住，第四面是 h=2 的空格 | 无气 |
| 居高临下 | A 的棋子在 h=2 的 `F7`，下方 `F6` 为 h=0 的空格 | `F6` 获得 A 的覆盖，但不是 `F7` 那串的气 |
| 仰视不覆盖 | A 的棋子在 h=0 的 `F6`，上方 `F7` 为 h=2 的空格 | `F7` 不获得 A 的覆盖 |
| 林地不接收覆盖 | A 的棋子在 `F6`，右侧 `G6` 是空的林地 | `G6` 不获得任何覆盖；若 `G6` 是信物格，信物不因此被发现，只能靠占据揭示与控制 |
| 林地上的棋子仍向外覆盖 | A 的棋子在林地 `G6`，右侧 `H6` 为空草地 | `H6` 获得 A 的覆盖 |
| 栅栏不挡覆盖 | A 的棋子在 `F6`，`F6`–`G6` 之间有栅栏，`G6` 为空草地 | `G6` 获得 A 的覆盖，但不是 `F6` 那串的气 |
| 隔岸覆盖 | A 的棋子在 `F6`，`G6` 为未架桥深水，`H6` 为与 `F6` 同高的空草地 | `H6` 获得 A 的覆盖，`G6` 不记录覆盖 |
| 宽河不可隔岸 | A 的棋子在 `F6`，`G6` 与 `H6` 都是未架桥深水 | `G6`、`H6`、`J6` 均不获得 A 的覆盖 |

### 3.2 人数适配

不同人数使用独立手工地图，不通过简单裁切同一张大图适配。

| 人数 | 建议可落子格 | 出生区 | 信物格 | 据点 |
|---:|---:|---:|---:|---:|
| 2 | 50–65 | 2 | 7–9 | 6–8 |
| 3 | 75–90 | 3 | 10–12 | 9–11 |
| 4 | 95–110 | 4 | 13–15 | 10–14 |

- 4 人区间在 `denser-map` 由 100–115 收紧为 80–95，`terrain-model` 再放宽为 95–110：可落子按"非障碍且非未架桥深水"计；三层高度与水系需要过渡带来表达地貌，而空格已在 `scoring-sites` 退出计分（势力改由据点分 + 军势构成），"铺满盘面"不再是对局长度的主控变量。2 人与 3 人（含据点数）为未制作地图的估值，须在各自定稿时按同一密度口径重估。据点数越界的地图加载时报错（如 4 人图 16 个据点 → 报告超出 10–14）。
- 2 人与 4 人地图优先采用旋转对称（4 人为 C4，绕中心 90° 旋转不变、出生区编号轮换）。不再要求镜像：立体地貌做双轴镜像会强行折叠山脊与河道的走向。
- 3 人地图独立设计，不强行套用方形对称。
- 各出生区到最近公共信物、中央入口、主要咽喉、最近篝火与最近石碑的最短距离**沿气边**计算（营帐在区内，不纳入），两两差值不超过容差 1（逐图放宽须在地图数据里写明理由）：崖壁、未架桥深水、栅栏挡住的路不算路。失衡时指出出生区编号、目标与距离值（如出生区 1 到最近石碑 6、出生区 2 为 9 → 被拒）。
- 不再要求障碍占比（`denser-map` 曾定 25%–35%）：深水与崖壁同样在压缩可落子空间，只数障碍已无意义，密度由可落子格区间把控。仍不得在出生区制造无法建立活形的必死口袋（沿气边连通的空区面积小于 8 即口袋，除非显式豁免）；每个出生区必须至少有一条沿气边到中央入口的通路；桥必须架在深水格上、栅栏必须在相邻格之间；每个据点必须位于可落子格上、标注档位、不与信物格重合（违规时指出坐标，如把石碑标在信物格 `G7`、把篝火标在未架桥深水上都被拒）。障碍、崖壁、深水与栅栏都在压缩可落子空间并充当围杀需要的墙，可落子格因此仍是首次冲突时点的主控变量（见 §16）。

### 3.3 4 人原型地图

当前基准图为 `siege-4p-base-v4`（change `scoring-sites`）：地形与 `siege-4p-base-v3`（change `terrain-model`）逐格相同，另加 12 个据点。同一地形换了计分口径就升号，不沿用 v3 的 Id（裁决 S-9）；`maps/siege-4p-base-v3.json` 保留为历史存档并注明已被取代，它缺据点、通不过校验规则 8，不再被默认加载。v1（11×11、109 可落子，`maps/siege-4p-base-v1.json`）与 v2（11×11、85 可落子、36 障碍，`maps/siege-4p-base-v2.json`，change `denser-map`）同样保留为历史存档，加载都会被拒绝：v2 的 85 格越出 95–110 区间；v1 的 109 格虽在区间内，但每区 15 格越出出生区 12–14 的区间；两者都不含地形字段（按全平地读入），只能用于人工比对，不能用来开局。

- 外接尺寸 13×13，目标 95–110 个可落子格；v4 实际 105（h=0 / 1 / 2 = 45 / 8 / 52），岩石 36、深水 32（其中预置桥 4、未架桥 28）、栅栏 4 段、林地 4、土路 16。
- 四个出生区各 13 格（预算每区 12–14 格），全部在 h=2 高台；每区经 2 格 h=1 缓坡下到 h=0 的落脚点，其余边缘是崖壁、岩石或深水——开局安全由地形提供而非规则禁止。每区可容纳单名玩家前三大回合最多 9 枚基础部署。
- 中央是 h=0 低地：一格宽的环形深水围出 21 格的岛，四座预置桥是进岛的唯一路径，也就是四个咽喉（桥格只有 2 个气边邻格）；岛四角是林地（只能靠占据拿），岛内四块岩石作围杀的墙。
- 信物格 13 个 = 出生区 8（每区 2）+ 公共 5（四座桥头各 1 个标准档 + 岛心 1 个高档）；岛心 `G7` 同时是中央入口。C4 对称下轨道大小只有 4 或 1，公共信物因此是 5 而不是"约 6"（裁决 B-1 / B-2）。
- 据点 12 个 = 营帐 4 + 篝火 4 + 石碑 4，每档恰是一条 C4 轨道，无一与信物格重合（规则见 §7.4）。C4 下据点数只能是 4k 或 4k+1，唯一的中心格 `G7` 已是信物，所以是 12（落在 4 人 10–14 区间）。布点定稿见裁决 S-13：
  - **营帐** `B3 / L2 / M11 / C12`：各出生区腹地的 h=2 非信物格，四个邻格都是本区高台，只有本区高台上的棋子能覆盖它——保护期内就有可守的点，事实上只有主人能控制。
  - **篝火** `J2 / M9 / E12 / B5`：每个出生区的**河外低地**（从该出生区出发、不经过任何桥格、沿气边可达的 h=0 格）里 1 个，且与**另一个**出生区的 h=2 格几何相邻（如出生区 1 的 `J2` 紧贴出生区 2 的崖边 `K2`），邻家可居高覆盖、主人从低地仰视覆盖不到那格高台。这套地形上满足这一约束的只有这一条轨道。邻家第 1 大回合在崖边落子即可唯一覆盖篝火，主人保护期内下不了山，所以前三大回合篝火通常归邻家，主人第 4 大回合起下山占据或覆盖才能夺回或打成争议（裁决 S-8）。
  - **石碑** `H5 / J8 / F9 / E6`：**岛**（只能经桥格沿气边到达的中央 h=0 区域）上的非林地格。每块石碑与一座桥头信物隔栅栏相邻（如 `H5` 与 `G5`）：站在那座桥头的一家隔栏即可覆盖但走不进去，另一家从相邻桥头经林地角绕入才能占据。否决岛心内圈 `G6 F7 H7 G8`（`G7` 一子覆盖四块）与 `F5` 轨道（主人隔一格深水从自家低地覆盖）。
- 中央、交通咽喉和高风险边缘区承担更高的信物强度预算。
- 玩家数量与出生区数量相同，但多人可以选择同一出生区。
- 地图对绕中心 90° 旋转不变（C4 对称，高度、地表、障碍、桥、栅栏、信物格与据点的位置和档位逐格一致，出生区编号 1 左下 → 2 右下 → 3 右上 → 4 左上 轮换）；四个出生区到最近公共信物、中央入口、最近咽喉、最近篝火与最近石碑的沿气边最短距离精确相等（5 / 7 / 4 / 6 / 8），极差为 0，容差 1 未放宽；全盘可落子格沿气边连通成一块，无口袋、无豁免。
- 出生区编号对人显示一律为 1–4（文本图、棋盘、插旗提示、校验器与对称检查报文、平衡分析报告），地图数据与日志字段的内部索引从 0 起（裁决 S-14）。

文本图（`dotnet run --project src/Siege.Sim -c Release -- map` 的输出原样，只省略末尾的"已导出 maps\siege-4p-base-v4.json"一行；每格两位 = 高度 + 标记）：

```
地图 siege-4p-base-v4  13×13
可落子格 105   岩石 36   深水 32（其中桥 4）   栅栏 4   林地 4   土路 16
可落子格按高度 h0/h1/h2 = 45/8/52
出生区 4 个，各 13/13/13/13 个可落子格，高度 2/2/2/2
信物格 13（出生区 8，公共区 5）
据点 12（营帐 4，篝火 4，石碑 4）
  营帐（T） L2 B3 M11 C12
  篝火（C） J2 B5 M9 E12
  石碑（S） H5 E6 J8 F9
各出生区沿气边最短距离（出生区 1/2/3/4）：
  最近公共信物  5/5/5/5
  中央入口  7/7/7/7
  最近咽喉  4/4/4/4
  最近篝火  6/6/6/6
  最近石碑  8/8/8/8
咽喉 G4 D7 K7 G10   中央入口 G7   桥 G4 D7 K7 G10
栅栏 G5-H5 E6-E7 J7-J8 F9-G9

地图校验通过。

 13 ## 24 24 24 ## ## ## ## ## 23 23 23 ## 
 12 24 2r 2T 24 0C 0  0  ## 1. 23 23 2r 23 
 11 24 24 2r ~~ ~~ ## 0. 0. 1. 23 2r 2T 23 
 10 24 24 24 ~~ ~~ ~~ 0= ~~ ~~ ~~ ~~ 23 23 
  9 ## 1. 1. ~~ 0F 0S|0o 0  0F ~~ ~~ 0C ## 
  8 ## ## 0. ~~ 0  ## 0  ## 0S ~~ ## 0  ## 
                            --             
  7 ## 0  0. 0= 0o 0  0R 0  0o 0= 0. 0  ## 
                --                         
  6 ## 0  ## ~~ 0S ## 0  ## 0  ~~ 0. ## ## 
  5 ## 0C ~~ ~~ 0F 0  0o|0S 0F ~~ 1. 1. ## 
  4 21 21 ~~ ~~ ~~ ~~ 0= ~~ ~~ ~~ 22 22 22 
  3 21 2T 2r 21 1. 0. 0. ## ~~ ~~ 2r 22 22 
  2 21 2r 21 21 1. ## 0  0  0C 22 2T 2r 22 
  1 ## 21 21 21 ## ## ## ## ## 22 22 22 ## 
    A  B  C  D  E  F  G  H  J  K  L  M  N  

每格两位：首位是高度 0/1/2，次位是标记。## 岩石  ~~ 深水  = 桥  1-4 出生区  r 出生区信物  o 公共信物  R 公共高档信物  T 营帐  C 篝火  S 石碑
@ 中央入口  ^ 咽喉（与信物、据点或桥同格时显示信物 / 据点 / 桥的标记；入口 G7 是高档信物 R、四座桥即咽喉 =；据点不与信物重合）  F 林地  . 土路   格间 | 与行间 -- 为栅栏
```

障碍、崖壁、深水与栅栏都是围杀需要的墙：空旷棋盘上堵不掉整条棋串的气，可落子空间越小、贴墙的棋串越多，围杀成本越低。可落子格因此是首次冲突时点的主控变量，见 §16。同一地形在旧计分口径下平均第 10.57 大回合结束、14% 的对局整局无提子（高台自留地不下山也能撑到终局）；换成据点计分后的实测见 §16。

### 3.4 地形改造

地形不是对局内的不变量：**匠人**（§9.2）落子的那一瞬间可以携带一个地形改造，把地图的地表与设施单向改写。改造是本作"每步只决定往哪放"之外的第二个决策维度——落点决定能改哪里，所以"要烧哪片林，先站到它旁边"。

**动作集**（只有这三种，不多不少）：

| 动作 | 目标 | 效果 |
|---|---|---|
| 搭桥 | 一个**未架桥的深水格** | 该格此后带桥，按可落子格处理，高度取地图给定值 |
| 立栅 | 一条**边** | 该边此后带栅栏，两格之间不再有气边；覆盖关系不受影响 |
| 烧林 | 一个**林地格** | 该格地表变为草地，此后可以成为覆盖目标；高度、障碍与其上的信物 / 据点不变 |

没有逆向动作：拆桥、拆栅、把草地变回林地、把陆地变水都不存在；改造也不改高度、不改信物、不改据点。已架桥的深水格、已有栅栏的边、已是草地的格因此都不是合法目标——对它们提交改造，整批以"目标已被改造过"被拒。

**目标口径**（裁决 T-2 / T-11）：

- **格目标**（搭桥、烧林）必须是匠人落点的**几何四邻**（上下左右，不含斜向，也不含匠人脚下那一格）。
- **边目标**（立栅）必须**至少有一端是匠人落点的几何四邻格**：既包括匠人格与其四邻之间的 4 条内圈边，也包括四邻与更外一格之间的 12 条外圈边，一共 16 条候选（屏幕上就是"以匠人格为中心的十字五格区域的外轮廓 + 匠人格自己的方框"）。
- 口径用几何四邻而不是气边：深水没有气边，用气边会让搭桥根本不可能，还得为它写例外。
- 边目标一度只许 4 条内圈边，实测证明那样**任何改造都不可能直接提子**（内圈边必有一端是匠人自己刚落的棋子，既不是敌串的气，也切不断敌串内部的连接），下面"改造先于提子"的全部理由随之落空，故放宽到 16 条（裁决 T-11）。

**落子即改造**：

- 只有匠人能携带改造；其余五种棋子带改造目标的批次一律非法。
- 改造占用匠人自己的那一枚部署额度，**不另计额度、不新增回合阶段**。
- 改造**可选**：匠人可以不带目标，落点周围没有任何合法目标时该落点仍然合法（手牌不会因为抽到匠人而卡死）。
- 保护期内允许改造，不加限制：匠人只能落在自家出生区，目标天然限于区内与区边（裁决 T-5）。

**时机——改造先于提子**（裁决 T-3）：在预演与正式结算中，改造都在"放入本批全部棋子之后、移除无气敌串之前"应用（§6.1 第 4 步、§6.3 第 3 步）。因此：

- 立栅可以敲掉敌串的最后一条气边并**直接导致该串被提走**，也可以把一条敌串从中切断，切开后两半各自算气；
- 对称地，改造若使己方棋串在提子后仍然无气，整批判定为**自杀手**并被拒绝；
- 同一批次内的多个改造**同时生效**，结果不依赖应用顺序（两道栅栏合围一条敌串时，两道都记为"直接导致提子"）。

**批次内不链式**（默认 2）：一个批次内全部改造与全部落子的合法性都按**该批次开始前**的地形判定。本批新架的桥不能在同一批次内当落点、也不能当下一次改造的跳板；本批新烧出的草地不能在同一批次内再被改造；同一个目标在同一批次内只能被指定一次（边的两端写反也算同一个目标）。下一批次起，新桥与普通桥完全一样：可以落子，也可以以它为起点继续向外架桥，因此宽河需要多个回合逐格架，架桥本身是一项投资。

**不可逆、无归属、全公开**（裁决 D17 / R-3）：

- 已完成的改造永久生效，任何规则都不撤销它：立栅的匠人被围杀、搭桥的玩家出局或弃赛，设施都留在原处。
- 桥与栅栏不属于任何玩家，不计入任何人的势力、据点或信物控制。规则里不需要"谁的桥"。
- 改造结果对全部玩家公开，是盘面的一部分；**谁改的不被记录也不被展示**（对局日志为平衡分析记录改造方，公开视图与盘面不得出现，§13.1）。
- 盘面同形禁则的盘面表示因此纳入设施与地表（§6.2）：棋子分布相同但多一道栅栏，两个盘面不同形。改造不可逆，状态空间单向扩张，不会因此振荡。

标准算例（与 `terrain-edit` / `terrain` 规格及测试一致；坐标只表示相对位置）：

| 算例 | 布置 | 结果 |
|---|---|---|
| 搭桥使深水可落子 | 匠人落 `F6`，对未架桥深水 `G6` 搭桥 | `G6` 此后可落子、参与气边与覆盖，高度取地图值；`F6`–`G6` 有气边 |
| 立栅堵住一条气边 | 匠人落 `F6`，在 `F6`–`G6` 之间立栅 | 该边此后有栅栏，两格之间不再有气边；`G6` 仍被 `F6` 覆盖 |
| 烧林后可被覆盖 | 匠人落 `F6`，烧掉相邻林地 `G6` | `G6` 变草地，此后可成为覆盖目标；其上的信物或据点仍在原处 |
| 边目标可落在外圈 | 匠人落 `F6`，指定 `F7`–`F8` 立栅（`F7` 是 `F6` 的四邻） | 合法，栅栏立在 `F7`–`F8` 之间 |
| 边目标不得离得更远 | 匠人落 `F6`，指定 `F8`–`F9` 立栅 | 非法：该边两端都不是匠人落点的几何四邻格 |
| 格目标必须相邻 | 搭桥 / 烧林目标与匠人落点不是几何四邻 | 非法：该目标不相邻 |
| 非匠人不得改造 | 给一枚堡垒子指定改造目标 | 非法：只有匠人能改造 |
| 无目标可改仍可落子 | 匠人四邻全是已架桥格 / 非林地，16 条候选边都已有栅栏 | 该落点仍然合法，匠人作为普通棋子落下 |
| 立栅导致提子 | 敌串只剩一条气边，该边一端是匠人落点的四邻格，匠人在该边立栅 | 结算时该敌串无气，被整体提走 |
| 改造把自己堵死 | 匠人立栅后己方棋串无气，且本批未提走任何敌串 | 整批判自杀手被拒，地形一个字节都不改 |
| 当批不能站上新桥 | 同一批次里先给某深水格搭桥，再往该格暂放棋子 | 非法：该格在本批次开始时不可落子 |
| 同一目标只能被改造一次 | 一个批次里两枚匠人指定同一个目标 | 非法：该目标重复 |
| 改造后不算重复盘面 | 结算后棋子分布与此前某个已提交盘面相同，但其间新增了一道栅栏 | 两个盘面不同，不触发同形禁则 |

## 4. 开局与前三大回合

### 4.1 匿名插旗

- 地图地形、出生区和信物格位置公开，信物具体内容隐藏。
- 联网模式中，所有玩家在 15 秒内同时插旗；旗帜只显示位置，不显示玩家身份，锁定前可自由更换。
- 多名玩家可以选择同一出生区，主动换取高风险的近身博弈。
- 锁定后，第一大回合的行动顺序随机产生。
- 原型可由 AI 或调试界面依次完成插旗，不把热座匿名性作为验证目标。

### 4.2 构筑保护期

- 第一至第三大回合，玩家只能在自己锁定的出生区内落子。
- 多名玩家选择同一区域时，共享同一合法落子范围。
- 围杀、覆盖、信物发现和势力计算在保护期内均正常运行。
- 保护期暂停“盘面与手牌同时为空即出局”的检查。即使棋子暂时被清空，玩家仍能在下个小回合正常征募和重新落子。
- 第三大回合结束后解除区域限制。所有尚未弃赛的玩家都保证获得第四大回合中的第一次小回合，即使此时盘面与手牌同时为空也不会提前出局。
- 第四大回合起，玩家可以在全图任意合法空格落子；盘面为空的玩家可在自己的第一次小回合完成征募后，从任意合法空格重新进入。
- 每名玩家的开局出局保护在其第四大回合的小回合完成后单独解除；从此以后按正常规则即时检查该玩家是否出局。

## 5. 玩家小回合

每名玩家的小回合按以下顺序执行。

### 5.1 信物快照

- 读取此刻由玩家控制的非先锋信物。
- 生成本小回合的征募展示数、免费选取数、手牌类型槽和部署上限。
- 本小回合中新占领的信物不改变已经生成的参数，从玩家下一小回合开始生效。
- 先手玩家若夺走后手的信物，后手在本大回合轮到自己时无法获得该效果。

### 5.2 整理手牌

- 默认拥有 5 个手牌类型槽。
- 每个槽位对应一种棋子类型，同类型数量无限叠加。
- 玩家可以主动整类弃牌，为新类型腾出槽位。
- 若兵站信物丢失导致槽位缩水，玩家必须在征募前整类弃牌，直至类型数量合法。

### 5.3 私人征募

- 默认展示 5 枚棋子，玩家最多免费选取 3 枚。
- 同一面板允许出现重复棋子。
- 无金币、无基础免费刷新；未选棋子在小回合结束时消失。
- 新类型在没有空槽时不可选择，已有类型可继续叠加。
- 征募候选和选择过程只对当前玩家可见。
- **落后者征募补偿**（对局配置，标准局开启）：在小回合开始生成效果快照的同一时刻读取此刻的公开势力名次（只含参赛玩家，势力相同者共享名次），名次 > ⌈参赛人数 ÷ 2⌉ 时展示数 +1；名次等于当时参赛玩家中的最大名次且 > 1 时免费选取数再 +1（两档可同时获得）。补偿与信物加成直接相加，只在快照生成时读一次，本小回合内名次变化不回收，也不累计或结转到之后的小回合；已出局与已弃赛玩家既不计入参赛人数，也不获得补偿。全员并列第 1 时无人获得补偿，因此保护期内照常生效、不需特判。（catch-up-recruit 新增。）

### 5.4 批次部署

- 基础部署上限按大回合分阶段：第 1–3 大回合为 3，第 4–6 大回合为 4，第 7 大回合及以后为 5；在小回合开始生成效果快照时按当前大回合读取一次，本小回合内不变。军令信物在该基础值上继续提高，不设置全局硬上限。（v1.1 原文为“基础部署上限为 3”，growth-pass-1 调整为分阶段。）
- 玩家从手牌中暂放 0 至当前部署上限枚棋子。
- 批次内可以包含不同类型，也可以分散在彼此不相连的位置。
- 暂放棋子可自由撤销、换位和替换，确认前不改变正式盘面。
- 玩家不能把棋子暂放在当前已被占据、但预计会在本批次中腾空的格子上。

### 5.5 Pass

- 玩家确认 0 落子时记为 Pass。
- Pass 会撤销本小回合从私人刷新面板中新获得的全部棋子；进入小回合前已有的手牌不受影响。
- 即使新旧棋子属于同一类型，系统也只扣回本轮新增数量。
- 只要批次中至少确认 1 枚棋子，本轮征募所得便全部保留，包括尚未部署的部分。
- 未使用的部署额度不会跨回合保存。

## 6. 批次合法性与围杀结算

### 6.1 合法性预演

系统以完整批次的最终状态判断合法性，而不是逐枚结算。

1. 所有暂放位置必须为空、可落子（非障碍、非未架桥深水），并符合前三大回合的出生区限制；每个改造目标必须满足 §3.4 的全部约束。**第 1 步的全部判定都基于本批次开始前的地形**（批次内不链式）。
2. 暂放数量不得超过部署上限，所用棋子不得超过手牌库存。
3. 模拟放置整个批次。
4. 同时应用本批次的全部改造。
5. 找出所有因此无气的敌方棋串，并同时移除。
6. 再检查当前玩家的所有相关棋串；若仍有棋串无气，则整个批次属于自杀手，不可确认。
7. 计算结算后盘面；若与此前任一已提交盘面完全相同（盘面表示含设施与地表，见 §6.2），则触发盘面同形禁则，不可确认。

一个单独看似无气的暂放棋子，只要完整批次完成提子后获得气，便属于合法落子。第 4 步先于第 5 步是有意的：立栅可以敲掉敌串的最后一口气，堵死自己则算自杀手（§3.4）。

### 6.2 盘面同形禁则

- 只比较每次合法批次结算后的盘面，不比较预览中的中间状态。
- 盘面状态包含每格的占用者与棋子类型，以及**当前的桥集合、栅栏集合与被改造过的地表**（§3.4）。
- 不比较手牌、征募结果、信物控制、势力值、行动顺序或当前行动者。
- 同一玩家在同一位置用不同类型棋子形成的盘面不视为同形。
- 棋子分布相同但设施或地表不同的两个盘面不视为同形。改造不可逆，设施集合单向扩张，纳入同形表示不会造成振荡，反而避免"棋子回到老位置但地形已变"被误判为同形。

### 6.3 正式结算顺序

合法批次确认后：

1. 从手牌扣除已部署棋子。
2. 将整个批次加入正式盘面。
3. 同时应用本批次的全部改造（§3.4）。
4. 同时移除所有无气的敌方棋串。
5. 更新首次进入覆盖范围的信物并永久公开其内容。
6. 重新计算信物控制、据点控制（§7.4）、空格归属、棋串军势（含高地加值）和总势力（据点分 + 军势）。
7. 更新公开排名并检查出局与终局条件。

整个过程对外表现为一次原子状态变更：外部观察者不得观察到"棋子已落下但设施未写入"或"设施已写入但敌串尚未移除"的中间盘面。

围杀没有通用金币、抽棋、经验或击杀分奖励。其收益来自削弱敌方军势、释放空间并改变信物与据点的控制（被提走的占据者立即失去据点，低处被压制的棋子消失也会带走对方的高地加值）。首轮原型不加入击杀成长棋子。

## 7. 覆盖、空格归属、信物与据点控制

### 7.1 占据优先

- 棋子所在格由棋子所有者直接控制，不会因敌方覆盖而进入争议状态。
- 若要改变已占据格的归属，必须先围杀并移除该棋串。
- 棋子按覆盖关系（§3.1）提供覆盖：通常是几何相邻格；林地不接收覆盖；跨崖壁只能居高临下（目标不比棋子高 2）；遇一格宽的深水时落到对岸那一格，宽河不行；栅栏不挡覆盖。覆盖不沿斜向传播，不穿过障碍或棋盘外沿。

### 7.2 空格归属

- 空格只受到一名玩家覆盖时，为该玩家**独占**。
- 空格同时受到多名玩家覆盖时，为**争议**，不属于任何玩家。
- 空格没有受到任何覆盖时，保持**中立**。
- 覆盖来源的棋子数量不影响判定：同一玩家用 1 枚还是 4 枚棋子覆盖同一空格，都是该玩家独占。
- 障碍与未架桥的深水不属于任何玩家。
- 空格归属**不产生任何势力分数**（裁决 D8），只用于信物与据点的控制判定（§7.3、§7.4）和盘面层归属读法的展示（§14.2）。

### 7.3 信物发现与控制

- 所有信物在开局时根据对局种子一次性生成，之后不会刷新或迁移。
- 信物格首次进入任意玩家的覆盖范围、或首次被任意玩家的棋子占据时，具体内容向所有玩家永久公开；即使该格立即成为争议格，也会完成揭示。林地不接收覆盖，林地上的信物只能靠占据揭示与控制。
- 玩家直接占据信物格或唯一覆盖该格时获得控制权。
- 争议信物不向任何玩家提供效果。
- 揭示不会授予永久收益；失去控制后，信物效果不再进入玩家下一次快照。
- 弃赛玩家的遗留棋子仍可控制或封锁信物，但弃赛玩家不再获得新的行动、征募或先手收益。

### 7.4 据点控制

据点是地图静态数据里的固定格：位置与档位由设计师给定、随地形一起做 C4 对称校验（§3.3），与信物格完全分离、不重合，对局中不迁移、不随机。据点格是普通可落子格，不阻挡棋子；据点没有"未发现"状态，位置、档位、分值与控制状态开局即向所有玩家公开。

| 档位 | 名称 | 标准局分值 | 4 人基准图布点 |
|---:|---|---:|---|
| 1 | 营帐 | 5 | 每个出生区内 1 个（h=2 非信物格） |
| 2 | 篝火 | 15 | 每个出生区的河外低地里 1 个，与邻家 h=2 高台几何相邻 |
| 3 | 石碑 | 45 | 岛上 4 个，非林地 |

- 三档分值是对局配置，与大回合上限一样开局前确定、公开、写入存档，进行中不可改；必须为正整数且 营帐 ≤ 篝火 ≤ 石碑，违规配置在建局时被拒并指出违规档位。标准局 5 / 15 / 45 经 `scoring-sites` 扫档后保持不变（§16）；改动时须同时更新 §10.1 与变更记录。分值放在对局配置而不放进地图数据，是为了同一张图可以换口径扫档而地图 Id 仍唯一。
- 控制判定与信物控制（§7.3）同口径，复用同一份覆盖数据，不另写覆盖语义：
  - 某玩家的棋子直接占据据点格 → **占据**，该玩家控制。占据优先于覆盖判定。
  - 据点格为空且被某玩家唯一覆盖 → **唯一覆盖**，该玩家控制。
  - 据点格为空且被多名玩家覆盖 → **争议**，无人控制。
  - 据点格为空且无人覆盖 → **无人**。
- 林地不接收覆盖，林地上的据点只能靠占据控制（4 人基准图的石碑不放林地）。
- 每个被控制的据点向控制者计入该档分值（§10.1）；争议与无人不产生分数。据点分不参与任何棋串倍率，不取整、不折算。
- 据点控制在每次合法批次结算与每次 Pass 后、于势力重算的同一步内实时重算，只依赖当前盘面，不保留历史控制状态。
- 弃赛玩家遗留的棋子照常占据与覆盖据点；由此控制的据点分计入该弃赛玩家的盘面势力（与其棋串军势同样显示、不占名次、不参与获胜判断）。
- 据点除分值外不提供任何效果：不改征募、部署、先手、气或覆盖。

标准算例（与 `site-control`、`coverage-territory`、`elimination-endgame`、`map-definition` 规格及测试一致；分值为标准局 5 / 15 / 45，另注明者除外）：

| 算例 | 布置 | 结果 |
|---|---|---|
| 占据即控制 | A 的棋子位于某石碑格上，B 的两枚棋子覆盖该格 | 占据，由 A 控制 |
| 唯一覆盖即控制 | 某篝火格为空，只有 B 的棋子覆盖它 | 唯一覆盖，由 B 控制 |
| 多人覆盖即争议 | 某营帐格为空，同时被 A 与 C 覆盖 | 争议，A 与 C 均不得分 |
| 居高临下制造争议 | h=0 的空篝火，所在低地的主人 A 的棋子在旁边同高格覆盖它，邻家 B 的棋子位于与它几何相邻的 h=2 高台格 | A、B 同时覆盖，争议；A 若要独得必须落子占据 |
| 仰视无法争夺 | h=2 的空营帐只被主人 A 的同高棋子覆盖，B 的棋子位于与它几何相邻的 h=0 格 | B 不覆盖该营帐，唯一覆盖，由 A 控制 |
| 保护期内篝火归邻家 | 第 1 大回合，邻家在与某篝火几何相邻的自家 h=2 格落子，篝火所在低地的主人尚无棋子在低地 | 邻家唯一覆盖控制，获得该篝火分值 |
| 失去控制立即掉分 | A 唯一覆盖一块石碑得 45 分，随后 B 落子使该石碑变为争议 | 该次结算后 A 的势力立即减少 45 |
| 占据者被围杀 | 占据某篝火的 A 的棋串被 B 围杀移除，提子后该格只被 B 覆盖 | 同一次结算内改由 B 以唯一覆盖控制，A 失去 15、B 获得 15 |
| 弃赛者封锁据点 | 已弃赛玩家 D 的棋子占据某营帐 | 由 D 控制，参赛玩家均不得分；D 的盘面势力含这 5 分，但 D 不占名次 |
| 弃赛者遗留棋子制造争议 | 已弃赛玩家 D 的棋子与参赛玩家 A 的棋子同时覆盖某个空据点 | 争议，A 不获得该据点分 |
| 分值取自对局配置 | 以据点分值 10 / 30 / 90 开局，A 控制 1 个篝火 | A 从据点获得 30 分 |
| 分值配置非法被拒 | 以 5 / 0 / 45 或 20 / 15 / 45 开局 | 对局配置校验失败并指出违规的档位 |
| 据点不带额外效果 | A 控制全部 4 块石碑 | A 的征募展示数、免费选取数、部署上限、先手修正与未控制时完全相同 |
| 开局即可见 | 对局开始、尚无任何棋子 | 所有玩家都能看到 12 个据点的位置、档位与分值，控制状态均为无人 |

## 8. 信物系统

### 8.1 原型信物

| 信物 | 基础效果 | 作用 |
|---|---:|---|
| 探勘 | 征募展示数 +1 | 提高找到目标棋的机会 |
| 征召 | 免费选取数 +1 | 提高持续供给 |
| 兵站 | 手牌类型槽 +1 | 支持更复杂的组合 |
| 军令 | 部署上限 +1 | 提供单轮爆发能力 |
| 先锋 | 先手修正 +1 | 支持抢先流 |
| 流派徽记 | 提高指定棋子类型权重 | 定向形成空间组合 |

- 同类信物可以叠加，结构数值直接相加。
- 不设置统一数值硬上限，由地图中信物数量、位置和生成预算控制极值。
- 公共争夺区可生成效果 +2 的高阶结构信物；高阶流派徽记等效为两枚普通徽记。
- 先锋只在大回合结束、生成下一轮顺序时读取；其他信物在玩家小回合开始时读取。

### 8.2 生成权重

出生区信物分布：

| 类型 | 权重 |
|---|---:|
| 流派徽记 | 45% |
| 探勘 | 20% |
| 兵站 | 15% |
| 征召 | 8% |
| 军令 | 7% |
| 先锋 | 5% |

出生区不生成效果 +2 的高阶信物。

公共争夺区信物分布：

| 类型 | 权重 |
|---|---:|
| 流派徽记 | 30% |
| 探勘 | 15% |
| 兵站 | 10% |
| 征召 | 15% |
| 军令 | 15% |
| 先锋 | 15% |

公共区约 20% 的结构信物升级为效果 +2。生成过程使用“随机类型 + 区域强度预算”，保证各出生区总稀有度接近，但具体组合仍具有随机性。

## 9. 棋子与征募权重

### 9.1 初始配置

- 每名玩家开局获得 5 枚普通子。
- 所有玩家使用同一基础棋池。
- 基础棋池权重为：普通子 40、堡垒子 20、连珠子 18、倍增子 12、协同子 10、**匠人 10**。匠人那一档是**对局配置**（照据点分值的口径：开局固定、公开、入存档、写进批量跑局的 `config.json`），其余五档不可配置；配置匠人权重只替换它自己那一档，不改动其余五档的绝对值。
  - 代码内的默认值 `RecruitWeights.DefaultArtisanWeight` 目前是 **10**；**扫档拍板值是 5**（裁决 T-13，见 §16），`sim-out/artisan-final/` 的确认 200 局用 `--artisan-weight 5` 显式给出。默认值改为 5 尚未落地，见 `.trellis/tasks/09-18-artisan-terrain-edit/implement.md` 段 E 待决 1。
- 流派徽记可以绑定六种类型中的任意一种，含匠人；匠人徽记与其余五种按同一条公式调权。
- 每枚对应类型的流派徽记按下式调整权重：

`调整后权重 = 基础权重 ×（1 + 0.75 × 徽记数量）`

调整后重新归一化所有类型的出现概率。每个候选位独立抽取，允许重复类型。

### 9.2 原型六种棋子

| 棋子 | 基础军势 | 效果 |
|---|---:|---|
| 普通子 | 1 | 无附加效果，用于连接、围杀和扩张 |
| 堡垒子 | 4 | 提供高基础军势，但围杀规则与普通子相同 |
| 连珠子 | 1 | 每条长度为 L 且 L≥2 的连续横向或纵向连珠，提供 `L×(L−1)` 位置加值；"连续"指相邻两枚之间存在气边，隔着崖壁、未架桥深水或栅栏的连珠子不成一条线 |
| 倍增子 | 1 | 棋串中每枚倍增子令该棋串的基础军势依次乘以 1.5，至多 3 枚生效，只放大基础军势、不放大连珠 / 协同 / 高地的位置加值与据点分（第 4 枚起只提供基础军势与共享气；v1.1 为至多 5 枚，growth-pass-1 调整为 4，multiplier-rebalance 调整为 3 并改为只放大基础军势） |
| 协同子 | 1 | 每枚协同子根据同棋串中除协同子外的棋子类型数，每种提供 2 点位置加值 |
| 匠人 | 1 | 落子的那一瞬间可携带一个地形改造（搭桥 / 立栅 / 烧林，§3.4）；落下之后就是一枚普通棋子，**没有任何持续效果**，被提走也不撤销已完成的改造 |

- 不同类型可以构成同一棋串并共享气与倍率。
- 十字交叉中的连珠子可以同时参与一条横线和一条竖线。
- 每条连续线按其最大长度计算一次，不把同一条线拆成多个子区间重复加分。
- 所有棋子遵守完全相同的有气、无气和围杀规则；类型效果不得提供额外气、免死或复活。
- 棋子与组合状态随当前盘面实时重算，不保留永久成长层数。

## 10. 势力值

### 10.1 计算公式

`总势力 = 控制中的据点分之和 + 所有己方棋串的军势值之和`

每个棋串独立计算：

`棋串军势 = 向下取整〔基础军势总和 × 1.5^生效倍率指数〕 + 位置加值`

`位置加值 = 连珠加值 + 协同加值 + 高地加值`

生效倍率指数 = min(倍增子数量, 3)，倍率上限 1.5^3 = 3.375；超出第 3 枚的倍增子仍计入基础军势、共享气并参与类型计数。倍率只放大基础军势，连珠、协同与高地的位置加值在取整后直接相加、不被倍率放大；取整只作用于"基础军势总和 × 倍率"，逐棋串各一次。（v1.0 原式为 `1.5^倍增子数量`，无上限；v1.1 封顶为 5、倍率上限 7.59375；growth-pass-1 调整为 4；multiplier-rebalance 调整为 3，并由 `⌊(基础 + 位置加值) × 倍率⌋` 改为只放大基础军势。）

- 据点分见 §7.4：每个被控制的据点按档位计入对局配置的分值（标准局营帐 5 / 篝火 15 / 石碑 45），争议与无人不计；据点分不参与棋串倍率，不取整、不折算。
- 空格归属（独占 / 争议 / 中立）不产生任何分数；棋子所在格只通过该棋子的基础军势计分。
- 势力值只评价当前盘面，不可消耗，也不累计历史积分。
- 棋串被围杀、连珠断开、倍增子失去连接、被压制的低处敌子被提走或据点失去控制后，相关分数立即下降。
- 势力明细公开且结构化：据点分总计与所控制据点清单（坐标、档位、分值、控制方式：占据 / 唯一覆盖）；每条棋串的棋子坐标、基础军势总和、位置加值及其连珠 / 协同 / 高地拆分、倍增子数量、生效倍率指数、倍率与取整后军势。独占空格坐标集合仍可得，只供盘面层归属读法展示，不出现在任何计分字段中。

**高地压制加值**：对盘面上每一枚棋子 s（任意类型、任意玩家，含弃赛者遗留棋子），若存在格 t 同时满足 ① t 是 s 的覆盖目标（§3.1 覆盖关系：含跨崖居高临下、含隔一格宽深水到对岸，林地不是覆盖目标）；② t 上有一枚所有者不是 s 所有者的棋子；③ t 的高度严格低于 s——则 s 提供 1 点高地加值。每枚棋子至多 1 点，无论满足条件的 t 有几个；按棋串求和计入该棋串的位置加值，不被倍率放大，随盘面实时重算。判定只走覆盖关系，不另写邻接或高度比较：不用几何四邻（规则里多一种邻接用法），也不用气边（高差 2 没有气边，每家只有 2 格缓坡，规则几乎不会出现）（裁决 S-4）。

| 算例 | 布置 | 结果 |
|---|---|---|
| 跨崖居高临下 | A 的棋子在 h=2 的 `F7`，B 的棋子在与之几何相邻的 h=0 的 `F6` | A 的 `F7` 提供 1 点高地加值；B 的 `F6` 不提供 |
| 缓坡压制 | A 的棋子在 h=1 的 `F6`，B 的棋子在 h=0 的 `G6`，两格之间无其他地形 | A 的棋子提供 1 点 |
| 同高不加 | A 与 B 的棋子相邻且同高 | 双方都不提供 |
| 林地里的敌子压制不到 | A 的棋子在 h=1 的 `F6`，B 的棋子在 h=0 的林地 `G6` | `G6` 不是 A 的覆盖目标，A 不提供 |
| 隔河压制 | A 的棋子在 h=1 的 `F6`，`G6` 为未架桥深水，B 的棋子在 h=0 的 `H6` | A 的棋子提供 1 点 |
| 多个低处敌子只加 1 | A 在 h=2 的一枚棋子能覆盖到三枚更低处的敌子 | 该棋子只提供 1 点 |
| 己方棋子不算 | A 的棋子在 h=2，其覆盖目标上只有 A 自己在 h=0 的棋子 | 不提供 |

标准算例（与 `power-score`、`piece-effects` 规格及测试一致；据点分值为标准局 5 / 15 / 45）：

| 算例 | 布置 | 结果 |
|---|---|---|
| 孤立棋子 | 一枚没有任何竞争的孤立普通子位于平地中央，四周为空的可落子格、均非据点，附近无敌子 | 该玩家从此局部获得 **1 点**势力（棋子军势）；四个相邻独占空格不计分 |
| 标准棋串 | 同一棋串包含普通子×3、堡垒子×1、倍增子×2，无位置加值 | 基础军势 9、倍率 2.25，棋串军势 `⌊9×2.25⌋ + 0 = 20` |
| 匠人按 1 计 | 同一棋串包含匠人×2、堡垒子×1，无倍增子、无位置加值 | 基础军势 `1 + 1 + 4 = 6`，棋串军势 6；匠人落下后不提供任何附加加值 |
| 位置加值不被倍率放大 | 4 枚连珠子成横线 + 2 枚倍增子，无协同子、无高地加值 | 基础 6、连珠加值 `4×3 = 12`，棋串军势 `⌊6×2.25⌋ + 12 = 25`，而非 `⌊18×2.25⌋ = 40` |
| 含位置加值的计算 | 基础军势 6、位置加值 5、含 1 枚倍增子 | `⌊6×1.5⌋ + 5 = 14`，而非 `⌊(6+5)×1.5⌋ = 16` |
| 高地加值不被倍率放大 | 位于 h=2 的棋串含普通子×2、倍增子×2，其中 3 枚各自压制到一枚更低处的敌子 | 基础 4、高地加值 3，棋串军势 `⌊4×2.25⌋ + 3 = 12`，而非 `⌊7×2.25⌋ = 15` |
| 据点与棋串相加 | 控制 1 个营帐与 1 个石碑，棋串军势分别为 20 与 7 | 总势力 `5 + 45 + 20 + 7 = 77` |
| 独占空格不计分 | 12 个独占空格、不控制任何据点、棋串军势合计 27 | 总势力 27 |
| 倍率不作用于据点分 | 控制一块 45 分的石碑，并拥有一条倍率 2.25 的棋串 | 45 按原值计入总势力，不乘 2.25 |
| 明细可复算总势力 | 读取任一玩家的势力明细 | 据点分总计 + 全部棋串军势 = 总势力 |
| 据点分可溯源 | 某玩家据点分总计 65 | 明细列出所控制据点，分值之和为 65，且逐个标明占据或唯一覆盖 |
| 位置加值可溯源 | 某棋串位置加值 14 | 明细拆出连珠、协同、高地三部分，三者之和为 14 |

### 10.2 排名

- 每个合法批次结算后立即更新所有玩家势力值和公开排名。
- 排名只影响信息展示、下一大回合顺序与落后者征募补偿的判定（§5.3），不在每轮转换为累计积分。
- 已弃赛玩家继续显示其盘面势力，但标记为“已弃赛”，不参与行动顺序、落后者征募补偿判定或获胜判断。

## 11. 大回合行动顺序

### 11.1 基础顺序

- 第一大回合随机排序。
- 此后每个大回合结束时，排除已弃赛和已出局玩家，将参赛玩家按势力值从高到低排列。
- 势力值越高，基础行动位置越早；该正反馈是“抢先流”的核心收益。
- 排名的作用范围到此为止：信息展示、下一大回合顺序、落后者征募补偿（§5.3）。排名不转换为任何累计资源，领先者也不因排名获得资源。

### 11.2 先手值

`先手值 =（参赛人数 − 势力名次）+ 先手修正`

- 先锋信物提供先手修正。
- 按先手值从高到低生成下一大回合顺序。
- 先手值相同时，先手修正更高者优先；仍相同则势力值更高者优先；再次相同则上一大回合行动更晚者优先。
- 首回合所有条件仍相同时，使用对局种子确定稳定顺序。

4 人局中，第 4 名基础先手值为 0。若拥有“先手 +2”，其先手值变为 2，可以与原第 2 名竞争行动位置。

## 12. 出局、弃赛与终局

### 12.1 出局

- 每名玩家完成第四大回合中的第一次小回合后，开局出局保护解除。此后每次合法批次结算或 Pass 完成后，都检查所有已解除保护的参赛玩家是否满足出局条件。
- 尚未获得第四大回合第一次小回合的玩家不会因先手玩家的行动提前出局。
- 玩家同时满足“盘面没有棋子”和“手牌没有棋子”时立即出局。
- 出局玩家不再获得小回合，也不参与最终获胜判断。

### 12.2 主动弃赛

- 玩家可以主动弃赛并继续观战。
- 弃赛立即失去本局胜利资格，不再获得小回合、征募和先手效果。
- 弃赛者既有棋子与棋串不会消失，仍照常产生覆盖、争议格和势力，也可以被其他玩家围杀。
- 弃赛者遗留的两眼活棋可能永久留在棋盘上，成为无法清除的地形压力。
- 弃赛者的势力值继续显示，但最终排名低于所有正常完成比赛的玩家。
- 未来带入带出系统可以读取弃赛时的盘面、手牌和资源快照；具体收益不属于核心原型范围。

### 12.3 终局条件

满足任一条件立即结束：

1. 只剩一名参赛玩家，该玩家直接获胜。
2. 一个完整大回合中，所有参赛玩家均确认 0 落子并 Pass。
3. 盘面不存在任何可落子的空格。
4. 第「大回合上限」个大回合结束时对局仍在进行（达大回合上限）。
5. 势力碾压：某参赛玩家的势力值 ≥ 其余全部参赛玩家势力值之和，并保持到其余参赛玩家各完成一次小回合（候选制，见下），该玩家直接获胜。

大回合上限是对局配置的一部分：随地图与对局种子一同在开局前确定、对所有玩家公开、写入存档，进行中不可更改。标准局初值 15；配置为 0 表示不设上限。条件 1–3 与条件 5 保持"立即"生效且优先于条件 4：上限只在大回合结束（最后一名参赛玩家的小回合完成结算或 Pass 之后、生成下一大回合行动顺序之前）检查一次，同一时刻同时满足条件 2 与条件 4 时记为条件 2。

势力碾压采用候选制。当前大回合 ≥「碾压起始大回合」时，每次合法批次结算与每次 Pass 完成后检查碾压式；恰有一名参赛玩家满足且当前没有候选时，该玩家成为碾压候选，此刻其余全部参赛玩家记为待回应名单（多人同时满足——只可能是最高两人势力相同且其余为 0——不产生候选）。此后每个检查点复查：候选跌破碾压式或出局 / 弃赛即取消、名单清空，之后再次满足时重新建立，此前的回应进度不保留；名单中的玩家每完成一次小回合（确认或 Pass），或出局 / 弃赛，即移出名单；名单为空且候选仍满足时碾压成立。碾压只统计参赛玩家：出局与弃赛者的势力不计入其余之和，其本人也不能成为候选。碾压起始大回合与大回合上限同为对局配置（开局前确定、公开、写入存档、进行中不可改），标准局初值 7，0 表示关闭势力碾压；候选玩家与待回应名单始终公开并随存档保存。

同一时刻满足多条时，终局原因按以下优先级记录：只剩一名参赛玩家（条件 1）> 势力碾压（条件 5）> 棋盘填满（条件 3）> 整轮 Pass（条件 2）> 达大回合上限（条件 4）。

第 2、3、4、5 种情况下按当前势力值排名（以势力碾压终局时，获胜者必为势力最高者，按同一规则自然位列第 1）。势力值相同时，依次比较控制中的信物数量、控制中的据点数量（§7.4）和盘面棋子数；仍完全相同则共享同一名次。独占空格数不参与比较：例如两名玩家势力均为 60、控制信物均为 2，控制据点 3 与 1，则前者名次高。弃赛者排在所有完赛玩家之后；多名弃赛者按弃赛时势力值排序。出局者按出局先后倒序排列。

## 13. 信息公开规则

### 13.1 始终公开

- 地图、障碍、出生区和信物格位置，以及据点的位置、档位与当前分值配置。
- **当前地形**：每格的高度、地表、障碍，以及当前的桥与栅栏。对局中完成的改造与地图预置的设施**以同一形式呈现**，玩家无法也无需区分；公开视图与盘面 MUST NOT 标记改造者（日志为平衡分析记录改造方，见 §17）。
- 已发现信物的类型、强度和当前控制者。
- 盘面上每枚棋子的玩家与类型。
- 空格归属、争议状态、据点控制状态（占据 / 唯一覆盖 / 争议 / 无人及控制者）、棋串结构与可推导的气。
- 每名玩家的当前势力、势力明细（据点分与所控制据点清单；每条棋串的基础军势、位置加值的连珠 / 协同 / 高地拆分、倍率与最终军势）、行动顺序和公开属性。
- 每名玩家手牌中当前存在的棋子类型。

### 13.2 保持隐藏

- 尚未发现的信物内容。
- 玩家当前私人征募面板及其选择过程。
- 对手每种手牌的具体数量。
- 对手尚未确认的批次部署。

## 14. 信息界面

### 14.1 批次预演

部署期间应显示：

- 暂放棋子的落点、类型和手牌消耗；匠人另显示其改造目标与动作类型。
- 当前部署上限及已用数量（改造不额外占额度）。
- 提交后预计被围杀的棋串（含仅由改造导致无气的棋串）。
- 提交后的己方棋串、气和自杀风险，**按应用改造后的地形计算**。
- 当前匠人落点的全部合法改造目标（可搭桥的深水格、可烧的林地格、16 条可立栅的边），供玩家换目标。
- 各棋串的“基础军势 + 位置加值 × 倍率”预览。
- 预计势力和排名变化。
- 首次进入覆盖范围但仍未知的信物格只显示“将揭示”，不提前显示内容。

非法批次必须说明具体原因，并高亮相关落点或棋串。

### 14.2 战术信息层

按住战术视图键时进入临时信息模式；松开后立即返回默认棋盘。键鼠与手柄可分别配置，辅助设置允许将“按住显示”改为“点击切换”。

- **盘面层**：弱化棋子效果和演出，有两种读法，一次只显示一种，用独立键（`Tab`）切换：
  - **归属读法**：显示每个格子的占据、独占、争议与中立状态。
  - **棋串读法**：突出棋串轮廓、所有气和危险棋串；**栅栏侧必须与空侧可区分**，玩家据此看出气边被堵在哪里。
- **势力层**：显示每个据点的档位、分值与控制状态（占据 / 唯一覆盖 / 争议 / 无人及控制者，争议时标出各覆盖方）、各棋串分数（位置加值拆出连珠 / 协同 / 高地）及倍率热区；空格不再标任何分数。据点本身在默认棋盘上已有地标（§20），势力层负责"看分"，不另设据点层（裁决 S-5）。
- **信物层**：突出信物内容、强度、控制者和失效原因；未发现内容继续隐藏。
- **顺序层**：显示势力名次、先手修正、先手值和下一大回合顺序。

信息层可在批次部署和其他玩家行动时查看，不改变任何游戏状态。

**暂放匠人时的可改造目标标示在默认棋盘上**（不依赖打开任何信息层）：玩家暂放一枚匠人时，系统标出该落点的全部合法改造目标——可搭桥的深水格、可烧的林地格、16 条可立栅的边，并与已有设施在外观上区分（§20）。这条不放进战术层，是因为"能改哪里"是当前这一手的直接后果，与暂放棋子本身同级；放进层里会逼玩家一边按住层键一边选目标。

立起新栅栏后归属读法与棋串读法的差集同样要能由地形解释：被覆盖但不是气的格来自居高临下、隔岸或**栅栏**（含对局中新立的），是气但无人覆盖的格来自林地（含尚未被烧掉的）。

归属与棋串合为一层而非两层，是因为二者描述的是同一批棋子对周边空格的两种作用。在 `terrain-model` 之前两者点亮的空格恒等（覆盖 = 四邻接相邻格 = 棋串的空邻格）；地形进入规则后气边与覆盖关系分开（§3.1），两个集合只在没有崖壁、林地、栅栏与深水的平地区域恒等，差集必须能由地形解释：被覆盖但不是任何棋串的气的格，来自居高临下、隔岸或栅栏；是气但无人覆盖的格，来自林地。盘面层要把这份差集连同原因一并列出。分成两层只会让玩家在同一片格子的两种着色之间来回切换。两者各自仍有对方没有的内容：归属读法另画有棋子的格与无人覆盖的中立格，棋串读法另给棋串分组与危险度。

读法切换用独立键而不是层键：默认的"按住显示"模式下层键正被按住，没有"再按一次"可言，绑在层键上会让按住显示的玩家被锁死在一种读法里。

### 14.3 手牌信息面板

- 点击手牌信息按钮打开全玩家对比面板，再次点击或返回即可关闭。
- 自己的区域显示棋子类型、准确数量、槽位占用，以及本轮新征募但尚未提交的数量。
- 敌方区域只显示其持有的棋子类型，不显示数量、私人刷新或未确认部署。
- 面板同时显示每名玩家公开的展示数、选取数、手牌槽、部署上限及其信物来源。
- 已弃赛玩家保留最后的公开手牌类型，并清楚标记为不再行动。

## 15. AI 设计

### 15.1 玩家对战 AI

- 正式人机混合模式中，AI 只能读取公开信息和自己的私有信息。
- AI 不得读取未发现信物内容、敌方手牌数量或未确认批次。
- AI 对未知信物和敌方库存只能根据公开规则与概率估计。

### 15.2 启发式决策

AI 对候选征募和批次使用加权评价：

- 即时势力增量。
- 敌方势力损失与提子规模。
- 信物发现、控制和阻断价值。
- 棋串剩余气、两眼潜力和整体被围杀风险。
- 连珠、倍增与协同组合成长。
- 下一大回合先手位变化。
- 手牌供给与部署能力是否匹配。

简单难度只考虑即时收益；更高难度扩大候选批次数量，并不通过读取隐藏信息提高强度。部署上限较高时，AI先筛选高价值落点，再组合有限数量的候选批次，避免穷举所有排列。

### 15.3 原型调试 AI

用于批量跑局的调试 AI 可以选择读取完整权威状态，以更快发现极端组合；该模式必须标记为测试专用，不得用于面向玩家的对局。原型还应允许设计者在任意小回合人工接管任何 AI。

## 16. 原型数值目标

- 第 1–3 大回合：基础部署上限 3，部署上限通常保持在 3，完成基础棋串和定向构筑。
- 第 4–6 大回合：基础部署上限 4，多数玩家部署上限处于 3–5（基础 4，军令在其上叠加），开始争夺公共信物。
- 第 7 大回合以后：基础部署上限 5，成型玩家通常达到 5–8 部署（基础 5 + 军令）；极端构筑允许超过 8。
- 势力值从开局个位或十位成长到中期几十至一百左右；成熟倍增棋串允许达到数百。
- 首次跨出生区冲突通常发生在第 4–5 大回合。**这个时点由盘面密度决定，不由保护期长度决定**：实测首次提子稳定发生在盘面棋子数占可落子格约 62% 时，因此"冲突落在第几大回合"等价于"第几大回合的落子量填到可落子格的 62%"。v1（109 个可落子格）下密度曲线为第 3–7 大回合 26 / 37 / 50 / 60 / 65%，62% 这条线落在第 6 大回合，实测平均第 6.52；把可落子格压到 85 后，第 4 大回合结束的约 52 枚即达 61%。凡是改地图尺寸、改障碍数或改部署上限，都必须同时复看平衡报告里的"冲突时的盘面占用率"（§17 第 9 条），否则冲突时点会静默漂移。62% 是 v1 / v2（无地形字段）上的实测，不是常数：13×13 地形在旧计分下为 61.0%，换成据点计分并取 `Safety = 27` 后降到 38.6%（`sim-out/sites-final/`），冲突仍在第 4 大回合；加入匠人与地形改造、`Safety` 取 35 后为 38.8%（`sim-out/artisan-final/`），冲突第 4.01 大回合，两者一致。
- 4 人完整大回合平均不超过 3 分钟。
- 多数标准局在第 7–10 大回合结束，平均约 20–30 分钟，但规则允许更长拉锯。
- 在至少 200 局同强度 4 人 AI 对局中，第 3 大回合领先者的最终胜率目标不高于 50%，避免“势力 → 先手 → 抢资源”循环接近必胜。**基线两侧的偏离同样算偏离**：胜率显著低于 25% 的随机基线意味着早期领先反而是劣势，对局沦为运气，与本条的意图一样相悖。
- **上列各项的实测值必须标注 AI 权重口径**。AI 的评价权重直接决定这些指标，不同权重下的数据不能直接相比。截至 2026-09-18，`Safety` 为校准值 **35**（`artisan-terrain-edit` 在 v4 地图 + 据点分值 5 / 15 / 45 + 匠人与地形改造上重扫确定，裁决 T-13）。口径谱系：2026-09-17 定的 27 口径为"v4 + 据点计分、无匠人"，加入改造后失效；2026-09-16 定的 5 口径为 v2 地图 + 旧计分，计分口径变更后已作废；更早各轮的实测数据（`multiplier-rebalance`、`catch-up-recruit`、`denser-map` 的 200 局回归）口径均为未校准的 `Safety = 20`。三者都不得与本版数据直接相比。
- **规则一变，`Safety` 就得重扫**：本轮实测同一个 27 在有改造的盘面上把领先者胜率推到 35.5%（无改造时是 25.5%）。栅栏给了"补一口气就加分"之外的第二条保守手段，收敛性与胜率曲线整体右移，27 → 35 才把领先者胜率压回 24.0%。这与 `scoring-sites` 的教训同源（计分口径一变默认权重必须重扫），本轮是**规则口径**一变。
- **被围杀风险这一维的权重与对局收敛性不是单调关系**：权重越高，被威胁的棋串越是总有“补一口气就加分”的手可下，AI 因此不再 Pass，对局拖至回合上限。调这一维必须双向扫档。v4 + 据点分值 5 / 15 / 45 上九档加 22 / 25 / 27 三个加密档（各 200 局，种子 1–200）：第 3 大回合领先者胜率 3→80.5%、5→95.5%、10→95.0%、20→95.5%、22→76.0%、25→31.0%、27→25.5%、30→17.0%、40→13.5%、60→15.5%，不收敛率依次 64.5 / 49.5 / 28.5 / 29.5 / 27.0 / 15.0 / 6.5 / 8.0 / 12.0 / 25.0%，40 与 60 过半对局整局无提子。领先者胜率在 20 与 30 之间陡变，粗档会漏掉可行值，临界区必须加密。低 Safety 下的机理：第 3 大回合四家据点分相同（自家营帐 + 邻家篝火），军势领先者按先手规则第 4 大回合先动，抢下岛上约 3 块石碑后滚雪球；据点分量三档（3 / 8 / 24、5 / 15 / 45、10 / 30 / 90，Safety 5）领先者胜率 90.0% / 95.5% / 92.0%，分量不是这一指标的杠杆。
  加入匠人与地形改造后（`artisan-terrain-edit`，裁决 T-13）重扫一遍，结论方向不变但整条曲线右移：先在 Safety 27 下扫匠人征募权重 5 / 10 / 18，领先者胜率 35.5 / 45.5 / 51.5%、不收敛率 38.0 / 44.5 / 57.0%、据点分占比 26.4 / 30.1 / 35.7%，取 **5**（匠人越多越滚雪球）；再在匠人权重 5 下双向扫 Safety 25 / 27 / 30 / 35 / 40 / 45，领先者胜率 44.0 / 35.5 / 34.5 / **24.0** / 22.5 / 23.0%，不收敛率 38.0 / 38.0 / 33.5 / **26.0** / 38.0 / 37.0%，每批次提子 0.63 / 0.67 / 0.60 / **0.52** / 0.55 / 0.51。40 与 45 两档整局无提子分别 78 / 94 局（39% / 47%），远超"≤ 5%"的目标，出局；取 **35**。
- 终局据点分占参赛玩家总势力的比例目标 25%–45%（对应"据点分约占三分之一"的设计取向，裁决 S-10）。逐局计算"参赛玩家据点分之和 ÷ 参赛玩家总势力之和"再取批次平均，例如某局终局四人总势力 600、据点分 180 → 30%。
- 整局无提子局目标 ≤ 5%（裁决 S-10）：同一地形在旧计分下为 14%，高台自留地不下山也能撑到终局，是本轮要修的问题之一。

**当前实测**（change `artisan-terrain-edit` 确认跑 `sim-out/artisan-final/`：`siege-4p-base-v4`、据点分值 5 / 15 / 45、4 人 Standard AI、`Safety = 35`、**匠人征募权重 5**、大回合上限 15、碾压起始第 7 大回合、种子 1–200；与扫档 `artisan-w5-s35` 档逐项相同）：

| 目标 | 目标值 | 实测 | 判定 |
|---|---|---|---|
| 第 3 大回合领先者最终胜率 | ≤ 50%，且不显著低于 25% | 24.0%（95% 区间 18.6%–30.4%，含 25%） | 达标 |
| 多数标准局结束大回合 | 第 7–10 大回合 | 终局局平均第 7.56 大回合 | 达标 |
| 首次跨出生区冲突 | 第 4–5 大回合 | 第 4.01 大回合 | 达标 |
| 整局无提子局 | ≤ 5% | 0 局（0%） | 达标 |
| 终局据点分占参赛玩家总势力 | 25%–45% | **31.5%** | **达标**（上一版 22.1% 未达标，本轮首次进入目标带） |
| 不收敛率（以达大回合上限终局） | 越低越好 | **26.0%**（整轮 Pass 143、达上限 52、势力碾压 5） | **已知偏离**，见下 |

打法比上一版更主动：每批次平均提子 **0.52**（`scoring-sites` 终版 0.29、同一地形旧计分基线 0.40——`artisan-terrain-edit` 设计文档 Context 预判的"立栅能把提子拉回来"成立）、Pass 率 16.4%、石碑争议小回合占比 48.3%、篝火由所在低地主人控制的小回合占被控制小回合的 19.6%（上一版 10.6%，主人可在自家崖边立栅封住邻家下山路）、高地加值占全部位置加值 1.1%。改造实测：425 次 / 200 局（每局 2.13 次，整局无改造 33 局），搭桥 97（22.8%）、立栅 328（77.2%）、烧林 0；改造直接导致提子 119 次，全部来自立栅；首次改造平均第 2.32 大回合；带改造的匠人占已落匠人 99.5%；匠人选择率 19.9%、盘面占 3.0%、势力占 0.8%（每枚 1 点，与基础军势 1 相符）。

**已知偏离（本轮不修，记录在案）**：

1. **不收敛率 26.0%，显著高于 `scoring-sites` 终版的 6.5%**（达上限终局 52 / 200，上一版 13 / 200）。成因是栅栏使盘面更难填满：一道栅栏把两侧的气边永久切断，被切开的小空区谁落谁死，"整轮 Pass"因此来得更晚，更多对局撞到大回合上限 15。终局条件与保护期属本轮 Non-goals（裁决 T-9 明写第二轮遗留三项不并入本轮），**留待后续单开一轮处理**；在此之前引用本表的收敛数据必须带上这条口径。六档 Safety 里 35 已是不收敛率最低的一档（25 / 27 / 30 / 35 / 40 / 45 = 38.0 / 38.0 / 33.5 / 26.0 / 38.0 / 37.0%），不是没调，是调不下去。
2. **烧林几乎无人使用**：扫档 9 批共 1800 局（匠人权重三档 600 局 + Safety 六档 1000 局 + 确认 200 局）里烧林合计 **1 次**（仅出现在匠人权重 18 的那一批 `sim-out/artisan-w18/`），确认跑 200 局 0 次。直接原因是全图只有 4 格林地，另有一层原因是 AI 单点候选排行榜被 16 条同分栅栏候选挤占（裁决 T-12）。负责人拍板**接受"烧林是冷门动作"，本轮不加林地、不改地图**（裁决 T-14）——加林地会作废本轮全部基线。分析报告对 0 次的动作照常输出该行，不省略。

## 17. 数据记录与平衡验证

每局应记录：

- 地图和随机种子、据点分值配置、**匠人征募权重**、完整信物分布及揭示时间。
- 每轮征募候选、玩家选择和被撤销的 Pass 征募。
- 每次批次落子、合法性结果、提子数量和盘面同形检查。
- **每次地形改造**：大回合、小回合、改造方、动作类型（搭桥 / 立栅 / 烧林）、目标坐标或目标边，以及该次改造是否直接导致提子。改造记录必须足以离线重建任意时刻的地形；改造方只供平衡分析，公开视图与盘面不得出现（§13.1）。
- 信物与据点的控制变化（据点变化记大回合、小回合、坐标、旧状态与新状态）、各项结构参数和行动顺序。
- 每个棋串的基础军势、位置加值（连珠 / 协同 / 高地拆分）、倍率与最终军势；每名玩家的据点分。
- 每次势力排名变化、Pass、出局、弃赛和最终结果。
- 小回合、大回合与整局耗时。

批量跑局的每个批次另写配置记录（`config.json`），如实写出实际生效的据点分值、匠人征募权重与每名玩家的完整 AI 权重（未显式配置的取默认值），分析报告据此标注口径。产出裁决数据的入口对未识别的命令行选项一律报错退出，不回退到缺省值（change `strict-cli`）——扫档的口径由它把关，不再靠人工核对 `config.json`。

重点分析：

1. 第三大回合领先者与最终胜者的相关性。
2. 先手流是否形成不可逆滚雪球。
3. 各棋子与信物的选择率、控制时长和胜率。
4. 高倍率棋串的形成轮次、峰值及被摧毁概率。
5. 出生区随机资源是否造成显著胜率差异（出生区按 1–4 显示）。
6. 供给、部署、槽位和倍率四条成长轴是否存在唯一最优顺序。
7. 玩家是否频繁用“至少落 1 子”规避 Pass 撤销，并形成无意义拖延。
8. 势力碾压胜的占比、碾压成立（获胜）的平均大回合，以及成立时获胜者与第 2 名的势力比。
9. 首次跨出生区冲突发生时的盘面占用率（盘面棋子数 ÷ 可落子格），与冲突大回合并列输出。
10. 据点：按档位（营帐 / 篝火 / 石碑）给出被控制小回合占比、争议小回合占比、首次被控制的平均大回合、控制过该档据点的玩家胜率；篝火另给出由所在低地主人控制的小回合占比与主人首次控制的平均大回合；石碑另给出由相邻桥头那家（桥头信物所在河外低地的主人）控制的小回合占比；终局快照中据点分占参赛玩家总势力的比例（逐局比值取批次平均，见 §16）；高地加值占全部位置加值的比例。缺据点字段的旧日志整局排除并计数。
11. 地形改造：每局平均改造次数、整局无改造的局数；三种动作（搭桥 / 立栅 / 烧林）各自的次数、占比与"其中直接导致提子"的次数；带改造的匠人占已落匠人的比例；改造直接导致提子的总次数；首次改造的平均大回合；改造过的玩家胜率；终局时新增桥数、新增栅栏数与被烧林地数。**次数为 0 的动作照常输出该行**（"烧林 0 次"本身就是结论，见 §16 已知偏离 2）；缺改造字段的旧日志整局排除并计数。

## 18. 首轮原型范围

### 18.1 必须实现

- 4 人基准地图、匿名插旗和三大回合保护期。
- 六种棋子（含匠人与三种地形改造）、六类信物和区域预算生成。
- 私人征募、类型槽、批次部署及完整围杀预演。
- 势力值、实时排名与按势力重排先手。
- 信物发现、控制和效果快照。
- Pass、出局、弃赛和三类终局判断。
- 战术信息层、全玩家手牌面板和势力明细。
- 简化启发式 AI、批量跑局与人工接管调试功能。
- 完整对局日志。

### 18.2 暂不实现

- 金币、付费刷新、棋子升级和传统数量羁绊。
- 固定轮数（对局恰好 N 轮）、历史积分和通用击杀奖励。§12.3 的大回合上限只是兜底，不是固定轮数，可配置为 0 关闭。
- 统帅、阵营和预设职业。
- 正式联网同步与匹配。
- 2 人、3 人正式地图。
- 带入带出、局外成长和弃赛收益结算。
- 完整博弈树 AI。
- 超出玩法验证所需的正式美术和演出。

## 19. 后续扩展顺序

1. 验证并稳定 4 人核心规则与数值曲线。
2. 设计 2 人、3 人专属地图和对应资源预算。
3. 增加更多通过连接、成线、邻接和资源位置生效的棋子与信物。
4. 实现公平信息边界下的正式 AI。
5. 实现联网同步、断线处理和匹配。
6. 设计带入带出与局外成长，并明确弃赛的收益与代价。

任何扩展都不得改变基础四邻接、气、批次围杀、占据优先和唯一覆盖规则。传统羁绊、统帅或永久成长只有在证明不会取代盘面决策后才考虑加入。


## 20. 视觉风格基准

游戏采用**明快低多边形奇幻棋盘**作为正式视觉方向。首张基准图为 `art/style-exploration/siege-style-03-low-poly-fantasy.png`。

- 场景呈现为悬浮于奇幻世界中的立体战争沙盘，使用温暖自然光、低多边形地貌，以及哑光石材、木材和布料质感。
- 四方势力继续使用红、蓝、金、紫作为主色，同时通过旗帜图形和棋子轮廓区分阵营，不能只依赖颜色。
- 方格边界必须始终清晰。高度、深水、桥、栅栏与林地是承载规则的地形要素，默认视图下必须可判读：三档高度以地砖层高与崖壁侧面区分，高度差 2 的崖壁与差 1 的缓坡要能分开；深水、桥、林地各有可辨的地表表现；栅栏沿两格的公共边立起，不占任一格的落点。纯装饰物（岩石造型、草丛、遗迹、旗帜等）不得遮挡合法落点、气和空格归属的判读。
- 棋盘四边的坐标标注由 `Coord` ↔ 3D 的唯一映射推出，列字母跳过 `I`、个数随棋盘宽度（13 列为 `A B C D E F G H J K L M N`），行数字自下而上；标注平铺在棋盘外圈的 h=0 平面上，边缘格高度不同时仍可读，不得镜像、倒置或遮挡任何格子。
- **据点地标**：三档据点在默认棋盘（不打开任何信息层）上以三种低多边形地标呈现——营帐、篝火、石碑，不看文字也能区分，轮廓不与六种棋子的轮廓语言混淆。据点格另加按档位区分的低饱和底色框（三档灰度明度两两拉开，且与所在地砖拉开），石碑用明显亮于岩石的石色，篝火火焰加大加亮（裁决 S-16）。被控制的据点插控制方旗帜，旗帜同时使用阵营主色与阵营徽记，灰度截图中仍能分辨控制方；争议插交叉旗、无人不插旗，二者外观不同。据点格是可落子格：无棋子时地标放大约 1.5 倍（受固定相机下不遮挡远侧邻格棋子约束，营帐与石碑的高度 / 进深略小于 1.5 倍），地标、旗帜均不越出格界；有棋子占据、本人暂放棋子或打开信息层时，地标缩小并退到格角、旗帜插在棋子身后，棋子完整可见、可被拾取；地标与底色框不带碰撞体。人工检查清单与截图见 `art/sites-v4/`。
- 六种棋子使用稳定且差异明显的轮廓语言：普通子简洁圆润，堡垒子采用塔楼体块，连珠子表现连接关系，倍增子使用放射状轮廓，协同子使用多节点或聚合轮廓，**匠人使用工具或支架状轮廓**（定稿为偏心斜立木柄 + 柄顶横置宽槌头 + 一道斜撑，是六种里唯一不对称的轮廓，灰度缩略图下与塔楼体块的堡垒子分得开）。
- **地形改造的可视表现**：对局中完成的改造必须在默认棋盘上一眼可读——新架的桥与预置桥外观一致，新立的栅栏与预置栅栏外观一致并在两格的公共边上可辨，被烧过的林地呈现为草地（可保留焦痕一类细节，但不得让玩家误判它仍不接收覆盖）。改造落成的那一刻必须有一次**可察觉的反馈**（短促的落成动效或音效提示位），使对手知道地形变了。
- **可改造目标高亮**：玩家暂放一枚匠人时，默认棋盘上标出该落点的全部合法改造目标——候选深水格、候选林地格与 16 条候选边。候选边一律贴边画半透明虚线短条、不做偏移：一枚匠人的 16 条候选边在屏幕上恰好是"以匠人格为中心的十字轮廓套一个小方框"，"这条边属于哪个落点"由图形本身回答；多枚匠人时只显示光标所在（或最后暂放）那一枚的候选，已选目标则全部一直显示。候选 / 已选 / 真设施在明暗、虚实、立卧三条通道上都不同（贴地青白半透明虚线 / 立起的暖黄实心亮板 / 棕木立柱），灰度下也分得开；三种颜色都不是木料色，与真设施一眼分得开。选目标用轮换键而不是点边——拾取原语是格，点边要再做一次消歧，代价不值。人工检查清单与截图见 `art/artisan-v4/`。
- 默认 UI 使用深色半透明面板、克制的金色边框和高对比信息色，保持 PC 策略游戏的信息密度，不采用手游广告式大按钮和夸张弹窗。
- 批次预览使用半透明发光棋子；预计提子使用清楚但克制的虚线或轮廓提示。
- 战术信息层可以临时压低场景饱和度与装饰对比，只突出当前查看的空格归属、气、势力、信物或顺序信息。
- 整体气质应亲和但不幼稚，避免塑料玩具质感、过量粒子特效和手游广告式表现。
- 基准图用于确定材质、色彩、轮廓和信息层级，不代表最终地图布局或固定 UI 尺寸；画面中的 `ROUND 7` 仅表示当前大回合，不表示总回合数。

## 变更记录

| 日期 | 变更 | 内容 |
|---|---|---|
| 2026-09-14 | change `cap-multiplier`（v1.0 → v1.1） | §9.2 / §10.1：倍率指数封顶为 5（生效倍率指数 = min(倍增子数量, 5)，倍率上限 7.59375）。依据 heuristic-ai 36 局实测：无上限的 1.5^n 使倍增子边际收益永远为正、对局不收敛、单串军势指数膨胀远超 §16 "数百" 量级；推翻 `add-territory-power` 裁决 3"倍率不设上限"。 |
| 2026-09-14 | change `round-cap`（与 cap-multiplier 共用 v1.1） | §12.3 / §18.2：新增第 4 类终局条件"达大回合上限"（对局配置，标准局初值 15，0 = 不限；只在大回合结束检查一次，优先级低于条件 1–3；名次规则与条件 2、3 相同）。依据与 cap-multiplier 相同的 heuristic-ai 36 局实测：不收敛率 75–100%，对局只能靠跑局层硬停；规则化后跑局层不再有自己的"达上限"语义。§18.2 明确固定轮数仍不实现，上限是兜底。 |
| 2026-09-15 | change `dominance-victory` | §12.3：新增第 5 类终局条件"势力碾压"（候选制：势力 ≥ 其余参赛玩家之和成为候选，其余参赛玩家各完成一次小回合后仍满足即获胜，跌破即取消；只统计参赛玩家；对局配置"碾压起始大回合"标准局初值 7、0 = 关闭）；终局原因优先级 只剩一人 > 势力碾压 > 棋盘填满 > 整轮 Pass > 达大回合上限。依据：round-cap 之后 200 局回归出局 0 人次，条件 1 在私人征募补给规则下不可达，终局只有整轮 Pass 150 局与达上限 50 局，没有"打赢"这件事；首版判定式"满足即胜"在开局与稀疏局面退化（首位落子者 X ≥ 0 立即成立，实测 4 局全部在第 1 个小回合结束），改为候选制；200 局对比起始第 4 / 6 / 7 大回合后定为第 7 大回合起（起始 4 时第 3 大回合领先者胜率 58%、40% 的局从未发生提子）。 |
| 2026-09-15 | change `growth-pass-1` | §5.4 / §16：基础部署上限按大回合分阶段 3 / 4 / 5（第 1–3 / 4–6 / 7+ 大回合，生成效果快照时按当前大回合读一次），军令信物在其上继续叠加；§16 三段部署目标（3 / 3–5 / 5–8）改为由分阶段基础值天然达成。§9.2 / §10.1：倍率指数封顶 5 → 4（倍率上限 5.0625）。依据：200 局实测第 7+ 大回合部署上限均值 3.36（目标 5–8），4 人基准图平均每局约 1.4 枚军令，军令供给算术上达不到 5–8；倍增子选择率 86.9%（封顶 5 下仍“出现就选”），按 cap-multiplier Open Question 1 的阈值降到 4。 |
| 2026-09-15 | change `multiplier-rebalance` | §9.2 / §10.1：棋串军势公式改为 `⌊基础军势总和 × 1.5^min(倍增子数量, 3)⌋ + 位置加值`——倍率只放大基础军势、不再放大连珠与协同的位置加值，倍率指数封顶 4 → 3（倍率上限 3.375）；§10.1 标准算例无位置加值，结果 20 不变。依据：growth-pass-1 之后 200 局终局盘面势力归因，倍增子占全部势力 76.3%（每颗平均 53.0，普通 1.0 / 堡垒 4.0 / 连珠 2.2 / 协同 8.3），降封顶未降低选择率（86.9% → 84.8% → 85.8%）；同批盘面离线重算比较：仅封顶 3 仍占约 67%，每枚 ×1.25 约 39%（但改 §10.1 核心数与标准算例），加强普通子 74–76%，"封顶 3 + 只放大基础"约 51%，选定后者。 |
| 2026-09-16 | change `denser-map` | §3.3 / §16：4 人基准地图改版为 `siege-4p-base-v2`——可落子格 109 → 85，障碍 12 → 36（占外接 29.8%），出生区每区 15 → 13 格；外接尺寸、出生区数量、信物格总数（14）、D2 对称与距离容差 1 均不变。§16 补记"首次冲突时点由盘面密度决定"，并把"冲突时的盘面占用率"列入常规分析（§17 第 9 条）。依据：catch-up-recruit 之后 200 局实测首次跨出生区冲突平均第 6.52 大回合（目标 4–5），200 局全部在第 4 大回合解禁当回合就落子到区外，排除"不出门"；首次提子发生时盘面平均占可落子格 62%，而 v1 的密度曲线（第 3–7 大回合 26 / 37 / 50 / 60 / 65%）把 62% 推到第 6 大回合。备选"整图缩到 9×9"与"缩短保护期"均否决：前者要重做出生区与信物布局，后者只把密度曲线整体左移一格、不改善对局长度。 |
| 2026-09-16 | change `catch-up-recruit` | §5.3 / §10.2 / §11.1：新增“落后者征募补偿”（对局配置，标准局开启）——小回合开始生成效果快照时读一次公开势力名次，名次 > ⌈参赛人数 ÷ 2⌉ → 征募展示数 +1，名次为当时最大名次且 > 1 → 免费选取数再 +1；并列共享名次，出局 / 弃赛者不计入人数也不获补偿，补偿不累计、不结转、本回合内不回收。排名的作用范围随之扩写为“信息展示 + 下一大回合顺序 + 落后者征募补偿”，仍不转换为累计积分。依据：multiplier-rebalance 之后第 3 大回合领先者胜率 54.0%（§16 目标 ≤ 50%），200 局拆解排除了先手顺序（第 1 大回合随机顺序下 1–4 号位最终名次 2.52 / 2.66 / 2.48 / 2.33）、信物（领先者与其他人同为 2.00 枚）与领地（9.6 vs 9.5），差异集中在保护期征募到的倍增子 1.79 vs 0.96、堡垒子 3.37 vs 2.77、棋串 3.9 vs 5.0 条——即“抽到关键棋子的机会不均”，故只在供给侧补偿落后者，不改先手值公式、不惩罚领先者。 |
| 2026-09-16 | change `ai-safety-weight` | §16：`EvaluationWeights.Default.Safety` 由 20 改为校准值 5；§16 补记“实测值必须标注 AI 权重口径”与“被围杀风险维度与收敛性非单调”，并补记领先者胜率显著低于 25% 基线同样算偏离。依据：20 是 heuristic-ai 阶段在 v1 空旷地图上凭 5 局与 2 颗种子试出的初值，其 doc 注释自身标注为“阶段 B 的首个校准项”，校准从未执行，而此后各轮基线均建立其上。denser-map 之后 v2 上按 3 / 5 / 7 / 8 / 10 / 20 / 30 / 40 / 60 九档各跑 200 局（种子 1–200）：不收敛率 10.0 / 9.5 / 3.5 / 13.0 / 31.5 / 27.5 / 34.0 / 42.0 / 57.5%，曲线非单调且调高更糟，推翻旧注释“取 40 一半收敛”的方向。取 5 不取不收敛率最低的 7：7 的第 3 大回合领先者胜率 61.5%，与旧值 20 的 64.5% 没有差别，滚雪球原样保留；5 为 51.0%，而领先者胜率是 §16 目标项、不收敛率只是“越低越好”。取 5 不取 3：3 的领先者胜率跌到 16.0%，低于 25% 的随机基线。v2 上 Safety=5 同时把首次冲突由第 5.46 压到 4.68 大回合、整局无提子局由 60 / 200 降到 3 / 200。 |
| 2026-09-16 | change `merge-board-layer` | §14.2：领地层与气层合并为**盘面层**的两种读法（归属／棋串），战术信息层由五种变为四种；键位顺延为 `1` 盘面、`2` 势力、`3` 信物、`4` 顺序，读法切换用独立键 `Tab`。依据：二者点亮的空格集合恒等——覆盖 = 棋子的四邻接相邻格，气 = 棋串的空邻格，所以"被某方覆盖的空格"就是"某条棋串的气"；实测 4 人盘面下两集合均为 21 格、双向差集为空。归属读法另画占据格与中立格、棋串读法另给棋串分组与危险度，这两部分不重复，故合并为一层两读法而非删去其一。读法切换不绑层键：按住显示是默认模式，按住期间无法再按同一键。 |
| 2026-09-16 | change `terrain-model`（v1.1 → v1.2） | §2 / §3.1–3.3 / §6.1 / §7.1 / §7.3 / §9.2 / §14.2 / §20：地形进入规则——格属性（高度 0/1/2、地表 草地/土路/林地/深水、障碍、预置桥）+ 边属性（栅栏），在几何四邻之上导出两套各只有一处实现的关系：气边（连接 / 棋串 / 气 / 围杀：两格可落子、高度差 ≤ 1、无栅栏）与覆盖关系（覆盖 / 归属 / 唯一覆盖 / 信物发现：林地不接收、跨崖只能居高临下、可隔一格宽深水、栅栏不挡）。§2 补术语"格属性 / 边属性""气边""覆盖关系"，气 / 棋串 / 覆盖改沿新关系定义；§3.1 收入 `terrain` 规格全部标准算例（`F6`/`F7` 崖壁、`F6`–`G6` 栅栏、`F6`→`G6`→`H6` 隔岸、林地、居高临下 / 仰视）；§3.2 4 人可落子区间 80–95 → 95–110，取消"障碍占 25%–35%"，对称改 C4 旋转，距离与口袋沿气边，新增桥 / 栅栏合法性与出生区连通校验；§3.3 4 人基准图改版 `siege-4p-base-v3`（13×13、可落子 105、四家 13 格 h=2 高台 + 2 格缓坡、中央环河岛 21 格、四桥即咽喉、信物 13 = 出生区 8 + 桥头 4 + 岛心 1、沿气边距离 5 / 7 / 4）；§6.1 落子合法性补"非未架桥深水"；§7.3 信物揭示补"或被占据"；§9.2 连珠"连续"改沿气边；§14.2 盘面层两读法由"恒等"改为"平地恒等 + 差集可由地形解释"；§20 地形从装饰升级为可判读的规则要素，坐标标注列数随棋盘宽度。依据：项目负责人 2026-09-16 的 31 条裁决（模型底座 D1–D7 / F1 / E4 / E5，地图 D18 / D19 / E2，实施 D20 / D21 / E3 / 默认 5 等；本轮只做地形轮①，②据点计分与③匠人另开 change）与段 A–C 期间追加的 A-6（连珠线沿气边）、B-1（公共信物 5 = 桥头 4 + 岛心 1）、B-2（中央入口与岛心高档信物同格 `G7`）、C-8（崖壁阈值具名常量 `TerrainData.CliffDrop = 2`，Core 与表现层共用）。**计分不动，§16 数值目标不动**（等本轮 200 局回归数据再议）；13×13 + 领地计分下对局变长是预期结果，本轮不修、不调参。 |
| 2026-09-17 | change `scoring-sites`（v1.2 → v1.3） | §1 / §2 / §3.2 / §3.3 / §6.3 / §7 / §9.2 / §10.1 / §12.3 / §13.1 / §14.2 / §16 / §17 / §20：去围棋化第二轮——**空格领地退出计分**（原"独占空格每格 1 分、孤立普通子最多 5 点势力"作废），`总势力 = 控制中的据点分 + 棋串军势`。§2 补术语"据点""据点分""高地加值"，势力值改定义；§3.2 人数预算加据点数（2 / 3 / 4 人 6–8 / 9–11 / 10–14），静态校验加据点须在可落子格、标注档位、不与信物重合，距离均衡目标加最近篝火与最近石碑；§3.3 基准图升为 `siege-4p-base-v4`（地形与 v3 逐格相同，Id 升号不沿用，裁决 S-9），加 12 个据点：营帐 `B3 / L2 / M11 / C12`、篝火 `J2 / M9 / E12 / B5`、石碑 `H5 / J8 / F9 / E6`（裁决 S-1 / S-2 / S-13），距离均衡目标加最近篝火 6 / 最近石碑 8，文本图换为 `map` 子命令真实输出，出生区对人显示统一为 1–4（裁决 S-14）；§6.3 第 5 步加据点控制；§7.2 空格归属只作控制判定与展示；新增 §7.4 据点控制（占据 > 唯一覆盖 > 争议 > 无人，与信物同口径复用覆盖数据；分值是对局配置、正整数且单调不减；弃赛者遗留棋子照常控制，据点分计入其盘面势力）；§10.1 公式与算例重写（孤立棋子 1 点、据点与棋串相加 77、独占空格不计分 27），新增高地压制加值（覆盖目标上有高度严格更低的敌子 → +1，每枚至多 1，进位置加值、不被倍率放大，只走覆盖关系，裁决 D13 / E1 / S-4）；§12.3 并列链"独占空格数"→"控制中的据点数量"；§13.1 公开据点与控制状态；§14.2 势力层改看据点分与高地拆分，不加新层（裁决 S-5）；§16 `Safety` 5 → 27、新增"据点分占比 25%–45%""整局无提子局 ≤ 5%"两条目标并给出确认 200 局实测表（裁决 S-10 / S-15）；§17 补据点记录项，重点分析改为编号列表并补齐第 8 项（碾压，`dominance-victory` 已实现但本节漏写）、第 9 项（冲突占用率，`denser-map` 已实现、§16 已引用但本节漏写）与新增第 10 项据点分析；§20 新增据点地标（裁决 S-16）。依据：同一地形旧计分 200 局回归（`sim-out/terrain-v3/`，已作废）整局无提子 14%、第 3 大回合领先者胜率 17%、平均第 10.57 大回合结束，空格均质计分让"铺满盘面"成为最优解；据点分量三档与 Safety 九档加 22 / 25 / 27 加密档各 200 局扫档，负责人拍板分值保持 5 / 15 / 45、`Safety` 取 27（领先者胜率 25.5%、不收敛 6.5%、整局无提子 0、平均结束第 7.78 大回合；据点分占比 22.1% 低于新目标下限，知情接受）。 |
| 2026-09-18 | change `artisan-terrain-edit`（v1.3 → v1.4） | §2 / §3.1 / **新增 §3.4** / §6.1–6.3 / §9.1 / §9.2 / §10.1 / §13.1 / §14.1 / §14.2 / §16 / §17 / §18.1 / §20：去围棋化第三轮——**地形不再是对局内的不变量**。新增第六种棋子「匠人」（基础军势 1、征募权重进对局配置）与三种地形改造（搭桥 / 立栅 / 烧林），落子即改造、占匠人自己那一枚额度、不新增阶段、可选；格目标为匠人落点的几何四邻，边目标至少一端是四邻格（16 条候选，裁决 T-11）；改造在预演第 4 步与正式结算第 3 步应用，**先于提子**（立栅可直接提子、堵死自己算自杀手）；批次内不链式（一律按批次开始前的地形判定）；改造不可逆、不属于任何玩家、全公开且不记改造者；盘面同形表示纳入桥集合、栅栏集合与地表。§2 补术语「匠人」「地形改造」并把「对局中不变」改为「地表与设施可被改造单向改写」；§3.1 同步；§3.4 为新增的改造一节（动作集、目标口径、时机、不链式、不可逆与 13 条标准算例）；§6.1 预演六步 → 七步、§6.3 正式结算六步 → 七步并补原子性；§6.2 同形表示纳入设施；§9.1 棋池加匠人一档并写明它是对局配置；§9.2 标题「五种」→「六种」、棋子表加匠人行；§10.1 算例加「匠人按 1 计」；§13.1 公开信息加当前地形并写明不标记改造者；§14.1 预演补改造目标与可改造目标；§14.2 棋串读法区分栅栏侧、暂放匠人时在默认棋盘标出可改造目标；§16 `Safety` 27 → 35、按确认 200 局重定全部实测并新增两条已知偏离；§17 记录项补匠人权重与改造事件、重点分析新增第 11 项地形改造；§18.1「五种棋子」→「六种棋子」；§20 匠人轮廓定稿、改造的可视表现与 16 条候选边的高亮读法。依据：匠人征募权重三档（5 / 10 / 18，Safety 27）与 Safety 六档（25 / 27 / 30 / 35 / 40 / 45，匠人权重 5）各 200 局扫档（种子 1–200），负责人 2026-09-18 拍板 `EvaluationWeights.Default.Safety` 27 → 35、匠人征募权重取 5（裁决 T-13），并接受「烧林是冷门动作」、本轮不加林地不改地图（裁决 T-14）；确认跑 `sim-out/artisan-final/` 200 局与扫档档位逐项相同——领先者胜率 24.0%、终局局平均第 7.56 大回合、首次冲突第 4.01 大回合、整局无提子 0 局、据点分占比 31.5%（首次进入 25%–45% 目标带）、每批次提子 0.52（上一版 0.29）。已知偏离两条：不收敛率 26.0%（上一版 6.5%，成因是栅栏使盘面更难填满，终局条件属本轮 Non-goals，留待后续）、扫档 1800 局烧林仅 1 次（裁决 T-14 接受为冷门动作）。 |
