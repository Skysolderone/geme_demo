# 任务：board-coordinates

## 1. 纯逻辑部分先测

- [ ] 1.1 测试：11 列棋盘的列标注序列为 `A B C D E F G H J K L`（无 `I`），且由 `Coord` 的既有记法转换推出。验证：变异——让标注改用连续字母（含 `I`），测试变红。
- [ ] 1.2 测试：行标注自下而上为 `1`–`11`，与 `Coord` 的行号一致（不是自上而下）。验证：变异——把行序反转，测试变红。
- [ ] 1.3 守门：`src/godot/scripts/` 下 MUST NOT 出现第二份跳过 `I` 的字母表。这个工程不在解决方案里，对常规测试隐身，必须用源码扫描守门（testing.md 已记：扫描正则不要以 `\b` 开头匹配复合标识符）。验证：故意在视图层加一份字母表，扫描变红。

## 2. 实现

- [ ] 2.1 `BoardGeometry` 暴露取棋盘四边标注锚点的方法（格心 + 固定边距），保持它作为 `Coord` ↔ 3D 的唯一映射。
- [ ] 2.2 `BoardView` 创建四边的标注节点，文本与位置全部取自 2.1，不自行推算。
- [ ] 2.3 标注设为始终面向相机，位置锚在棋盘四边。
- [ ] 2.4 调整标注的大小与边距，确保不覆盖任何格子。

## 3. 人工验证（必须产出截图）

- [ ] 3.1 正面截图：四边标注齐全，列字母在上下、行数字在左右，`I` 不出现。
- [ ] 3.2 相机旋转 180° 截图：标注仍正对玩家，无镜像、无倒置。
- [ ] 3.3 边缘格有棋子时的截图：标注不遮挡该格的落子判读。
- [ ] 3.4 对照截图与一局真实日志：任取三个格子，界面标注与日志坐标一致。
- [ ] 3.5 把上述检查项写成清单留档，格式参考 `art/style-exploration/README.md`。

## 4. 不做

- 不做坐标点击定位、跳转、搜索。
- 不改相机控制与棋盘朝向。
- 不适配 2 人 / 3 人地图。
