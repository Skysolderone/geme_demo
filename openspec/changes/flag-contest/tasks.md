## 1. 选区实现（Siege.Core/Match）

- [ ] 1.1 先写测试：`match-setup`「原型插旗替代路径」四个新 Scenario 与改写的"区数等于人数时保持现状"。验证：新增的先红。
- [ ] 1.2 实现 D1：`GameSeed.FlagRisk` 新子流；`PrototypeZoneAssignment.Assign` 增加冒险概率参数（缺省由调用方传入配置值）。验证：1.1 全绿；既有「原型插旗替代路径」Scenario 全绿；变异（冒险抽签改用 `ZonePick` 子流时"不扰动其他随机"应红；p = 0 时仍推进原有分支之外状态导致结果变化应红；"之前没有旗也冒险"应红）。
- [ ] 1.3 依赖走法的黄金哈希 / 期望：写死 p = 0 保持不变；若仍分叉，探针归因。

## 2. 配置、记录与分析

- [ ] 2.1 D2：`MatchOptions` / 批量配置字段、日志首部、`--flag-risk` CLI（严格 CLI：非法值报错）；旧日志缺字段按 0。验证：往返测试；旧日志回放保持绿。
- [ ] 2.2 D3：分析器"同区对局数 / 占比、同区玩家平均名次与胜率"。验证：单元测试（构造日志）。

## 3. 验证与收尾

- [ ] 3.1 v5、种子 1–20、4 名 Standard、缺省 p = 15 冒烟：同区对局数、截断、终局原因、结束大回合、同区玩家名次；数据 `sim-out/flag-contest/smoke20/`。只报告（负责人：不再跑 200 局）。
- [ ] 3.2 设计文档 §4.1 补原型路径冒险概率，版本 v1.11 → v1.12，变更记录加一行。
- [ ] 3.3 全量回归：两处 `dotnet build` 零警告；`dotnet test -c Release` 全绿；`openspec validate flag-contest --strict` 通过（不跑 200 局规模慢测试）。
