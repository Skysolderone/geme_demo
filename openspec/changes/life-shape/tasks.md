## 0. 前置

- [x] 0.1 `restore-go-core-rules` 已归档。验证：`openspec list` 不再列出它；`openspec/specs/information-visibility`「始终公开的信息」第 3 条为空格归属。
- [x] 0.2 核对本 change 的 MODIFIED 增量与归档后主规范逐字一致（除活形相关改动外）。验证：`openspec validate life-shape --strict` 通过。

## 1. 段 A——活形分析（Siege.Core/Board，纯计算）

- [x] 1.1 先写测试：`life-shape`「空区与封闭眼空间」八个 Scenario（单格眼；贴敌子不封闭；角上 `A1`；崖壁 / 栅栏算墙；深水算墙；13 格超上限；林地可为眼；多串共享）。用文本盘面夹具搭局面。验证：先红。
- [x] 1.2 实现空区划分与封闭判定：只调用 `GameBoard.LibertyNeighbors` 与既有棋串计算；集合按坐标序遍历。验证：1.1 全绿；守门（源码扫描）——新文件里不出现几何四邻偏移量、不出现第二处邻接遍历；变异（把"无气边即墙"改成把崖上敌子算作邻格应红）。
- [x] 1.3 先写测试再实现：「活形三态」十二个 Scenario（两单格眼；一单格眼；直二；方四；直四；被栅栏隔开的 2×2；三格；单格 + 三格；五格；六格；随盘面重算；不入存档）。方四判定按气边图成环。验证：全绿；变异（按几何 2×2 判方四时"被栅栏隔开的 2×2"应红；眼值不相加时"单格 + 三格"应红）。
- [x] 1.4 「禁入格」四个 Scenario 与「活形查询」两个 Scenario：按玩家给禁入格集合；弃赛 / 出局者的活形照常产生禁入；预演副本可查、正式盘面不受影响；重复查询逐项相同。验证：全绿；确定性守门——不直接遍历 `HashSet` / `Dictionary`。
- [x] 1.5 性能基线：在 `siege-4p-base-v5` 与 `siege-frontier-v2` 的中盘局面上各测一次全盘活形分析耗时，记入 implement 记录。验证：单次分析中位数不超过一次全盘棋串计算的 3 倍（宽松上界，避免机器抖动假红）。

## 2. 段 B——预演与合法落子范围（Rules / Match）

- [x] 2.1 先写测试：`capture-resolution`「以整批最终状态判定合法性」新增八个 Scenario（同时填两眼被拦；围死活形被拒——外气填满后一批匠人立栅隔断两眼；未定棋串的眼可以进；立栅切开活形；立栅隔开眼与棋子；搭桥漏眼；不影响活形的改造合法；所有者可以拆自己的眼），既有六个 Scenario 保持绿。验证：新增的先红。
- [x] 2.2 预演第 1 步加活棋禁入、第 5 步后加破坏活形（只复查批次开始前已活的非己方棋串，按其棋子在副本上的所在棋串重查）；新增两个原因码及定位信息。验证：2.1 全绿；`batch-deployment`「非法批次必须给出可定位的原因」四个 Scenario 全绿；变异（把第 6 步挪到提子之前应红——用"提子后才恢复活形"的夹具；对所有者也施加禁入应红）。
- [x] 2.3 守门测试："已确定活形棋串在任何非所有者的合法批次后仍为已确定活形"——随机局面性质测试（固定种子集、每种子若干批次，含匠人改造），任何反例即红；"仍为已确定活形"包含"没有被提走"。验证：种子 1–200 无反例；人为去掉第 6 步后该测试应红（变异验证）。
- [x] 2.4 合法落子范围扣除禁入格：`turn-sequence`「合法落子范围的对外契约」三个 Scenario（含共享出生区）。验证：全绿；守门——禁入只在契约一处扣除，预演与契约共用同一个禁入格查询。
- [x] 2.5 公开视图暴露活形状态、眼空间与各玩家禁入格：`information-visibility`「活形状态公开」。验证：全绿；`MatchPublicView` 中不含任何隐藏信息的既有守门保持绿。
- [x] 2.6 AI 适配（最小改动）：AI 的候选落点来自合法落子范围，确认不会再生成落进禁入格的候选；预演返回两类新原因时按既有"被拒绝"路径处理。验证：4 AI 对局 50 局无异常；日志中 AI 的"活棋禁入"拒绝次数为 0（候选已被范围过滤），"破坏活形"拒绝次数记入 implement 记录。

## 3. 段 C——预演提示与表现层

- [x] 3.1 `BatchPreview` 两类新原因的文案与高亮数据：`batch-preview`「非法批次必须说明原因并高亮」五个 Scenario。验证：全绿。
- [x] 3.2 `Siege.Presentation`：棋串读法加"已活"标记；默认棋盘禁入格视图数据（区别于地形不可落子与范围外）；指向禁入格给出原因与所有者。测试：`tactical-layers`「活形与禁入格的标示」三个 Scenario。验证：全绿；守门——表现层不调用活形分析，只读公开视图。
- [x] 3.3 终端版：文本盘面用独立符号标禁入格与已活棋串，图例同步。验证：脚本化终端测试；人工看一局。
- [x] 3.4 Godot：已活棋串标记（形状 + 颜色，不只靠颜色）、禁入格标示、悬停提示。验证：`--auto-demo`、`--pick-check`、`--screenshot` 在 v5 与 `siege-frontier-v2` 上退出码 0；截图放 `sim-out/` 供负责人过目。

## 4. 段 D——遥测、分析与收尾

- [x] 4.1 日志：活形确立 / 失去事件、两类拒绝尝试。测试：`match-telemetry`「活形记录与统计」前三个 Scenario。验证：全绿；旧日志回放测试保持绿。
- [x] 4.2 分析与报告：首次活形确立大回合、终局活形数 / 眼空间格数、禁入格占比、有活形玩家胜率、两类拒绝次数；他人致失活单列为规则缺陷。测试：后两个 Scenario。验证：全绿。
- [x] 4.3 设计文档同步：§6.1 合法性预演加入活棋禁入与破坏活形两步；新增活形判定小节（眼空间、三态表、`EYE_SPACE_MAX`、无气边即墙）；§12 弃赛者遗留活棋的说明指向新小节；文末变更记录加一行。验证：人工核对与规范一致。
- [x] 4.4 200 局基线：4 人 v5 图、种子 1–200、同强度 AI（`restore-go-core-rules` 只跑了 20 局冒烟，完整基线由本项首次给出），记录截断率、终局原因分布、平均结束大回合，并与 ① 的 20 局冒烟并列。验证：报告放 `sim-out/`，关键数字与 ① 的基线并列写入 implement 记录（仍为 AI 未校准口径）。
- [x] 4.5 `.trellis/spec/core/` 补一条约定：活形 / 禁入只有一处实现，预演、契约、公开视图、AI 共用同一查询。验证：`boundaries.md` 的单一实现清单更新。
- [x] 4.6 全量回归：`dotnet build` 零警告；`dotnet test -c Release` 全绿；`openspec validate life-shape --strict` 通过。
