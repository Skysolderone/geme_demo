## 1. 据点数据与地图（段 A1，段末交文本图给负责人审布点，点头后才派 A2）

- [ ] 1.1 `MapData` 新增据点集合（坐标 → 档位：营帐 / 篝火 / 石碑）；`MapFile` 读写 `Sites`，缺字段按无据点读入。验证：单元测试——往返逐字段相等；缺字段旧文件读入据点为空。
- [ ] 1.2 `MapValidator` 规则 8：据点数落在人数区间（4 人 10–14）、据点在可落子格、不与信物格重合、档位必填；规则 1 距离均衡目标加最近篝火与最近石碑。算例（`map-definition` 场景）：石碑标在 `G7` 被拒并指出坐标；篝火标在未架桥深水被拒；16 个据点被拒并报告越界；A 到最近石碑 6、B 为 9 被拒并指出"石碑"。验证：单元测试逐条。
- [ ] 1.3 `MapSymmetry` 旋转比对纳入据点（位置与档位）。验证：单元测试——只改一个据点档位的图被判不对称；变异：比对漏掉据点应红。
- [ ] 1.4 v4 布点（地形沿用 v3，Id 升为 `siege-4p-base-v4`，`RunConfig` 默认地图切到 v4，v3 JSON 保留并加注释说明已被取代；既有引用 v3 Id 的测试按"地形测试仍用 v3 地形、地图身份测试改 v4"逐条处理并列入 `implement.md`）：营帐每区 1（h=2 非信物）；篝火每家河外低地 1（从本区出发不经桥沿气边可达的 h=0 格，且与另一出生区 h=2 格几何相邻）；石碑岛上 4（只能经桥到达的中央 h=0 区域，非林地）；三条 C4 轨道；不与信物重合；地形不变。验证：校验器全项通过；测试断言 `map-definition`「据点布点」与「篝火可被邻家居高覆盖」两个场景（后者用真实 `CoverageTargets` 断言，不手算）；导出 `maps/siege-4p-base-v4.json`；断言篝火轨道为 `J2 / M9 / E12 / B5` 或给出不同的理由；`map` 子命令文本图标出三档据点；文本图写入 `implement.md` 供负责人审。

## 2. 计分（段 A2）

- [ ] 2.1 据点控制唯一实现：读 `GameBoard` 占用与 `CoverageMap` 唯一覆盖，产出每个据点的状态（占据 / 唯一覆盖 / 争议 / 无人）与控制者。算例（`site-control` 场景）：占据优先于两名敌方覆盖；空格只被 B 覆盖 → B；A、C 同时覆盖 → 争议；h=0 篝火被主人同高覆盖 + 邻家 h=2 相邻棋子 → 争议；h=2 营帐旁 h=0 敌子 → 不影响。验证：单元测试逐条；守门——据点控制实现内不得出现 `Neighbors(` / `CoverageTargets(` / `HeightAt(` 调用（只读 `CoverageMap`），做变异验证。
- [ ] 2.2 `MatchOptions` 新增据点分值（营帐 / 篝火 / 石碑，标准局 5 / 15 / 45），校验正整数且单调不减。算例：10 / 30 / 90 下控制 1 个篝火得 30；5 / 0 / 45 与 20 / 15 / 45 被拒并指出档位。验证：单元测试。
- [ ] 2.3 高地加值唯一实现：对每枚棋子 s，`CoverageTargets(s)` 中存在"敌方棋子且高度严格低于 s" → +1，每枚至多 1；按棋串求和进位置加值。算例（`piece-effects`「高地压制加值」7 个场景）：h=2 `F7` 对 h=0 `F6` 敌子 +1、反向 0；h=1 对 h=0 缓坡 +1；同高 0；敌子在林地 0；隔一格深水 +1；三个低处敌子仍 1；己方低处棋子 0。验证：单元测试逐条；变异：去掉"严格低于"、去掉"每枚至多 1"、改走几何邻居，各应红。
- [ ] 2.4 `PowerCalculator` / `PowerSnapshot`：总势力 = 据点分 + 军势；移除 `TerritoryScore`；明细含据点分总计与控制据点清单（坐标、档位、分值、控制方式）；`GroupPower` 位置加值拆分连珠 / 协同 / 高地。算例（`power-score` 场景）：控制营帐 + 石碑、军势 20 与 7 → 77；12 个独占空格、无据点、军势 27 → 27；孤立普通子 → 1；h=2 棋串普通×2 倍增×2、高地 3 → `⌊4×2.25⌋+3 = 12`；明细三部分之和等于位置加值、据点清单之和等于据点分。验证：单元测试逐条。
- [ ] 2.5 弃赛者：遗留棋子照常控制据点，据点分计入其盘面势力、不占名次。算例：D 占据营帐 → D 势力含 5、参赛者不得分；D 与 A 同时覆盖空据点 → 争议。验证：单元测试（`elimination-endgame`、`site-control` 场景）。
- [ ] 2.6 `FinalStandings` 并列链改为 势力 → 信物数 → 据点数 → 棋子数；存档字段随之改，旧存档据点数按 0 回填。算例：势力 60、信物 2 相同，据点 3 与 1 → 前者名次高。验证：`elimination-endgame`「终局名次与并列判定」场景测试；存档往返测试。
- [ ] 2.7 改写既有测试：所有以独占空格计分、孤立棋子 5 点势力、§10.1 含领地算例为期望的用例按新公式改写期望，逐条列入 `implement.md`（旧期望 → 新期望 → 依据的规格场景）；几何算例改坐标重构不改期望。验证：全量测试绿。

## 3. 跑局与遥测（段 A2）

- [ ] 3.1 `RunConfig` 支持据点分值（未配置取标准局值），与各玩家权重一并写入批次 `config.json`；命令行或配置文件均可指定。算例：3 / 8 / 24 + 全员 Safety 7 → `config.json` 中两项如实写出。验证：单元测试 + 跑 1 局人工核对 `config.json`。
- [ ] 3.2 日志：每名玩家据点分（替代 `Territory`）；据点控制变化事件（大回合、小回合、坐标、旧状态、新状态）；位置加值来源含高地。验证：`match-telemetry`「据点控制变化可查」场景测试。
- [ ] 3.3 分析 §17 第 10 项：三档据点的控制小回合占比、争议小回合占比、首次被控制平均大回合、控制者胜率；篝火主人控制占比与主人首次控制平均大回合；石碑由相邻桥头那家（桥头信物所在河外低地的主人）控制的小回合占比；终局据点分占参赛玩家总势力比例；高地加值占全部位置加值比例；缺据点字段的旧日志整局排除并计数。算例：终局四人总势力 600、据点分 180 → 30%。验证：单元测试手算样本；跑 20 局人工看报告。

## 4. 界面（段 B）

- [ ] 4.1 `Siege.Presentation`：公开视图含据点位置、档位、分值与控制状态；势力层据点项（档位、分值、状态、控制者或覆盖方）替代每格领地贡献；棋串分数拆出高地。验证：`tactical-layers`「势力层显示据点控制」「势力层显示高地加值」与 `information-visibility`「据点控制公开」场景测试；守门——势力层视图模型不再含领地贡献字段。
- [ ] 4.2 Godot：营帐 / 篝火 / 石碑三种低多边形地标；控制方旗帜（主色 + 徽记）；争议与无人外观不同；有棋子占据时地标退到旁侧或缩小，棋子可见可拾取。验证：`--build-solutions`、`--auto-demo`、`--auto-demo --pick-check` 退出码 0；截图 `art/sites-v4/`（开局、中局有争议与控制、终局），`README.md` 写人工清单（三档可辨、灰度下控制方可辨、争议 / 无人可辨、占据时棋子可见）。
- [ ] 4.3 Godot 源码级守门：`src/godot/` 不自算据点控制或高地加值（token 表加据点控制与高地加值的入口名）。验证：守门测试 + 变异（在 `BoardView` 调一次据点控制计算应红）。

## 5. 校准（段 C，主会话执行）

- [ ] 5.1 据点分量三档（3 / 8 / 24、5 / 15 / 45、10 / 30 / 90）× Safety 5 × 200 局（种子 1–200），输出到 `sim-out/sites-scale-{1-5,1-3,1-2}/`；每批核对 `config.json` 实际生效值。验证：对比表（D-H 第 3 步所列指标，含终局原因分布与碾压成立大回合），按 Open Question 3 的优先级给出推荐档。
- [ ] 5.2 在推荐档（或负责人指定档）下 Safety 九档（3 / 5 / 7 / 8 / 10 / 20 / 30 / 40 / 60）× 200 局，输出到 `sim-out/sites-safety<N>/`。验证：对比表 + 推荐值。
- [ ] 5.3 负责人拍板后：改 `MatchOptions` 默认据点分值与 `EvaluationWeights.Default.Safety`，钉住默认值的测试随之改；确认跑 200 局（`sim-out/sites-final/`）与拍板那一档的数据一致（比例项 ±3 个百分点、回合数 ±0.3）。验证：全量测试绿 + 对照表。

## 6. 文档与规范（段 D）

- [ ] 6.1 设计文档 v1.2 → v1.3：§2 术语补据点与高地加值；§3.3 v4 地图（v3 地形 + 据点布点）与文本图；§7.2 空格归属去计分语义；新增 §7.4 据点控制；§6.3 结算顺序第 5 步加"据点控制"；§10.1 公式与标准算例（含"孤立棋子 1 点"、据点与高地算例）；§12.3 并列链；§13.1 公开信息；§14.2 势力层；§16 按拍板数据重定并加 Open Question 1 的两条目标；§17 第 10 项；§20 据点地标；变更记录加一行。§16 只在 5.3 之后改。验证：人工检查；规则类算例与规格场景逐条对得上。
- [ ] 6.2 `.trellis/spec/core/boundaries.md` 单一实现表补"据点控制""高地加值"两行；`testing.md` 补本轮扫档与口径核对的教训（如有）。验证：人工检查。
- [ ] 6.4 出生区编号统一（S-14）：校验器报文、对称检查、遥测报告与文本图 / 棋盘统一为对玩家显示 1–4；内部索引可保持 0 起，只改显示层；改写的既有断言逐条列入 `implement.md`。验证：全量测试绿；`map` 与校验报错人工核对。
- [ ] 6.3 变异验证汇总表：段 A–D 全部变异逐条（编号、变异、红掉的测试 / 命令、还原确认）。验证：`implement.md` 表格齐全。
