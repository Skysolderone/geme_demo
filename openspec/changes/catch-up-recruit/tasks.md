## 1. 对局配置

- [ ] 1.1 `MatchOptions` 新增落后者征募补偿开关（标准局开启）；开局固定、进入公开视图、入存档、旧存档回填开启并可查知。验证：单元测试（往返用非回填值"关闭"）。
- [ ] 1.2 测其它规则的既有测试在建局时显式关闭补偿，方法体不改；夹具默认保持开启。改动点逐个列入提交信息。验证：全量测试通过。

## 2. 补偿判定

- [ ] 2.1 效果快照生成时读取公开势力名次（参赛玩家、并列共享），名次 > ⌈参赛人数÷2⌉ → 展示 +1；名次为当前最大名次且 > 1 → 选取 +1。算例：A=60、B=45、C=30、D=20 → C 展示 6 选取 3；D 展示 6 选取 4；B 无补偿。验证：单元测试。
- [ ] 2.2 并列与边界。算例：A=60、B=45、C=20、D=20 → C、D 均展示 +1 选取 +1；全员 0 → 无补偿；2 人局 A=30、B=10 → B 展示 +1 选取 +1。验证：单元测试。
- [ ] 2.3 出局与弃赛者不计入参赛人数、不获补偿。算例：D 弃赛、A=60、B=45、C=30 → C 展示 +1 选取 +1。验证：单元测试。
- [ ] 2.4 与信物加成相加。算例：最后一名 + 1 枚探勘 → 展示 7、选取 4。验证：单元测试。
- [ ] 2.5 只在快照生成时读一次，本小回合内名次上升不回收；下一小回合重新判定。验证：单元测试。
- [ ] 2.6 关闭开关时任何名次都无补偿。验证：单元测试。

## 3. 来源拆分与界面

- [ ] 3.1 结构参数来源拆分新增「落后补偿」；手牌面板显示补偿来源。算例：D 展示 6 标注 1 点来自落后补偿。验证：单元测试。
- [ ] 3.2 终端 `play` 与 Godot 结构参数显示只读来源拆分，不另算。验证：Godot 编译通过、终端测试断言文案。

## 4. 遥测与文档

- [ ] 4.1 Sim 日志首部记开关；报告新增落后补偿段落（获补偿小回合占比、各档触发次数、获补偿玩家最终名次分布），口径测试含被排除样本。验证：单元测试。
- [ ] 4.2 设计文档 §5.3 增补补偿规则、§10.2 / §11 排名作用范围同步、文末变更记录加一行。验证：人工检查。

## 5. 变异验证与回归

- [ ] 5.1 变异验证并记录：阈值改 `≥`、并列不共享名次、弃赛者计入人数、快照内重读名次、最后一名条件去掉 `> 1`、来源拆分把补偿算进信物、开关不入存档。
- [ ] 5.2 200 局回归：第 3 大回合领先者胜率、Pass 率、获补偿小回合占比、平均结束大回合、终局原因分布、各棋子势力占比，与 multiplier-rebalance 之后对照。验证：跑局统计如实记录。
