# 任务：ai-safety-weight

## 1. 守门测试先行

- [ ] 1.1 新增测试：`EvaluationWeights.Default` 的七个维度逐一钉住当前校准值（PowerGain 10、EnemyLoss 8、Relic 6、Safety 5、Growth 4、Initiative 20、Supply 2）。验证：把任一维度改成别的值，该测试变红；逐维各做一次变异，记录红的条数。
- [ ] 1.2 新增测试：默认权重表被用于未显式配置权重的跑局。验证：把 `PlayerAiConfig.Weights` 留空时 AI 使用 `Default`；变异——让留空时改用其他权重表，测试变红。

## 2. 改默认值

- [ ] 2.1 `EvaluationWeights.Default` 的 `Safety` 由 20 改为 5。
- [ ] 2.2 重写该字段的 doc 注释：删去 v1 上 5 局 / 2 颗种子的旧试探记录与"阶段 B 的首个校准项"标记，替换为 v2 九档各 200 局的扫档结论，并写明"调高更不收敛"这一反直觉方向。注释 MUST 指明数据所在目录 `sim-out/safety<N>/`。
- [ ] 2.3 全量 `dotnet test` 绿；检查是否有测试依赖旧的 Safety=20 行为（例如钉住某个具体局面的 AI 选点）。

## 3. 回归

- [ ] 3.1 200 局回归（种子 1–200，v2 图，与 `sim-out/denser-map-200` 同口径）。验证：不收敛率、首次冲突大回合、整局无冲突局数、终局局长度、第 3 大回合领先者胜率五项与 design.md §2 的 `Safety=5` 行一致（容差：比例项 ±3 个百分点，回合数 ±0.3）。
- [ ] 3.2 如实记录 §16 各目标项的达标情况，偏离超过 5% 的立即报告，不得自行调参掩盖。

## 4. 文档

- [ ] 4.1 设计文档 §16 的实测对照表更新为 `Safety=5` 下的 v2 数据，并对此前的数据注明"权重口径 Safety=20"。
- [ ] 4.2 变更记录追加一行。

## 5. 不做

- 不调其余六维默认权重。
- 不改 `GroupSafety.Score` 算式与 `DangerLiberties`。
- 不重跑 `sim-out/` 下的历史批次。
- 不处理落后者无处可下的问题。
