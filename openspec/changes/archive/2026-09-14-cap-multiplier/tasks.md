## 1. 计分实现

- [ ] 1.1 在全项目唯一的倍率实现中把指数改为 `min(倍增子数量, 5)`，封顶常量只定义一处；调用方继续传原始数量。验证：单元测试——算例（设计文档 §10.1）普通子×3 + 堡垒子×1 + 倍增子×2 → 20 不变；普通子×4 + 倍增子×5 → `⌊9 × 243/32⌋ = 68`；普通子×4 + 倍增子×6 → `⌊10 × 243/32⌋ = 75`。
- [ ] 1.2 倍率的显示字符串按生效指数生成，最大 `7.59375`。验证：单元测试——8 枚倍增子的显示值为 `7.59375`。
- [ ] 1.3 势力明细每条棋串新增"生效倍率指数"字段，保留"倍增子数量"原字段语义。验证：单元测试——9 枚倍增子的棋串明细同时读到数量 9、生效指数 5、倍率 7.59375。
- [ ] 1.4 断言封顶不影响其他效果。验证：单元测试——倍增子×7 + 协同子×1，协同加值为 2（类型数 1），棋串气数与 8 子串一致，只有倍率指数取 5。
- [ ] 1.5 更新溢出守门：封顶后 `3^5` 不可能溢出，checked 保留；改写原"n ≤ 80"注释与相关测试。验证：全量测试通过，且 checked 仍在（变异：去掉 checked 应有测试变红，若无则补一条以 `long.MaxValue` 为基础军势的溢出测试）。

## 2. 既有守门与遥测

- [ ] 2.1 跑全量测试，定位并改写依赖"倍率无上限"的断言（territory-power 阶段的单调性/无界测试）为"n ≥ 5 时倍率恒为 243/32"。验证：全量测试通过，改动的测试逐条列在提交信息里。
- [ ] 2.2 遥测倍率峰值记录保留原始倍增子数量，并可得生效指数。验证：单元测试——峰值串含 8 枚倍增子时，记录数量 8、生效指数 5。
- [ ] 2.3 `Siege.Sim` 快照的棋串明细加"生效倍率指数"，分析报告"高倍率棋串"段落双列输出（原始数量分布 / 生效指数分布）；旧日志缺该字段时按 `min(数量, 5)` 回填。验证：单元测试——用 heuristic-ai 归档前格式的日志行解析成功并回填正确。

## 3. 设计文档

- [ ] 3.1 设计文档 §10.1 公式改为封顶版，保留 v1.0 示例，加"生效倍率指数 = min(倍增子数量, 5)"说明；§9.2 倍增子效果加"至多 5 枚生效"。验证：人工检查——公式、示例、§9.2 三处一致。
- [ ] 3.2 设计文档标题版本记为 v1.1，文末加变更记录（日期、change `cap-multiplier`、36 局实测依据、推翻 territory-power 裁决 3）。验证：人工检查。

## 4. 变异验证与回归

- [ ] 4.1 对 1.1–1.4、2.2 的每个新测试做变异验证并记录在测试注释（改封顶常量为 6 / 去掉 min / 显示用原始指数 / 明细两个字段互换，各应变红）。验证：注释中有 `M-` 记录，红数与实际一致。
- [ ] 4.2 200 局 Standard 回归（`MaxMajorRounds=30`）：记录峰值军势分布（中位数、最高）、倍增子选择率、不收敛率、平均大回合数，与 heuristic-ai 归档时的 36 局对照，写入本 change design.md 的裁决记录。验证：跑局统计——峰值军势最高值降到 10³ 量级以内；其余指标如实记录，不作为通过条件。
