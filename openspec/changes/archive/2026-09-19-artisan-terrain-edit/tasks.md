## 0. strict-cli（段 O，独立 change `openspec/changes/strict-cli`）

- [x] 0.1 按 `strict-cli` 的 tasks 执行并归档该 change（守门测试先行、`CommandLine` 消费记录、未知选项报错与建议、各子命令收尾调用）。验证：该 change 的 tasks 全绿；实跑 `--matches 200` 非零退出且不建目录；正常跑局不受影响。

## 1. 匠人棋子（段 A）

- [x] 1.1 `PieceType` 新增匠人；基础军势 1。算例（`piece-effects`）：匠人×2 + 堡垒子 = 6；匠人落子后无持续效果。验证：单元测试；守门——军势表穷举六种类型。
- [x] 1.2 征募：基础权重表加匠人 10，权重可由对局配置覆盖；徽记绑定类型枚举六种。算例（`recruitment`）：默认池 40/20/18/12/10/10、匠人概率 10/110；配置为 18 时其余不变。算例（`relic-generation`）：大量种子下六种徽记各约六分之一。验证：单元测试 + 分布统计测试。
- [x] 1.3 手牌与类型槽：六种类型挤不进默认 5 槽时的整类弃牌路径。算例（`hand-management`）：已持有五种再选匠人被拒并说明槽位已满。验证：单元测试。
- [x] 1.4 改写既有测试：所有"五种棋子""穷举 PieceType"的用例改为六种，逐条列入 `implement.md`（文件、旧期望 → 新期望 → 依据）。**注意**：徽记绑定从 5 类变 6 类会改变既有种子的信物生成结果（R-1），凡钉住具体信物内容的测试须重新取值并注明。验证：全量测试绿。

## 2. 改造规则（段 B）

- [x] 2.1 地形写入口唯一实现：在 `TerrainData` / `MapData` 之上提供加桥、加栅、烧林三种不可逆写入，产出新地形快照；写入后 `GameBoard` 的气边、覆盖、棋串、气、据点与信物控制全部按新地形重算。验证：单元测试（架桥后两格同串、立栅后分串、烧林后可被覆盖）；守门——写入口之外不得直接构造带不同设施的 `TerrainData`，做变异。
- [x] 2.2 改造合法性唯一实现（`terrain-edit`「改造动作集」「匠人落子即改造」「批次内不链式」）：目标类型匹配、几何四邻、未被改造过、批内唯一、按批次开始前地形判定；非匠人带目标非法。算例：全部相关 Scenario 逐条。验证：单元测试逐条；变异——去掉"批内唯一"、改用气边口径、允许非匠人带目标，各应红。
- [x] 2.3 批次条目可带一个改造目标；额度与匠人共用。算例（`batch-deployment`）：上限 3 时三枚带改造的匠人合法、已用额度 3。验证：单元测试。
- [x] 2.4 预演与结算顺序：预演第 4 步、正式结算第 3 步应用改造，先于提子。算例（`capture-resolution`）：立栅致提子；改造导致的自杀手；多个改造同时生效与顺序无关；烧林后的揭示与控制。验证：单元测试逐条；变异——把改造挪到提子之后应红。
- [x] 2.5 同形表示加入设施与地表；旧存档按"无改造"回填。算例：棋子分布相同但多一道栅栏 → 不同形。验证：单元测试 + 存档往返测试（含旧存档读入）。
- [x] 2.6 全仓排查缓存地形派生数据的位置（气边表、覆盖表、距离表、AI 缓存等），逐处确认改造后失效或重算，清单写入 `implement.md`。验证：针对每处写一条"改造后结果变化"的测试，或说明该处不缓存。

## 3. AI、跑局与遥测（段 B）

- [x] 3.1 AI 枚举改造目标（design D-J）：对每枚暂放匠人枚举全部合法目标加"不改造"；记录扫档中每局 AI 计算耗时，与第二轮 19.2 秒/局对照；若显著上升则退到"只枚举致提子的改造 + 不改造"并记录。验证：单元测试（AI 在能一手立栅提子时选择该手）+ 跑 20 局看耗时。
- [x] 3.2 `RunConfig` 支持匠人权重（未配置取 10），写入批次 `config.json`。算例（`simulation-harness`）：匠人权重 18 + 分值 3/8/24 + Safety 7 如实写出。验证：单元测试 + 跑 1 局核对（`strict-cli` 已上线，未知选项会报错）。
- [x] 3.3 日志：改造事件（大回合、小回合、改造方、动作、目标、是否致提子）；配置记录匠人权重。算例（`match-telemetry`）：回放全部改造记录可重建终局地形。验证：场景测试逐条。
- [x] 3.4 分析第 11 项改造分析（含烧林 0 次也照常输出、缺字段旧日志整局排除并计数）。验证：单元测试手算样本 + 跑 20 局看报告。

## 4. 界面（段 C）

- [x] 4.1 `Siege.Presentation`：公开视图含当前设施与（暂放匠人时）合法改造目标；预演显示改造目标与动作、按改造后地形算气与自杀风险；棋串读法区分栅栏侧。验证：`batch-preview`、`tactical-layers`、`information-visibility` 相关场景测试；守门——公开视图不含改造者。
- [x] 4.2 Godot：匠人轮廓（工具或支架状）；可改造目标高亮（深水格 / 林地格 / 边）；改造落成的一次可察觉反馈；新设施与预置设施外观一致；被烧林地呈现为草地。验证：`--build-solutions`、`--auto-demo`、`--auto-demo --pick-check`（105/105）退出码 0；截图 `art/artisan-v4/`（暂放匠人时的目标高亮、搭桥前后、立栅前后、烧林前后），`README.md` 写人工清单（六种棋子轮廓可辨、三种改造前后可辨、灰度下可辨、改造反馈可察觉）。
- [x] 4.3 Godot 源码级守门：`src/godot/` 不自算改造合法性（token 表加改造合法性与地形写入口的入口名）。验证：守门测试 + 变异。

## 5. 校准（段 D，主会话执行）

- [x] 5.1 匠人权重三档（5 / 10 / 18）× 200 局（种子 1–200，分值 5/15/45、Safety 27），输出到 `sim-out/artisan-w{5,10,18}/`。验证：对比表（design D-I 第 3 步所列指标），给出推荐档。
- [x] 5.2 在推荐档下复查 Safety 25 / 27 / 30 各 200 局，输出到 `sim-out/artisan-safety{25,27,30}/`。验证：对比表 + 推荐值。
- [x] 5.3 负责人拍板后：落默认匠人权重（必要时连同 Safety），钉住默认值的测试随之改；确认跑 200 局（`sim-out/artisan-final/`）与拍板那一档一致（比例项 ±3 个百分点、回合数 ±0.3）。验证：全量测试绿 + 对照表。

## 6. 文档与规范（段 E）

- [x] 6.1 设计文档 v1.3 → v1.4：§2 术语补匠人与三种改造；§3.x 地形可变说明；§6.3 结算顺序加"应用改造"；新增改造一节（动作集、合法性、时机、不可逆）；§10.1 棋子表加匠人；§13.1 公开信息加设施；§14.2 战术层；§16 按拍板数据重定；§17 第 11 项；§20 匠人轮廓与改造反馈；变更记录加一行。§16 只在 5.3 之后改。验证：人工检查；规则类算例与规格场景逐条对得上。
- [x] 6.2 `.trellis/spec/core/boundaries.md` 补"地形写入口""改造合法性"两行，并写明"地形不再是对局内不变量"；`testing.md` 补本轮教训（如有）。验证：人工检查。
- [x] 6.3 变异验证汇总表：段 O–E 全部变异逐条（编号、变异、红掉的测试 / 命令、还原确认）。验证：`implement.md` 表格齐全。
