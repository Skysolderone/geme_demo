> 执行进度在 Trellis 维护：`.trellis/tasks/09-12-recruit-hand/implement.md`。本文件是规格侧快照，勾选状态不在此更新。

## 1. 手牌账目模型

- [ ] 1.1 定义手牌结构为"类型 → (回合前基数, 本轮新增数)"两段账。验证：结构可区分同类型的新旧棋子。
- [ ] 1.2 实现小回合开始时的折叠（上一回合总数 → 回合前基数，本轮新增清零）与确认落子后的折叠。验证：连续三个小回合的账目转换有显式测试。
- [ ] 1.3 实现已占用槽位由"数量 > 0 的类型数"派生，不单独存储。验证：任意操作后槽位数与类型数始终一致，不存在槽位泄漏。
- [ ] 1.4 实现初始手牌发放（每人 5 枚普通子，计入回合前基数）。算例（设计文档 §9.1）：开局手牌 5 枚普通子、占用 1 槽；第一小回合 Pass 不撤销它们。
- [ ] 1.5 实现"手牌是否为空"查询。验证：全部部署后返回是，供出局判定使用。

## 2. 类型槽与弃牌

- [ ] 2.1 实现类型槽为种类维度约束，同类型无限叠加。算例（设计文档 §5.2）：14 枚普通子只占 1 槽；普通子×9 + 堡垒子×1 → 占 2 槽、余 3 空槽。
- [ ] 2.2 实现满槽拒绝新类型、允许叠加已有类型。算例：5 槽满时面板出现第 6 种类型 → 拒绝并说明无可用槽；出现已有类型 → 允许，槽位不变。
- [ ] 2.3 实现主动整类弃牌（仅限整理手牌阶段，整类为单位，不返还资源）。算例（设计文档 §5.2）：弃掉全部 3 枚协同子 → 空出 1 槽；尝试只弃 2 枚堡垒子 → 拒绝。
- [ ] 2.4 实现槽位缩水时的阻断式强制弃牌门（超限未解除则征募阶段不开始）。算例（设计文档 §5.2）：持 6 类、快照槽位 5 → 必须弃至少 1 整类后方可征募。
- [ ] 2.5 断言弃牌不被 Pass 撤销。验证：本轮弃牌后 Pass，被弃类型不恢复。

## 3. 征募权重与抽取

- [ ] 3.1 实现基础棋池权重（普通 40 / 堡垒 20 / 连珠 18 / 倍增 12 / 协同 10）。验证：无徽记时大样本分布与权重表偏差在容差内。
- [ ] 3.2 实现徽记调权 `基础 × (1 + 0.75 × 数量)`，用整数运算 `× (4 + 3n) / 4` 避免浮点。算例（设计文档 §9.1）：1 枚倍增子徽记 → 12→21；1 枚高阶连珠子徽记（数量 2）→ 18→45；堡垒子徽记合计数量 4 → 20→80。
- [ ] 3.3 实现调整后的重新归一化（整数累积权重 + 整数取模抽样，不做浮点归一化）。验证：权重和为任意值时抽样分布仍正确。
- [ ] 3.4 实现每个候选位独立抽取、允许重复类型。算例（设计文档 §9.1）：展示 5 枚的面板允许出现多枚同类型。
- [ ] 3.5 实现 `recruit` 随机子流与 `relic-gen` 子流的隔离。验证：改变某玩家的一次征募选择后，信物生成结果逐格不变。

## 4. 征募面板与选取

- [ ] 4.1 实现按效果快照生成面板（展示数默认 5、免费选取数默认最多 3）。算例（设计文档 §5.3/§8.1）：快照展示 7、选取 4 → 面板 7 枚、最多选 4 枚。
- [ ] 4.2 断言无金币、无付费刷新、无基础免费刷新。验证：不存在任何刷新入口。
- [ ] 4.3 实现"最多选取"为上限而非强制。验证：玩家可以少选或不选而正常进入部署阶段。
- [ ] 4.4 实现未选候选在小回合结束时消失。算例（设计文档 §5.3）：展示 5 选 3 → 剩余 2 枚不进手牌也不结转。
- [ ] 4.5 实现满槽时不可选候选的呈现（照常展示并标注原因，不隐藏）。验证：面板返回的候选带可选性标志与拒绝原因。

## 5. 库存扣减与 Pass 撤销

- [ ] 5.1 实现结算第 1 步的部署扣减，优先从"本轮新增"扣。算例（设计文档 §6.3）：连珠子×3 部署 2 枚 → 结算后库存 1，槽位仍占用。
- [ ] 5.2 实现清零释放槽位。算例：协同子×1 全部部署 → 类型移除、槽位释放。
- [ ] 5.3 断言暂放不扣减库存。验证：暂放 3 枚未确认 → 库存不变。
- [ ] 5.4 实现 Pass 撤销：清零"本轮新增"，不动回合前基数。算例（设计文档 §5.5）：回合前普通子×4 + 本轮新增×2 → Pass 后为 4，不是 0 也不是 2。
- [ ] 5.5 断言撤销释放槽位。算例：本轮首次获得倍增子×1 → Pass 后该类型移除、槽位释放。
- [ ] 5.6 断言"落子 ≥1 枚即全保留"。算例（设计文档 §5.5）：本轮征募 3 枚只部署 1 枚 → 其余 2 枚保留。
- [ ] 5.7 断言无新增时 Pass 不影响已有手牌。算例：回合前 7 枚、本轮未征募 → Pass 后仍 7 枚。

## 6. 信息边界

- [ ] 6.1 实现手牌的私有视图（类型 → 数量）与公开视图（类型集合），公开视图 MUST NOT 含数量字段。验证：结构级断言 + 对手读取路径测试。
- [ ] 6.2 实现征募面板为当前行动玩家私有。算例（设计文档 §13.2/§15.1）：玩家 A 征募期间，B/C/D 与其对战 AI 无法读取候选内容或选择过程。
- [ ] 6.3 实现弃赛玩家的最后公开手牌类型保留与"不再行动"标记。算例（设计文档 §14.3）：D 弃赛时持普通子与连珠子 → 保留展示这两种类型并标记。
- [ ] 6.4 为调试 AI 的全量读取预留显式标记为测试专用的旁路。验证：旁路在非测试模式下不可达（设计文档 §15.3）。

## 7. 记录与回归

- [ ] 7.1 输出征募记录（每小回合的全部候选、被选取的候选、Pass 撤销数量）。验证：对局日志满足设计文档 §17 的记录要求。
- [ ] 7.2 输出"本回合落子数为 1 且势力无变化"的可统计信号。验证：遥测可统计该信号频次，用于分析玩家是否以最小落子规避 Pass 撤销（设计文档 §17）。
- [ ] 7.3 实现同种子同决策序列的完整重放。验证：重放一局，每轮候选与最终手牌逐项一致。
- [ ] 7.4 建立征募与手牌回归用例集，覆盖：两段账 Pass、同类型只扣新增、类型槽为种类维度、满槽规则、强制弃牌阻断、徽记调权三例、子流隔离、公开视图无数量。验证：全部通过并纳入持续回归。
