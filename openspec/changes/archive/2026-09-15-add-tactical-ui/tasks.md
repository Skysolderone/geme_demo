> 执行进度在 Trellis 维护：`.trellis/tasks/09-12-tactical-ui/implement.md`。本文件是规格侧快照，勾选状态不在此更新。

## 1. 信息边界

- [ ] 1.1 定义 `公开视图` 与 `当前玩家私有视图` 两个独立结构。验证：公开视图结构中不存在未发现信物内容、征募面板、对手手牌数量、对手暂放批次四类字段。
- [ ] 1.2 实现 §13.1 六类始终公开信息的完整暴露。算例（设计文档 §13.1）：A 落下倍增子 → 所有玩家可见其玩家与类型；B 查看 A 的势力 → 可见领地分与每条棋串的基础军势/位置加值/倍率/最终军势；任意棋串的气数与气位可推导。
- [ ] 1.3 实现 §13.2 四类隐藏信息在数据层的排除。算例：未覆盖的信物格 → 仅"未知信物"标记；B 查看 A 手牌 → 仅类型；A 暂放中 → 他人界面无任何提示。
- [ ] 1.4 与 `add-heuristic-ai` 共用同一套公开视图定义。验证：两者引用同一结构，不存在两份定义。

## 2. 批次预演

- [ ] 2.1 约定预演结果载荷必须包含六项信息（落点/类型/手牌消耗、部署上限与已用数、预计被提棋串、己方棋串与气与自杀风险、各棋串军势预览、势力与排名变化）以及失败类别与坐标、"将揭示"标志。验证：载荷结构断言，缺项即失败。
- [ ] 2.2 实现 UI 只消费预演结果，不做任何规则计算。验证：UI 层不依赖棋串/气/军势的计算模块。
- [ ] 2.3 实现预计提子的呈现。算例（设计文档 §14.1）：暂放将使敌方 5 枚棋串无气 → 明确标示该棋串将被提走。
- [ ] 2.4 实现自杀风险呈现。算例：暂放组合使己方某棋串结算后仍无气 → 标示风险并高亮该棋串。
- [ ] 2.5 实现军势预览呈现。算例：暂放倍增子接入某棋串 → 显示该棋串提交后的基础军势、位置加值、倍率与最终军势。
- [ ] 2.6 实现势力与排名变化呈现（含他人名次被挤动）。算例：势力 45→62、排名 3→2 → 完整显示变化。
- [ ] 2.7 实现部署额度呈现。算例：上限 5、已暂放 3 → 显示"3 / 5"。
- [ ] 2.8 实现"将揭示"标志的呈现，不提前显示信物内容。算例（设计文档 §14.1）：暂放使某未知信物格首次进入覆盖范围 → 显示"将揭示"，不显示类型与强度。
- [ ] 2.9 实现七类非法原因的说明与高亮。算例（设计文档 §14.1/§6.1）：自杀手 → 高亮结算后仍无气的己方棋串；同形 → 指出重复的历史提交；预占腾空格 → 文案为"该格当前已被占据"。
- [ ] 2.10 断言预演零副作用。验证：反复暂放/撤销/换位并多次查看预演 → 正式盘面与全部玩家状态序列化不变。
- [ ] 2.11 实现 Pass 确认前的撤销警示。算例（设计文档 §5.5）：本轮新征募 3 枚且暂放为 0 → 确认前提示"确认 0 落子将撤销本轮新征募的 3 枚棋子"。

## 3. 战术信息层

- [ ] 3.1 实现领地层（弱化棋子演出，呈现占据/独占/争议/中立四态）。验证：四态可区分。
- [ ] 3.2 实现气层（棋串轮廓、全部气、危险棋串），危险阈值可配置（初值：气 ≤2 危险、气 =1 紧急）。算例：只剩 1 口气的己方棋串 → 标示为紧急。
- [ ] 3.3 实现势力层（每格领地贡献、各棋串分数、倍率热区）。验证：显示内容与 `势力明细` 一致，不自行重算。
- [ ] 3.4 实现信物层（内容、强度、控制者、失效原因），未发现内容继续隐藏。算例（设计文档 §14.2）：争议信物 → 标示争议并说明不向任何玩家提供效果；未发现信物 → 仍显示为未知。
- [ ] 3.5 实现顺序层（势力名次、先手修正、先手值、预测的下一大回合顺序）。验证：玩家可据此解释下一轮排序，数据来自 `先手值明细`。
- [ ] 3.6 实现信息层互斥（一次只显示一层，切换即自动退出当前层）与默认棋盘上的统一"未知信物"标记（不区分预算分区，分区信息仅在信物层可见）。验证：切换后前一层退出；两个分属不同分区的未发现信物格在默认棋盘上显示相同。
- [ ] 3.7 实现按住进入 / 松开退出的临时信息模式。验证：按住期间显示，松开立即恢复默认棋盘。
- [ ] 3.8 实现键鼠与手柄分别配置战术视图键。验证：两套配置互不影响。
- [ ] 3.9 实现辅助设置的"点击切换"，与"按住显示"共用同一可见性状态机。验证：两种模式下信息层内容与行为完全一致。
- [ ] 3.10 断言信息层在自己部署期间与他人行动期间均可查看。验证：两种时机各有用例。
- [ ] 3.11 断言信息层为纯读视图、零副作用、不越权。验证：任意次开关后对局状态序列化不变；任意信息层均不显示四类隐藏信息。

## 4. 手牌信息面板

- [ ] 4.1 实现面板开关（点击按钮打开，再次点击或返回关闭）。验证：开关状态不影响对局。
- [ ] 4.2 实现自己区域（类型、准确数量、槽位占用、本轮新征募未提交数量）。算例（设计文档 §14.3）：持普通子×6、堡垒子×1 → 显示准确数量；本轮新征募普通子×2 → 标示其中 2 枚为本轮新增；3 种类型/上限 5 → 显示已占 3、余 2。
- [ ] 4.3 实现敌方区域只显示类型。算例：A 持普通子×6、堡垒子×1 → 他人只见两种类型，不见数量、征募与暂放。
- [ ] 4.4 实现公开结构参数与信物来源（展示数、选取数、手牌槽、部署上限及各自来源）。算例（设计文档 §14.3）：B 部署上限 5，其中 +2 来自两枚军令 → 显示数值并标注来源。
- [ ] 4.5 实现弃赛玩家的表现（保留最后公开手牌类型 + 清楚标记不再行动）。算例：D 弃赛时持普通子与连珠子 → 保留展示并标记已弃赛。

## 5. 视觉风格基准

- [ ] 5.1 建立风格基准文档，引用 `art/style-exploration/siege-style-03-low-poly-fantasy.png`，并明确其只用于确定材质、色彩、轮廓与信息层级。验证：文档中明确写出"不代表最终地图布局与固定 UI 尺寸"，以及 `ROUND 7` 仅表示当前大回合。
- [ ] 5.2 实现四方势力的红/蓝/金/紫主色 + 旗帜图形 + 棋子轮廓三重区分。验证：去色条件下四方势力仍可区分。
- [ ] 5.3 断言方格边界始终清晰、装饰不遮挡判读。验证：在含树木/遗迹/水面/高低差的地块上，合法落点、气与领地归属仍清晰可辨。
- [ ] 5.4 实现五种棋子的轮廓语言（圆润 / 塔楼体块 / 连接关系 / 放射状 / 多节点聚合）。验证：去色的小尺寸缩略图中五种轮廓彼此可区分。
- [ ] 5.5 实现默认 UI 风格（深色半透明面板、克制金色边框、高对比信息色、PC 策略游戏信息密度）。验证：不存在手游式大按钮与夸张弹窗。
- [ ] 5.6 实现批次预览的半透明发光与预计提子的虚线/轮廓提示，两者视觉可区分。验证：同屏出现时可分辨。
- [ ] 5.7 实现信息层的临时降饱和处理。验证：打开势力层时场景饱和度与装饰对比被压低，退出后恢复。

## 6. 回归

- [ ] 6.1 建立信息边界回归用例集，覆盖：公开视图无四类隐藏字段、敌方手牌只见类型、暂放不外泄、未发现信物不泄漏、信息层不越权。验证：全部通过并纳入持续回归。
- [ ] 6.2 建立预演一致性回归：对随机生成的局面，断言 UI 显示的预计提子集合与实际结算的提子集合完全一致。验证：1000 例随机局面全部一致。
- [ ] 6.3 断言 UI 层不包含规则计算。验证：依赖检查通过，UI 不引用棋串/气/军势计算模块。
