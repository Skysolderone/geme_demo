## 1. 实现

- [x] 1.1 先写测试：`ai-decision`「停手阈值」三个新 Scenario（无子时不受阈值限制；有子后恢复阈值；同批次内不中途切换）。验证：先红。
- [x] 1.2 实现 D1：决策入口按正式盘面一次算定实际阈值，单点排序与贪心组批共用。验证：1.1 全绿；既有「停手阈值」Scenario 全绿；变异（去掉豁免时"无子时不受阈值限制"应红；按暂放后盘面判定时"同批次内不中途切换"应红）。
- [x] 1.3 黄金哈希 / 依赖走法的期望若分叉，按 AI 行为变更重建，探针（临时去掉豁免）证明分叉只来自本改动。

## 2. 验证与收尾

- [x] 2.1 简单难度 v5、种子 1–20、4 名 Easy、默认值冒烟：首回合全员 Pass 局数（期望 0）、截断、终局原因、结束大回合；数据 `sim-out/pass-threshold-first-stone/easy20/`。只报告。
- [x] 2.2 设计文档 §15.2 补"无子豁免"，版本 v1.10 → v1.11，变更记录加一行。
- [x] 2.3 全量回归：两处 `dotnet build` 零警告；`dotnet test -c Release` 全绿；`openspec validate pass-threshold-first-stone --strict` 通过（不跑 200 局规模的慢测试）。
