> 执行进度在 Trellis 维护：`.trellis/tasks/09-12-board-core/implement.md`。本文件是规格侧快照，勾选状态不在此更新。

## 1. 盘面状态模型

- [ ] 1.1 定义格子数据结构：坐标、地形（可落子/障碍）、出生区归属、是否信物格、占用（玩家+棋子类型）。验证：单元测试断言障碍格写入占用被拒绝；信物格可正常写入占用且信物标记不变。
- [ ] 1.2 定义棋盘容器与围棋记法坐标（列字母 `A`–`L` 跳过 `I`、行数字自下而上从 1 起），并实现与内部索引的唯一双向映射。验证：单元测试断言 `A1` 的邻居集合恰为 `{B1,A2}`；11 列字母序列为 `A B C D E F G H J K L`；任意标识往返映射后不变。
- [ ] 1.3 实现最小写入原语（设置占用、清除占用），不含任何规则判定。验证：单元测试断言写入/清除后 `取格子` 返回值一致，且不触发任何提子逻辑。

## 2. 四邻接与派生计算

- [ ] 2.1 实现唯一的四邻接邻居遍历，作为全项目唯一入口。验证：全仓搜索确认无第二处邻居遍历实现；单元测试断言斜向不被返回。
- [ ] 2.2 实现棋串计算（同玩家、四邻接洪泛、跨棋子类型合并）。算例（设计文档 §9.2）：同玩家普通子 `C3`、堡垒子 `C4`、倍增子 `C5` → 返回单个 3 枚棋子的棋串。
- [ ] 2.3 实现气计算（相邻空的可落子格去重；障碍、越界、任意玩家棋子均不计气）。算例（设计文档 §10.1）：棋盘中央孤立棋子 → 气数 4；上方改为障碍 → 气数 3。
- [ ] 2.4 实现无气判定。算例：一枚棋子四周全被敌方棋子/障碍/边界封堵 → 判定无气。
- [ ] 2.5 实现"枚举玩家棋串"与"存在可落子空格"查询。验证：单元测试断言棋盘填满时后者返回否（对应设计文档 §12.3 终局条件 3）。
- [ ] 2.6 断言所有查询无副作用。验证：连续执行 1000 次随机查询后盘面序列化结果逐字节不变。

## 3. 盘面序列化

- [ ] 3.1 实现确定性盘面序列化，内容仅含每格占用者与棋子类型，按固定坐标顺序遍历。验证：同一盘面重复序列化结果逐字节相等。
- [ ] 3.2 断言序列化排除非盘面信息。算例（设计文档 §6.2）：两份盘面占用完全一致但手牌与信物控制不同 → 序列化结果相等；同一玩家同一位置换用不同棋子类型 → 序列化结果不相等。

## 4. 地图数据与校验

- [ ] 4.1 定义地图数据格式：外接尺寸、地形网格、出生区（编号+归属格）、信物格位置、信物强度预算分区、咽喉格标注、校验豁免标记。验证：能完整往返读写一张手工地图且无信息丢失。
- [ ] 4.2 实现人数适配预算校验（2 人 50–65/2/7–9；3 人 75–90/3/10–12；4 人 100–115/4/13–15）。算例：4 人地图 130 可落子格 → 拒绝并报告超出 100–115。
- [ ] 4.3 实现障碍占比校验（占外接区域 8%–12%）。算例：11×11 地图 5 个障碍（约 4.1%）→ 拒绝并报告低于 8%。
- [ ] 4.4 实现出生区容量校验（每区 12–15 格，且可容纳 9 枚基础部署）。验证：模拟单玩家在区内连续三大回合各落 3 子且无提子，每步均存在合法空格。
- [ ] 4.5 实现出生区距离均衡校验（到最近公共信物格、中央入口、标注咽喉的四邻接绕障最短落子距离，两两差值 ≤ 容差，默认 1，逐图可放宽但必须记录理由）。算例：A 距离 4、B 距离 9 → 拒绝并输出出生区编号与两个距离值。
- [ ] 4.6 实现必死口袋保守校验（出生区内被障碍/边界围成的连通空区面积 < 8 即失败，支持逐图豁免）。验证：构造一个 4 格死角地图 → 拒绝并输出该口袋坐标。
- [ ] 4.7 实现信物格校验（必须位于可落子格、必须标注强度预算分区）与出生区数量校验（等于地图支持的最大人数）。验证：构造把信物格放在障碍上的地图 → 拒绝。
- [ ] 4.8 断言地图加载不消费随机数。验证：用 3 个不同对局种子加载同一地图，地形/障碍/出生区/信物格位置逐字节一致。

## 5. 4 人基准地图

- [ ] 5.1 手工制作 4 人基准地图（外接约 11×11、可落子格 100–115、4 个出生区各 12–15 格、每区 2 个信物格、公共区约 6 个、总计 13–15 个）。验证：通过第 4 组全部校验。
- [ ] 5.2 标注中央区、交通咽喉与高风险边缘区的信物强度预算分区。验证：人工检查每个信物格都归属到出生区或公共争夺区之一，且公共区分区标注覆盖中央与咽喉。
- [ ] 5.3 记录本图的距离容差（默认 1）、两眼最小格数（8）与任何豁免项及其理由。验证：人工检查文档化的每一条豁免都有明确理由。

## 6. 契约与回归

- [ ] 6.1 固化下游只读契约（取格子/取棋串/取气/枚举玩家棋串/存在可落子空格/序列化盘面/加载地图）。验证：契约级测试覆盖每个接口的正常与边界输入。
- [ ] 6.2 建立拓扑回归用例集，覆盖：边角格、障碍贴边、跨类型棋串、共享气去重、敌我相邻不合并、棋盘填满。验证：全部用例通过，并纳入持续回归。
