## 1. 校验区间

- [ ] 1.1 `map-definition` 校验放宽：障碍占比 25%–35%、可落子格 80–95、出生区每区 12–14 格；新增"可落子格总数"校验项。算例：109 格的旧图被拒并报告越界；12 格障碍（9.9%）被拒并报告低于 25%。验证：单元测试。
- [ ] 1.2 出生区容量校验保持不变（前三大回合 9 枚部署不卡死）。验证：既有测试仍绿。

## 2. 新版地图

- [ ] 2.1 设计新障碍布局：11×11 外接，障碍约 36 格，可落子格约 85，四区各 12–14 格，信物格仍 14（出生区 8、公共 6），保持 D2 对称。验证：`MapValidator` 全项通过。
- [ ] 2.2 距离容差 ≤ 1：四区到最近公共信物、中央入口、咽喉的最短绕障距离两两差 ≤ 1。验证：校验器通过；测试断言四组距离。
- [ ] 2.3 导出 `maps/siege-4p-base-v2.json`，`map` 子命令打印新图。验证：人工看图 + 导出文件存在。
- [ ] 2.4 地图 Id 与引用更新。验证：全量测试通过。

## 3. 既有测试

- [ ] 3.1 改写写死尺寸 / 可落子格 / 障碍数 / 出生区格数的测试。逐条列入提交信息。
- [ ] 3.2 棋盘几何类测试若用到被新障碍占用的坐标，**换坐标重构用例**，不得改断言期望值。逐条列入提交信息并说明新坐标为何等价。

## 4. 遥测与文档

- [ ] 4.1 报告首次冲突段落补"盘面占用率"（首次提子时盘面棋子数 ÷ 可落子格），批次均值与分布。验证：单元测试，手算样本核对。
- [ ] 4.2 设计文档 §3.3 更新地图描述与障碍占比，§16 说明冲突时点由密度决定；变更记录加一行。验证：人工检查。

## 5. 变异验证与回归

- [ ] 5.1 变异验证并记录：校验区间写反、可落子格校验缺失、障碍集合少一格仍通过校验、距离容差判定用直线距离而非绕障。
- [ ] 5.2 200 局回归：首次冲突大回合与占用率、平均结束大回合、终局原因分布、第 3 大回合领先者胜率、各棋子势力占比，与 catch-up-recruit 之后对照。验证：如实记录；若平均结束大回合 < 7 立即报告。
