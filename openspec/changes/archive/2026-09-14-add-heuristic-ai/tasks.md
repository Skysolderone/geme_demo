> 执行进度在 Trellis 维护：`.trellis/tasks/09-12-heuristic-ai/implement.md`。本文件是规格侧快照，勾选状态不在此更新。

## 1. 信息边界

- [ ] 1.1 定义两种状态视图类型：`正式视图`（公开信息 + 自身私有信息）与 `调试视图`（完整权威状态）。验证：正式视图的结构中不存在未揭示信物内容、敌方手牌数量、敌方暂放批次等字段。
- [ ] 1.2 实现正式 AI 只接受正式视图。算例（设计文档 §15.1）：AI 查询对手手牌 → 只返回类型集合；查询未揭示信物 → 不返回类型与强度；查询对手暂放 → 不可达。
- [ ] 1.3 实现调试视图的构造入口显式标记测试专用，并在非测试模式下不可达。验证：非测试模式尝试构造 → 拒绝并说明仅供测试。
- [ ] 1.4 实现基于公开输入的概率估计（分区权重表、已揭示分布、对手公开类型与公开参数）。算例（设计文档 §8.2）：评估出生区未揭示信物 → 使用 徽记45/探勘20/兵站15/征召8/军令7/先锋5 作为先验。
- [ ] 1.5 断言估计与真实值无相关性。验证：大样本统计 AI 对未知信物的估计与真实内容的相关系数在噪声范围内。

## 2. 启发式评价

- [ ] 2.1 实现七维评价函数（即时势力增量、敌方势力损失与提子规模、信物发现/控制/阻断价值、剩余气与两眼潜力与被围杀风险、连珠/倍增/协同组合成长、下一大回合先手位变化、供给与部署匹配度）。验证：打分结果可分解为七个维度的贡献值。
- [ ] 2.2 实现两眼潜力的启发式近似（气数、气的分布、是否存在两个互不相邻的单点气），并在分解中单列。验证：人工构造的活形与死形局面上该维度给出显著不同的值。
- [ ] 2.3 实现权重配置化。验证：修改权重后同一局面的候选排序发生变化。
- [ ] 2.4 断言先手位维度生效。算例（设计文档 §11.2）：某候选批次使 AI 势力名次上升一位 → 该维度贡献为正。
- [ ] 2.5 实现评价并列的确定性打破（落点坐标字典序）。验证：两个同分候选 → 选择结果确定且可复现。

## 3. 候选生成与剪枝

- [ ] 3.1 实现单点评价与前 N 高价值落点筛选（N 按难度配置）。验证：N 可配置且筛选结果确定。
- [ ] 3.2 实现候选批次组合（贪心 + 种子驱动的随机扰动多次采样），数量上限 M 按难度配置（初值：简单 1 / 标准 8 / 高难 32）。算例（设计文档 §15.2）：部署上限 8、60 个合法空格 → 候选批次数量不超过 M，不枚举全部组合。
- [ ] 3.3 断言随机扰动由种子驱动。验证：同种子同局面 → 候选集合完全一致。
- [ ] 3.4 建立"必须找到的妙手"回归局面集（如一手同时提子 + 抢信物 + 补气）。验证：在标准难度剪枝参数下仍能找到这些手。

## 4. 难度与可复现

- [ ] 4.1 实现至少两档难度：简单只用即时势力增量与敌方势力损失且 M=1（贪心一条，作为对照组）；标准 M=8、高难 M=32，并相应扩大 N 与评价深度。验证：两档 AI 在同一局面的候选数量差异符合配置。
- [ ] 4.2 断言高难度不越权。算例（设计文档 §15.2）：最高难度 AI 在正式模式下读取的信息集合与简单难度完全相同。
- [ ] 4.3 实现 AI 决策的完全确定性。验证：同种子 + 同局面 + 同权重 → 征募选择与批次部署完全一致。

## 5. 调试 AI 与人工接管

- [ ] 5.1 实现调试 AI（读取调试视图），并在对局记录中标注该局使用了调试 AI。算例（设计文档 §15.3）：测试模式下可读未揭示信物与敌方手牌数量。
- [ ] 5.2 实现人工接管：可在任意小回合的任意阶段接管任一 AI 玩家，并可交还。算例（设计文档 §15.3）：在征募阶段接管 → 该阶段由人工决策，其余阶段与其他玩家不受影响。
- [ ] 5.3 断言接管不破坏状态一致性。验证：接管前后保存/恢复对局，状态逐字段一致；交还后 AI 正常继续。
- [ ] 5.4 在对局记录中标注接管发生的玩家、大回合与阶段。验证：日志可追溯每次接管。

## 6. 批量跑局

- [ ] 6.1 实现批量跑局配置（地图、人数、各玩家难度与权重、种子范围、局数）。验证：配置可完整序列化，随结果一同保存。
- [ ] 6.2 实现并行执行，局间零共享状态。算例（设计文档 §17）：同一批种子串行与并行执行 → 每个种子的结果完全一致。
- [ ] 6.3 实现纯 AI 局的种子复现。验证：凭种子与配置重跑 → 每一步候选、选择、批次与结算结果一致。
- [ ] 6.4 实现失败局的最小复现信息保留（种子、配置、终止前完整日志）。验证：断言失败的局可单独重跑复现。
- [ ] 6.5 断言随机子流隔离。算例：改变某玩家的一次征募决策 → 该局信物生成结果逐格不变。
- [ ] 6.6 实测单局墙钟耗时并给出 5000 局的预估总时长。验证：产出实测数据，作为是否优化全量重算的依据。

## 7. 对局日志

- [ ] 7.1 实现"事件流 + 每小回合快照"的混合日志结构。验证：快照含各玩家势力明细、结构参数、控制信物、手牌类型；事件含征募候选、批次尝试、提子。
- [ ] 7.2 实现设计文档 §17 的七类记录（地图与种子与信物分布及揭示时间；征募候选/选择/Pass 撤销；批次落子/合法性结果/提子数/同形检查；信物控制变化/结构参数/行动顺序；每棋串的基础军势/位置加值含来源拆分/倍率/最终军势；势力排名变化/Pass/出局/弃赛/最终结果；小回合与大回合与整局耗时）。验证：逐类断言字段存在且可解析。
- [ ] 7.3 断言非法批次也被记录。算例：提交自杀手批次 → 日志记录该次尝试、失败类别与相关坐标。
- [ ] 7.4 断言日志可离线解析，无需重跑即可完成全部分析。验证：分析工具只读日志文件运行成功。
- [ ] 7.5 实现日志体积控制（事件流可选压缩；全量局只留快照，失败局与抽样局保留完整事件）。验证：5000 局的日志总体积在预算内。

## 8. 平衡分析与数值回归

- [ ] 8.1 实现离线分析工具（与跑局解耦）。验证：修改分析口径无需重跑对局。
- [ ] 8.2 实现默认排除调试局与人工接管局，并在报告中说明排除数量。算例（设计文档 §15.3）：混有 10 局调试 AI → 默认排除并注明。
- [ ] 8.3 实现 §16 六项数值目标回归：三阶段部署上限分布（3 / 3–5 / 5–8）、势力成长曲线、首次跨出生区冲突轮次（第 4–5）、4 人大回合平均耗时（≤3 分钟）、结束大回合数与整局时长（7–10 大回合 / 20–30 分钟）、第 3 大回合领先者最终胜率（≤50%，至少 200 局）。验证：每项指标有独立断言，偏离时报告明确指出方向与幅度。
- [ ] 8.4 实现"第 3 大回合领先者"的双口径报告（组内任一人获胜 / 必须该具体玩家获胜），并列时两种口径分别给出。验证：并列局面下两个口径的数值均被输出。
- [ ] 8.5 实现 §17 七个分析方向：领先者与胜者相关性、先手滚雪球、各棋子与信物的选择率/控制时长/胜率、高倍率棋串形成轮次与峰值与被摧毁概率、出生区胜率差异显著性、四条成长轴的获取顺序分布、最小落子拖延信号占比。验证：每个方向有独立输出段落与可核对的数字。
- [ ] 8.6 实现胜率类指标的置信区间（默认 2000 局，200 局为快速回归下限）。验证：报告给出区间而非单点值。
- [ ] 8.7 实现 AI 决策质量旁证指标（平均每批次提子数、自杀手尝试率、Pass 率）。验证：当这些指标显示 AI 近乎随机时，报告给出"数值结论不可信"的显式警告。

## 9. 回归

- [ ] 9.1 建立 AI 与跑局回归用例集，覆盖：正式视图无隐藏字段、调试入口非测试不可达、估计与真实值无相关、剪枝仍找到妙手、并列确定性、串并行一致、子流隔离、失败局可复现。验证：全部通过并纳入持续回归。
- [ ] 9.2 执行一次完整的 2000 局基线跑局并归档报告。验证：报告覆盖 §16 六项目标与 §17 七个方向，作为后续数值调整的基线。
