## 1. 规则实现（Siege.Core/Board）

- [x] 1.1 先写测试：`life-shape`「活形三态」新增四个 Scenario（单子两眼为未定；两子两眼为活；单子与活形棋串共享眼空间仍禁入；切出单子即不再是活形——含所有者自切失活与非所有者立栅被判"破坏活形"两半）。验证：新增的先红。
- [x] 1.2 在 `LifeShapeReport` 三态判定处加单子上限（D1），眼空间与眼值不变。验证：1.1 全绿；变异（去掉上限时"单子两眼为未定"应红；上限误用于 2 子时"两子两眼为活"应红）。
- [x] 1.3 既有测试中以单子棋串做"活"算例的夹具改为多子棋串，只改盘面不改期望，逐条记入 implement 记录；性质测试（`SIEGE_SLOW=1` 种子 1–200）无反例。

## 2. 下游确认与收尾

- [x] 2.1 黄金哈希等走法相关期望按"规则变更"重建，写明归因（用探针证明分叉来自单子上限）。
- [x] 2.2 v5 4 AI 50 局复核（与 2026-09-23 实验 K1 同口径）：整局无提子、结束大回合、终局单子活形占比（应为 0）、他人致失活（应为 0），与实验数字并列写入 implement 记录。
- [x] 2.3 设计文档 §6.4 三态表下加"单子不成活"一句，文末变更记录加一行。
- [x] 2.4 全量回归：`dotnet build` 零警告（含 Godot 工程）；`dotnet test -c Release` 全绿；`openspec validate life-single-stone --strict` 通过。
